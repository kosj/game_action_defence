# 콘텐츠 수명 · 경쟁 설계 (Content & Competition)

> **이 문서가 답하는 질문**
> ① 테마를 다 깨면 더 할 게 없어지는 문제를 어떻게 푸는가
> ② 유저간 경쟁 요소가 필요한가
> ③ 필요하다면 어떤 형태로 넣는가

---

## 0. 전제 점검 — 지금 구조는 "선형 캠페인"이 아니다

| | 실제 구현 |
|---|---|
| 테마 해금 | 교외=무료 · 도심=**메타 골드 400 구매** · 연구소=**'15분 생존' 도전과제** |
| 한 판 | 테마 **선택** → 30분 생존 → 클리어 후 **오버타임 무한** |

즉 이미 **로그라이트 런 기반**이다. "테마 클리어 → 다음 테마 해금"으로 바꾸면 그때 비로소
선형 캠페인이 되고, **마지막 테마를 깨는 순간이 곧 콘텐츠의 끝**이 된다.

테마 하나에는 아트·프롭·기믹·보스가 통째로 붙는다(`ENVIRONMENT_PLAN`·`BOSS_PLAN` 참고).
**공급이 소비를 절대 못 따라간다.** 그래서 이 문서는 선형 해금으로의 전환을 권하지 않는다.

### ⚠️ 그보다 앞서는 현실

`BALANCE.md` §3 실측 기준 **30분 클리어에 도달한 런이 아직 하나도 없다.**
"끝나고 나서 뭘 하지"는 **첫 클리어가 성립한 뒤의 문제**다. 아래 계획은 전부
그 전제 위에 있고, 선행 항목은 `HANDOFF.md` P1-5(후반 이속)와 보스 조정의 사람 검증이다.

---

## 1. 장르가 콘텐츠 고갈을 푸는 방식

| 사례 | 스테이지 수 | 수명을 만든 것 |
|---|---|---|
| **Vampire Survivors** | 6~8개 | 무기×진화 조합표 + 도감. **Hyper·Inverse·Hurry** 로 같은 스테이지를 3배로 변주 |
| **Slay the Spire** | 3막 고정 | **Ascension 20단계** — 클리어할 때마다 규칙이 하나씩 추가. 신규 콘텐츠 제작 0 |
| **Hades** | 4구역 고정 | **Pact of Punishment** — 플레이어가 난이도를 직접 조립 |
| **Balatro** | 고정 | **스테이크(난이도 계단) + 데일리 챌린지 + 도감** |

공통점은 하나다 — **"콘텐츠를 더 만든다"가 아니라 "이미 있는 것을 다르게 플레이할 이유를 만든다."**

이 게임에는 이미 무기 28 · 진화 12 · 패시브 10 · 좀비 11 · 보스 · 캐릭터 3 · 테마 3 이 있다.
**부족한 건 콘텐츠가 아니라 그것들을 다시 꺼내 쓸 이유다.**

---

## 2. 단계별 계획

### Phase A — 위협 등급 (Threat Rank) ✅ **완료** (HANDOFF P1-12)

클리어할 때마다 등급이 해금되고, 등급마다 규칙이 **하나씩 누적**된다.

| 등급 | 누적 규칙(초안 — 실측으로 조정) |
|---|---|
| 1 | 기본 |
| 2 | 적 체력 +10% |
| 3 | 보물상자 주기 +25% (상자가 귀해진다) |
| 4 | 보스 자가회복 +1회 |
| 5 | 시작 체력 −1 |
| 6 | 엘리트 팩 주기 −20% |
| 7 | 적 이속 +5% |
| … | 20까지 확장 |

- **왜 이게 1순위인가** — `data/difficulty.tres`·`balance.tres` 의 기존 손잡이를 등급별로 얹기만 하면
  된다. 아트·기믹·보스 제작이 **0** 인데 20단계면 수십 시간이 생긴다.
- **데이터**: 신규 `ThreatRankData`(.tres) + 생성기 `tools/gen_threat_data.gd`
  (`CLAUDE.md` §2 — `.tres` 손편집 금지). 각 등급 = 배수/증감 딕셔너리 하나.
- **적용 지점**: `ZombieSpawner._hp_mult()`·`_spawn_interval()`·`GameData.balance` 를 읽는 곳에
  등급 배수를 곱한다. **밸런스 테이블을 대체하지 말고 그 위에 곱한다** — 등급 1이 현재와 동일해야
  기존 측정치가 그대로 기준선으로 남는다.
- **UI**: 새 게임 진입 전 등급 선택(해금된 것 중), HUD 상단에 현재 등급 표시.
- **저장**: 최고 해금 등급 + 등급별 최고 기록.
- **측정**: `tools/sim_balance.py` 에 `--threat N` 추가 → 등급별 곡선을 바로 잰다.

**수용 기준** — 등급 1의 측정치가 현재와 통계적으로 같고, 등급이 오를수록 생존 중앙값이 단조 감소한다. ✅

**구현 결과** (`BALANCE.md` §3-8 — 통제군은 그때의 `origin/main` 을 같은 기기에서 다시 잰 것)

| 조건 | n | 생존 중앙값 | 범위 |
|---|---|---|---|
| `origin/main` (통제) | 12 | 8.9분 | 6.2~10.7 |
| 등급 1 | 12 | 9.5분 | 2.8~10.6 |
| 등급 20 | 9 | 6.6분 | 3.5~10.5 |

- **해금 조건을 바꿨다** — 초안의 "클리어할 때마다"는 30분 클리어자가 아직 없어(사람 최고 19.0분)
  등급 2가 영원히 안 열린다. **보스 1회 처치**로 낮췄다. 초안대로 갔다면 Phase B 가 고친 병
  ("만들어 놓고 아무도 못 보는 콘텐츠")을 Phase A 가 다시 만들 뻔했다.
- **적용 지점**은 초안이 지목한 `_hp_mult()` 가 아니라 그 위의 `Events.diff_*` 세 배수였다 —
  거기가 모든 호출부의 합류점이라 `ZombieSpawner` 를 한 줄도 건드리지 않았다.
- **인접 등급은 현재 하네스로 구분되지 않는다**(등급 1 vs 5 가 순서까지 뒤집혀 나온다).
  단조성은 양 끝에서만 확인됐고, 등급별 곡선 조율에는 판수 30+ 가 필요하다.

---

### Phase B — 도감 (Codex) ✅ **완료** (HANDOFF P1-13)

**만들어 놓고 목록 화면이 없었다.** 유저는 진화가 존재하는지조차 모른다 — 오토플레이는 한 번도
발동시키지 못했고(`BALANCE.md` §3-4), 사람도 19분 런에서 하나(napalm)를 봤을 뿐이다(§3-6).

| 분류 | 계획 | **구현** | 발견 조건 |
|---|---|---|---|
| 무기 | 16 | **19** | 한 번이라도 획득 |
| 진화 | 12 | **11** | 진화 완료 |
| 패시브 | 10 | 10 | 획득 |
| 좀비 | 11 | 11 | 처치 |
| 보스 | 3 | 3 | 처치 |
| 캐릭터 | 3 | 3 | 해금 |
| 아레나 | 3 | 3 | 플레이 |
| **합계** | 58 | **60** | |

> 계획의 수는 문서가 낡은 것이었다. 실제 카탈로그는 진화 규칙 **11**개고, 진화 무기로 표시되는
> 14종 중 3종은 캐릭터 궁극기(진화 규칙에 없고 레벨업 카드로 해금된다)라 무기 절에 들어간다.
> 도감이 이 수를 화면에 드러내므로 앞으로는 어긋나면 바로 보인다.

- **아트 비용 0** — 아이콘 94개가 이미 아틀라스에 있다(`assets/ui/icons`).
- **UI**: 메인 메뉴 도감 패널. **`UIPopup.make()` 셸을 쓴다**(P2-1 로 도입됨, `scripts/UIPopup.gd`).
  격자 배치, 미발견은 실루엣 + `???`. **이름을 숨기는 것이 갈증을 만든다** — VS 의 방식이다.
- **저장**: `user://codex.save` — 발견한 id 집합만. 세이브 스키마가 단순해 마이그레이션 부담이 없다.
- **선택 항목은 하지 않았다** — 달성률 구간(25/50/75/100%) 메타 골드 보상. 수용 기준에 없고,
  보상 체계를 건드리면 이 항목이 도전과제 데이터 생성기까지 번진다. 필요해지면 그때 별도 항목으로.

**수용 기준** — 세 아레나를 한 판씩 돌면 도감에 항목이 채워지고, 재시작 후에도 유지된다. ✅

**구현 메모**
- 캐릭터만 저장하지 않는다 — 해금 상태를 `CharacterManager` 가 이미 영속으로 들고 있다.
  같은 사실을 두 곳에 적으면 언젠가 어긋나므로 화면에서 그때 계산한다.
- 실루엣은 `modulate` 로 만들 수 없다(곱셈이라 원본 색이 새거나 배경에 묻힌다 — 실렌더 2회로 확인).
  알파만 남기고 RGB 를 갈아끼우는 셰이더를 쓴다(`assets/shaders/codex_silhouette.gdshader`).
- 패널은 **처음 열 때 만든다.** 좀비·보스 아이콘이 게임플레이 아틀라스(767KB)에 있어
  메뉴 진입 시점에 조립하면 도감을 열지 않는 사람까지 그 페이지를 올리게 된다.

---

### Phase C — 일일 시드 챌린지 🟦 **경쟁의 답**

매일 **고정 시드 + 고정 캐릭터/아레나/등급**으로 한 판. 결과를 공유 가능한 요약으로 뽑는다.

```
🧟 좀비버스터 Day 142
⏱ 18:42  ·  Lv27  ·  💀 보스 2
▓▓▓▓▓▓▓▓░░░░  30:00 중 62%
```

- **서버가 필요 없다.** 날짜(UTC)를 해시해 시드로 쓰면 모든 기기가 같은 판을 만든다.
- **결정론 선행 작업**: `Events.env_seed` 결정론이 **이미 있다**(날씨, `ENVIRONMENT_PLAN` §B).
  같은 방식으로 스폰을 덮어야 한다 — `ZombieSpawner` 의 `randf/randi` 호출 **10곳**을
  시드 기반 RNG(`RandomNumberGenerator`)로 교체한다. 이게 이 Phase 의 실제 작업량이다.
- **기록**: 그날의 최고 기록 1개만 저장(재시도 허용). 규칙을 단순하게 둔다.
- **공유**: `DisplayServer.clipboard_set()` — P1-11 의 기록 복사와 같은 경로.

**왜 순위표가 아니라 이것인가** — §3 참고.

---

### Phase D — 온라인 랭킹 ⬛ 마지막

서버 + 치팅 방지가 선행이다(`LAUNCH_CHECKLIST.md` C). 웹에서 클라이언트 점수를 그대로 믿으면
**하루 만에 오염된다.** 순서상으로도 마지막이 맞다 — Phase C 가 있어야 "같은 판이니
리플레이 해시로 검증"이 가능해진다. 랭킹을 붙인다면 **등급별로 분리**하는 것이 정직하다.

---

## 3. 경쟁 요소 — 필요한가

**필요하다. 단, 순위표 형태로는 아니다.**

순수 순위표는 **상위 1%만 동기부여되고 나머지는 오히려 이탈**시킨다. 3일 차에 자기가
8,412등인 걸 확인한 유저는 다시 오지 않는다. 잘 알려진 실패 패턴이다.

**Wordle 이 반례다.** 순위표가 **없는데도** 경쟁이 격렬했다. 이유는 둘뿐이었다:

1. **모두가 같은 문제를 푼다**(일일 시드)
2. **결과가 스포일러 없이 공유 가능한 형태다**(초록/노랑 격자)

"내가 몇 등이냐"가 아니라 **"너는 몇 번에 맞췄어?"** 가 경쟁을 만들었다.
Vampire Survivors 도 사실상 순위표가 없고, 커뮤니티가 공유하는 것은 도전과제와 빌드다.

**그래서 이 게임의 경쟁 설계는 3층이다:**

| 층 | 대상 | 수단 |
|---|---|---|
| 1차 | **자기 자신** | 위협 등급 진행 · 도감 달성률 · 최고 기록 |
| 2차 | **같은 조건의 타인** | 일일 시드 + 공유 요약 |
| 3차 | **전체** | 온라인 랭킹(등급별 분리) — 서버가 생긴 뒤 |

**1차를 먼저 깔아야 한다.** 중하위권 유저가 붙잡을 것이 없으면 2·3차는 이탈 가속기가 된다.

---

## 4. 하지 않기로 한 것 (명시)

- **테마 순차 해금(선형 캠페인) 전환** — 마지막 테마가 곧 콘텐츠의 끝이 된다. 지금의
  "선택지" 구조를 유지하고, 테마마다 **성격 차이**를 주는 쪽으로 간다
  (교외=기본 / 도심=기믹 밀집 고위험 고보상 / 연구소=시야 제한 등).
- **F2P 강화 트레드밀**(장비 티어·일일 던전·길드) — 탕탕특공대류의 리텐션 장치지만
  웹 캐주얼에는 무겁고, 서버·운영 인력이 전제된다.
- **순위표 우선 도입** — §3.

---

## 5. 권장 순서와 근거

```
[선행] 30분 클리어 성립  ← HANDOFF P1-5 + 보스 조정 사람 검증
   └─ Phase A 위협 등급 ✅ ← 제작비 0, 수명 최대. 랭킹의 축도 여기서 나온다
        └─ Phase B 도감 ✅  ← 아트비 0, 이미 만든 콘텐츠를 목표로 전환
             └─ Phase C 일일 시드  ← 경쟁의 실질. 스폰 결정론이 실작업
                  └─ Phase D 온라인 랭킹 ← 서버·치팅 방지 선행
```

A·B 는 서로 독립이라 **다른 세션에 병렬로 넘길 수 있다**(A=밸런스/데이터, B=UI/저장).
C 는 `ZombieSpawner` 를 건드리므로 A 와 직렬로 둔다.
