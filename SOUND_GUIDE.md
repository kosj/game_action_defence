# 게임 사운드 작업 가이드

이 게임의 효과음을 만들고 고치면서 실제로 부딪힌 문제와 해결 방법을 정리한다.
프롬프트 모음은 [`SOUND_PROMPTS.md`](SOUND_PROMPTS.md)(설계 노트)와
[`SOUND_PROMPTS_EN.md`](SOUND_PROMPTS_EN.md)(복붙용)에 따로 있다.

여기 적힌 것은 전부 **실제로 한 번씩 틀렸다가 배운 것들**이다. 같은 함정을 다시 밟지 않기
위한 문서지, 일반론 모음이 아니다.

---

## 0. 가장 중요한 것

**측정은 원인을 좁혀 주지만, "무엇처럼 들리는가"는 판정하지 못한다.**

화살비를 세 번 고쳤다. 매번 대역·밀도·또렷함을 목표치에 맞췄고 매번 수치는 정상이었는데,
결과는 유리 → 벌떼 → 잡음이었다. 지표가 다 정상 범위인데 소리는 아닌 상태가 실제로 있다.

그래서 순서는 이렇다.

1. **귀로 판정한다.** "뭔가 아니다"는 사람만 말할 수 있다.
2. **측정으로 원인을 좁힌다.** 무엇을 고쳐야 하는지는 숫자가 알려준다.
3. **고치고 다시 귀로 확인한다.** 수치가 목표에 닿았다고 끝난 게 아니다.

측정만으로 자신 있게 끝냈다가 되돌린 적이 여러 번이라, 이 순서를 지키는 게 가장 많이 아꼈다.

```bash
python3 tools/analyze_sfx.py ult_arrow          # 한 소리 진단
python3 tools/analyze_sfx.py --compare a b      # 두 소리가 헷갈리는지
python3 tools/analyze_sfx.py --all              # 전체 훑기
```

---

## 1. 증상 → 원인 찾아가기

실제로 나왔던 지적을 그대로 옮겼다.

| 들리는 증상 | 확인할 값 | 원인과 처방 |
|---|---|---|
| "소리가 안 나는데?" | 폰 스피커 체감, 200Hz 이하 비율 | 저역 덩어리. 아래 §2 |
| "A랑 B가 헷갈려" | `--compare` 세 축 | 길이·중심 주파수·플랫니스가 겹침. §3 |
| "겹치면서 잡음처럼 들려" | 동시 겹침 수, 피크-중앙 | 재료가 너무 길다. §4 |
| "피치가 높아 거슬려" | 5kHz 이상 비율, 중심 주파수 | 고역 과다. §5 |
| "○○ 소리 같지 않아" | 대역 분포 | 음색 대역이 틀렸다. §6 |
| "연출이랑 안 맞아" | 온셋, 길이 | §7 |
| "품질이 좋지 않아" | 파고율, 피크 도달 시간 | 어택이 없다. §11 |
| "이 소리만 안 들려" | 폰 체감 + `_VOLUMES` 를 세트 전체와 비교 | 믹스가 벌어졌다. §12 |
| "여긴 원래 소리가 없나?" | 파일이 아니라 **코드**를 본다 | 무음은 어떤 지표에도 안 나온다. §14 |
| "소리가 다음 화면까지 따라와" | 파일 길이 vs 연출이 끝나는 시점 | 씬을 넘어도 안 끊긴다. §15 |

---

## 2. 저역만 있으면 안 들린다

베테랑 궁극기(지진)가 "재생되지 않는다"는 말을 들었는데, 코드는 정상이었고 소리도 나고
있었다. 에너지의 **99.4%가 200Hz 이하**라 폰 스피커가 그 대역을 못 내서 안 들렸을 뿐이다.
체감 음량이 다른 궁극기보다 **17~20dB** 낮았다.

**저역을 키우는 건 답이 아니다.** 기기가 그 대역 자체를 못 낸다. 대신:

1. **배음 생성(익사이터)** — 저역을 비선형(`tanh`)에 통과시켜 2·3배음을 만들고 150~900Hz만
   걸러 섞는다. 귀는 배음만 듣고도 원래의 낮은 음을 인지한다(결여 기본음). 원음에서 파생된
   배음이라 이질감도 없다.
2. **들리는 대역에 실제 내용을 만들어 넣는다** — 지진이면 암석 파열, 폭발이면 파편.
3. **저역 비중을 낮춘다** — 못 내는 대역이 RMS 예산을 다 먹으면 정작 들리는 대역이 조용해진다.

결과: 체감 -39.8dB → -22.9dB (16.9dB 개선). 구현은 `tools/gen_sfx.py` 의 `ult_quake`.

> 개발용 헤드폰에서는 멀쩡히 들린다. 이 문제는 **반드시 폰 스피커 지표로** 확인해야 한다.

---

## 3. 두 소리가 헷갈릴 때

좀비 출현음과 레벨업 징글이 헷갈린다는 지적을 받고 재보니, 좀비 소리의 스펙트럴 플랫니스가
**0.003**으로 레벨업(0.007)보다 **더 음정에 가까웠다**. 좀비 으르렁이 아니라 사실상 화음
스웰이었으니 헷갈리는 게 당연했다.

**세 축에서 벌려야 한다.**

| 축 | 의미 | "다르다"의 기준 |
|---|---|---|
| 길이 | 짧은 신호 vs 긴 연출 | 한쪽이 다른 쪽의 70% 이하 |
| 중심 주파수 | 낮은 소리 vs 높은 소리 | 700Hz 이상 차이 |
| 플랫니스 | 노이즈 vs 음정 | 2배 이상 차이 |

**두 축 이상이 비슷하면 실제로 헷갈린다.** 하나만 벌려도 대개 해결되지만, 두 축을 벌리면 확실하다.

게임 오디오의 기본 분리 규칙도 같이 기억해 둘 만하다 — **위협은 낮고 노이즈답게, 보상은
높고 음정답게.** 좀비 떼가 화음으로 울리면 안 되고 레벨업이 으르렁대면 안 된다.

---

## 4. 겹침 밀도가 소리의 성격을 정한다

화살비처럼 **한 소리를 여러 번 겹쳐 만드는 텍스처**에서 가장 중요한 값이다.

```
동시 겹침 = 재료 한 발의 길이 × 발수 ÷ 전체 길이
```

| 겹침 | 들리는 것 |
|---|---|
| 1~2 | 성기다. 개별 사건이 뚝뚝 떨어진다 |
| **2~3** | **적정.** 빽빽하면서 하나하나가 들린다 |
| 4 이상 | 여운이 서로 메워져 끊김 없는 층이 생긴다 |
| 6 이상 | 잡음 덩어리. 무슨 소리인지 알 수 없다 |

이 함정에 **세 번** 빠졌다.

- 비행음 0.26초 × 70발 ÷ 4초 = 4.5 → "벌떼가 몰려오는 소리"
- 공명 감쇠 0.055초가 겹침 → 저역이 계속 깔림
- 실제 녹음(0.41초) × 48발 ÷ 3.25초 = 6.1 → "겹치면서 잡음처럼"

세 번째가 특히 뼈아팠다. 재료를 합성음(0.17초)에서 실제 녹음(0.41초)으로 바꾸면서 **길이가
2.4배 늘어난 걸 발수에 반영하지 않았다.** 그래서 기준을 발수가 아니라 **겹침 수**로 바꿨다.

**처방:** 재료의 꼬리를 자른다(잔향이 겹침의 주범). 그 다음 발수로 미세 조정한다.
자를 때는 끝 50~60ms 페이드를 넣어 딸깍임을 막는다.

> 반대 방향으로도 틀린 적이 있다. "빈 구간 없이 이어지게" 편차를 1.9dB까지 낮췄더니
> 개별 화살이 뭉개져 벌떼가 됐다. **끊김 없음과 또렷함은 상충한다** — 후자가 우선이다.

확인 지표는 **피크-중앙**(10ms 포락선). 17dB 안팎이면 또렷하고, 12dB 아래면 뭉갠 것이다.
현존 기준: 파리떼가 **7.3dB**(웅웅거림), 화살비가 **17.0dB**(또렷한 타격)다.

> ⚠️ 이 문단은 한때 "파리떼 17dB · 유리 깨짐 29dB" 로 적혀 있었는데, 그 두 값은 피크-중앙이
> 아니라 **피크-바닥**(p95-p10)이었다. 두 지표를 섞어 인용한 것이라 기준으로 쓰면 어긋난다.
> 인용하던 유리(성수 착탄음)는 그 뒤 게임에서 빠져 지금은 측정할 수도 없다.

---

## 5. 반복 재생되는 소리는 다르게 다룬다

젬 획득음, 좀비 피격음, 스피터 발사음처럼 **초당 여러 번** 울리는 소리는 한 번 들었을 때 좋은
것과 기준이 다르다.

- **짧고 건조하게.** 꼬리가 길면 서로 겹쳐 뭉갠다(§4).
- **개성을 죽인다.** 인상적인 소리일수록 몇 분 만에 거슬린다.
- **2~5kHz를 조심한다.** 귀가 가장 예민한 대역이라 반복되면 금세 피로해진다.
- **스로틀을 건다.** `SoundManager._MIN_INTERVAL` 에 최소 간격(ms)을 등록한다. 성능뿐 아니라
  청감에도 좋다.

실제 사례 둘:

**젬 획득음** — 기본 주파수 3.7kHz에 콤보 상승(+40%)이 얹혀 최고조에서 5.1kHz까지 올라갔다.
런 내내 가장 자주 울리는 소리가 가장 예민한 대역에 있었던 셈이다. 피치를 0.8로 낮추고 콤보
상승 폭을 +15%로 줄여, **최고조에서도 원음을 넘지 않게** 했다.

**성수 착탄음**(지금은 게임에서 빠진 소리) — 5kHz 이상에 에너지의 58%가 몰려 쨍했다.
2.2kHz 위쪽을 13dB 깎아(`import_sfx.py` 의 `shelf`) 유리의 정체성인 0.8~3kHz 몸통은 남기고
찌르는 성분만 덜어냈다. 중심 주파수 5548 → 2638Hz, 체감 음량은 0.6dB 차이로 거의 그대로다.
소리는 없어졌지만 **`shelf` 는 남겨 뒀다** — "쨍하다"의 처방이 이것뿐이라, 쓰는 데가 없다고
지우면 이 문단의 처방을 실행할 수 없게 된다.

---

## 6. 음색의 정체성은 대역 분포에 있다

"무엇을 때린 소리인가"는 대역 분포가 정한다. 게임 내 소리를 기준점으로 쓸 수 있다.

| 재질 | 200~800Hz | 0.8~3kHz | 3~8kHz | 측정한 소리 |
|---|---|---|---|---|
| 나무 | 60% | 7% | 0.4% | 배트 스윙 †  |
| 흙에 꽂힘 | 20% | 11% | 5% | 화살 착탄 (`ult_arrow`) |
| 유리 | 6% | 60% | 31% | 성수 착탄 † |
| 전기 방전 | 2% | 26% | 54% | 테슬라 (`tesla_arc`) |

† 표시는 **지금은 게임에 없는 소리**다(근접 무기·성수 삭제). 대역 프로필은 재질 기준으로
계속 쓸 수 있지만 `analyze_sfx.py` 로 다시 재 볼 수는 없다.

좀비 피격음이 "툭툭거린다"는 지적을 받고 재보니 **중심 124Hz에 800Hz 이상이 0%**였다.
살점이 아니라 나무토막을 치는 소리였다. 젖은 중역을 넣고 길이를 0.43 → 0.18초로 줄였다.

---

## 7. 연출과 맞추기

- **온셋은 0ms.** 앞에 무음이 있으면 화면 셰이크·버스트와 어긋난다. 생성음은 대개 앞에
  50~70ms 무음이 붙어 나오므로 반드시 잘라낸다.
- **길이는 연출 길이에 맞춘다.** 궁극기 지속이 3.0초인데 사운드가 4.0초면, 화면에는 아무것도
  없는데 소리만 계속 난다. 연출 시간 + 짧은 여운이 맞다.
- **연출의 리듬을 소리에 반영한다.** 지진은 0.3초마다 피해 틱이 돌고, 궤도 폭격은 0.35초마다
  광선이 꽂힌다. 그 주기로 사건을 배치하면 화면과 붙는다.
- **게임 타이밍을 확인한다.** 자폭 좀비 점화음 소스는 삐 소리 3개가 1.41초에 퍼져 있었는데,
  게임의 점화~폭발 간격은 0.55초였다. 그대로 썼으면 뒤 두 개가 폭발 뒤에 울렸다.

---

## 8. 음량은 파일이 아니라 코드에서 잡는다

- **모든 파일을 같은 라우드니스(RMS -16dBFS)로 정규화**하고, 세기 차이는
  `SoundManager._VOLUMES` 에서 조정한다. 파일마다 음량이 제각각이면 믹스 관리가 불가능해진다.
- **RMS가 같아도 체감은 다르다.** 중역 위주 소리는 저역 위주보다 크게 들린다. 좀비 피격음을
  교체했을 때 RMS는 1.8dB *낮았는데* 체감은 4.8dB *높았다*. 볼륨 보정은 A-가중 값을 보고 한다.
- **트랜지언트가 강한 소리는 기준 RMS에 못 닿는다.** 피크 상한에 먼저 걸리기 때문이다.
  부족분은 코드 볼륨에서 보정하되, 트랜지언트는 RMS보다 크게 들리므로 전부 보정하면 과하다.
- **스테레오는 모노 합성을 확인한다.** 폰 스피커는 모노다. 좌우가 상관도 높으면 합쳐질 때
  피크가 넘칠 수 있다.

---

## 9. 절차적 합성의 한계

전기음(테슬라)처럼 **물리적 실물이 없는 소리**는 합성이 잘 맞는다. 불규칙한 스파크 게이트로
파직거림을 만드는 식으로, 원하는 성격을 직접 설계할 수 있다.

반면 **실물 타격음(살점·흙·나무)은 합성으로 만들지 않는 게 낫다.** 노이즈와 감쇠 정현파로는
대역 수치를 맞출 수 있어도 "무엇을 때린 소리인가"가 만들어지지 않는다. 감쇠 정현파 몸통은
악기음처럼 들려서, 화살비는 실로폰 같은 패터가 되고 좀비 피격은 북소리가 됐다.

**대신 이렇게 한다:**

1. 생성기에 **단발 사건 하나**를 시킨다("화살 한 발"). 생성기는 단발을 잘 만든다.
2. **겹치고 배치하는 일은 코드가 한다.** 밀도·길이·좌우 배치·변주는 코드가 정확히 통제한다.

이 분업이 이 프로젝트에서 가장 잘 통한 방법이다. 생성기에 "화살비" 전체를 시키면 밀도가
부족해 유리 깨지는 소리가 나왔지만, 화살 한 발은 문제없이 뽑혔다.

> 보너스: 생성기는 "한 발"을 시켜도 보통 여러 테이크를 한 파일에 담아 준다. 그걸 전부
> 변주로 쓰면 같은 소리를 피치만 바꿔 반복하는 것보다 훨씬 자연스럽다.
> `make_arrow_rain.py` 가 테이크를 자동으로 찾아낸다.

### 경계선을 미리 그어 두면 헤매지 않는다

공백 8곳을 한꺼번에 메우면서 이 기준으로 갈랐고, 결과가 갈린 대로 나왔다.

| 합성으로 만든다 | 생성기에 맡긴다 |
|---|---|
| 기계 — 전기톱 톱니, 드론 사격 | 생물의 목소리 — 보스 포효 |
| 전기·자기 — 테슬라, 자석 흡입 | 사람이 아는 특정 실물의 타격 |
| 공기·물리 — 날씨 훅, 파편 | 심리스 루프가 필요한 것(이음매는 편집 문제다) |
| 구조물 붕괴 — 보스 처치 | |

가르는 질문은 하나다 — **"이 소리의 정체성이 무엇에서 나오는가?"**
톱니 소리의 정체성은 *주기적인 임펄스 열* 이고, 그건 수식으로 정확히 쓸 수 있다.
포효의 정체성은 *성대와 공명강* 이고, 그건 노이즈와 정현파로 근사되지 않는다 —
대역 수치는 맞출 수 있어도 언제나 악기가 된다.

---

## 10. 소스 다루기

생성기 출력은 그대로 쓸 수 없다. 매번 확인할 것:

- **앞 무음** — 거의 항상 붙어 나온다. 잘라낸다.
- **여러 테이크** — 한 파일에 3~6개가 들어있는 경우가 흔하다. 포락선을 보고 가장 좋은 것을
  고르거나, 전부 변주로 쓴다. 배트 스윙 소스에는 휘두르기가 4번 들어 있었다.
- **약한 조각** — 가장 큰 테이크보다 12dB 이상 작으면 잔향·잡음이다. 정규화하면 오히려
  도드라지므로 버린다.
- **꼬리** — 잔향이 길면 겹칠 때 문제가 된다(§4).
- **길이** — 목표보다 대개 2~3배 길다.

`tools/import_sfx.py` 의 `PLAN` 에 구간·페이드·셸프를 적어 두면 재현 가능하게 처리된다.

## 11. 소스에 '때리는 순간'이 없을 수 있다

좀비 피격음이 "품질이 좋지 않다"는 지적을 받고 재보니, 대역폭은 18.1kHz로 멀쩡했고(게임의
기존 좀비 사망음은 4.3kHz니 오히려 더 좋다) 생성 품질 자체는 문제가 아니었다.

문제는 **어택이 없다는 것**이었다. 원본 포락선이 150ms에 걸쳐 -53dB에서 -4dB로 서서히
부풀어 올랐다. 타격이 아니라 질척한 텍스처만 뽑힌 것이다. 결과는 파고율 10.2dB,
피크 도달 86ms — 좀비 사망음이 19.5dB / 1ms인 것과 비교하면 뭉툭할 수밖에 없다.

**진단은 두 값으로 한다.**

| 값 | 타격음의 기준 | 뜻 |
|---|---|---|
| 파고율(피크/RMS) | 15~20dB | 낮으면 어택이 몸통에 묻혔다 |
| 피크 도달 시간 | 0~5ms | 늦으면 '때리는 순간'이 없다 |

> 휘두르기·스웰처럼 **원래 부풀어 오르는 소리**는 피크가 늦은 게 정상이다. 이 기준은
> 타격음에만 적용한다.

**여러 번 울리는 경보도 같은 함정에 빠진다.** 위험도 경고음 3종(P2-27)을 만들 때 펄스를
뒤로 갈수록 크게 해서 "점점 다급해지는" 모양을 냈더니 피크 도달이 **77·229·456ms** 였다.
음정은 올라가는데 첫 순간이 비어 있어, 경보가 귀를 붙잡지 못한다 — 경보는 **시작에서**
붙잡고 나머지 펄스가 "계속되고 있다"를 말해야 한다. 진폭을 앞으로 몰아(1.0 → 0.92 → 0.86
→ 0.82) 16~39ms 로 당겼다. **음정의 상승과 진폭의 상승은 분리할 수 있고, 분리해야 한다.**

**없는 어택은 잘라내서 만들 수 없다.** 소스를 아무리 앞에서 잘라도 그 안에 트랜지언트가
없으면 안 생긴다. 대신 **역할을 나눈다**:

- **몸통 = 실제 녹음.** 소스에서 가장 시끄러운 구간부터 쓰고 느린 빌드업은 버린다.
  살점의 젖은 질감은 생성음에서만 나오므로 여기가 정체성이다.
- **어택 = 합성.** 짧은 광대역 버스트(1~2ms 감쇠)를 앞에 붙인다. 고역이 남으면 유리처럼
  들리므로 6kHz 로 눌러 둔다.

이건 §9에서 "실물 타격음은 합성하지 말라"고 한 것과 모순이 아니다. 그때 실패한 건 소리의
**정체성**(무엇을 때렸나)을 합성으로 만들려던 것이고, 여기서 합성이 맡는 건 '탁' 하는
**가장자리**뿐이다. 정체성은 실제 녹음이 그대로 쥐고 있다.

결과: 파고율 10.2 → 13.2dB, 피크 도달 86 → 0ms.

**빈 대역도 같이 확인한다.** 이 소스는 200~800Hz가 테이크별로 2.7% / 9.2%밖에 없어서,
어택을 붙여도 저역 쿵과 고역 파열만 남는 속 빈 소리였다. 없는 대역을 노이즈로 채우면
이물감이 생기므로 **소스의 저역에서 배음을 만들어** 썼다(§2와 같은 방법). 27.2%까지 채웠다.

## 12. 믹스는 파일 하나씩 봐서는 안 보인다

소리를 하나씩 고칠 때마다 "규격 맞음"을 확인했는데도, **세트 전체를 한 줄로 세우자 폭이
26.7dB** 였다. 게다가 가장 조용한 넷이 하필 **가장 자주 나는 소리들**이었다.

재는 값은 하나다 — **폰 체감 + `_VOLUMES`**. 이게 플레이어가 실제로 듣는 크기다.

```
python3 - <<'EOF'
# analyze_sfx 의 phone 값에 SoundManager 볼륨을 더해 정렬한다
EOF
```

발견 당시 하위 넷(수정 전):

| 키 | 폰 체감 | 볼륨 | 실효 | 얼마나 자주 |
|---|---|---|---|---|
| `shoot` | -37.0 | -10.0 | **-47.0** | 초당 3회 — 게임에서 가장 자주 |
| `ui_click` | -37.1 | -6.0 | **-43.1** | 모든 버튼 |
| `gold` | -30.3 | -8.0 | **-38.3** | 젬마다 |
| `boom` | -31.3 | -4.0 | **-35.3** | 호출부 16곳 |

중앙값이 -26.5dB 였으니 `shoot` 은 **20.5dB 아래**였다. "총소리가 안 들린다"는 말이 안
나온 게 이상할 정도인데, 개발용 헤드폰에서는 들리기 때문에 아무도 눈치채지 못했다.

**두 겹으로 조용했던 것이 원인이다.** 파일이 규격 미달(`shoot` RMS -25, `ui_click` -37)인데
볼륨까지 깎여 있었다. 파일만 고치거나 볼륨만 올려서는 어느 쪽도 충분하지 않다.

> 파고율이 20dB 넘는 소리(총성·클릭)는 **애초에 기준 RMS 에 닿을 수 없다** — 피크 상한에
> 먼저 걸린다. 공용 `normalize()` 는 tanh 로 눌러 RMS 를 채우는데, 그 포화가 이런 소리에는
> 곧 왜곡이다. `gen_sfx.py` 의 `_quiet_norm` 은 기준 미달을 감수하고 피크에 맞춘 뒤,
> 모자란 만큼을 볼륨에서 채운다(§8).

**소리를 추가·수정한 뒤에는 세트를 다시 한 줄로 세운다.** 개별 규격 준수는 믹스가 맞다는
뜻이 아니다.

---

## 13. 생성기의 재현성은 두 군데서 깨진다

`shoot`·`boom`·`ui_click` 은 대응 원본(생성기 업로드)이 없어 **커밋된 에셋 자체가 재료**였다.
그런데 결과를 같은 경로에 덮어쓰므로, 그냥 읽으면 두 번째 실행이 이미 가공된 것을 다시
가공한다 — 배음 위에 배음이 얹혀 소리가 점점 뭉개진다. 한 번 돌리고 끝이면 모르지만,
파라미터를 조정하며 여러 번 돌리는 것이 정상 작업 흐름이라 반드시 밟게 된다.

`gen_sfx.py` 는 `PRISTINE_REV` 커밋에서 원본을 꺼내 쓴다(`git show <sha>:<path>`).
몇 번을 돌려도 결과가 같다.

> 확인은 해시가 아니라 **디코드한 PCM 으로** 한다. libvorbis 는 같은 입력에도 매번 다른
> 바이트를 쓰기 때문에 ogg 파일 해시는 항상 달라진다 — 실제로 이걸 이중 가공으로 오해할
> 뻔했다. wav 는 바이트까지 같았다.

### 두 번째 구멍 — 난수 씨앗을 공유하면 순서에 의존한다

원본을 고정해도 아직 부족했다. `gen_sfx.py` 는 `main()` 에서 난수 발생기 하나를 만들어
생성기들이 **차례로** 썼다. 그러면 앞 생성기가 난수를 몇 개 뽑느냐에 따라 뒤 생성기의
수열이 밀린다 — 결과가 **"무엇을 함께 생성했는가"와 "그 순서"** 에 달라진다.

증상은 §13 앞부분의 이중 가공과 똑같이 "돌릴 때마다 소리가 달라진다" 였지만 원인은 딴판이다.
실제로 `gen_sfx.py tesla_arc` 와 `gen_sfx.py`(전체)가 서로 다른 파일을 내놓고 있었다.

**사운드 이름에서 씨앗을 뽑으면 사라진다.** 하나만 다시 뽑든 전부 뽑든 결과가 같다.
파이썬 내장 `hash()` 는 실행마다 값이 달라지므로 쓰면 안 된다 — 고정 해시를 직접 계산한다.

> 고칠 때 이미 배포된 소리 셋(`zombie_hit`·`tesla_arc`·`ult_quake`)의 수열도 바뀐다.
> 파형은 달라지지만 **같은 파라미터의 다른 실현**일 뿐이라는 것을 지표로 확인하고 넘어갔다:
> 중심 주파수 변화 0.3% 이내, 폰 체감 0.4dB 이내. 귀로 판정할 수 없는 차이다.
> 이런 교체는 "괜찮겠지"로 넘기지 말고 **재기 전후를 나란히 찍어** 두는 편이 빠르다.

---

## 14. 없는 소리는 측정에 잡히지 않는다

지금까지의 진단은 전부 **파일이 있다**는 전제 위에 있다. 파고율·대역·플랫니스는 파일을
읽어야 나오는 값이고, 그러니 **아예 소리가 없는 자리는 어떤 지표에도 나타나지 않는다.**
가장 큰 결함이 가장 조용히 숨는다.

실제로 그랬다. 사운드셋 31종을 몇 번이나 재고 고쳤지만, 그동안 **무기 모듈 10개 중
3개(화염방사기·전기톱·드론)는 소리가 아예 없었다.** 재는 대상 안에 없었으니 당연하다.

무음은 **코드에서** 찾는다.

```sh
# 1) 크기에 비해 재생 호출이 없는 스크립트 — 무음 후보
for f in scripts/*.gd; do
  n=$(grep -c 'SoundManager\.play' "$f"); l=$(wc -l < "$f")
  [ "$n" -eq 0 ] && [ "$l" -gt 100 ] && echo "$l $f"
done | sort -rn

# 2) 시그널 하나하나에 대응하는 소리가 있는지 — 연출은 있는데 소리가 없는 자리
grep -n '^signal ' scripts/Events.gd
```

⚠️ **`SoundManager.play(` 만 세면 안 된다** — `play_ui(` 는 별개 함수다. 이걸 빠뜨리면
`MainMenu`(10곳)·`ChestRewardPanel`(13곳)이 무음으로 잡혀 없는 문제를 쫓게 된다.

두 번째 종류의 공백은 더 찾기 어렵다 — **소리는 나는데 빌려온 소리인 경우**다.
보스 처치는 잡몹과 같은 0.16초짜리 `zombie_die` 였고, 페이즈 전환은 주석에만 "포효"라고
적힌 채 실제로는 폭발음 피치다운이었다. 재생 호출이 있으니 위 두 검색에는 안 걸린다.
**주석이 말하는 소리와 실제로 재생하는 키가 다른 곳**을 의심하면 빨리 찾는다.

그리고 이 계통은 **틀려도 조용하다.** `play()` 는 모르는 키를 받으면 예외도 로그도 없이
돌아간다. 그래서 오타 하나가 그 소리를 영영 지워도 아무도 모른다.
`tools/verify_sound_keys.gd` 가 그것만 본다(호출부↔`_SOUNDS` 양방향 + 볼륨·스로틀 유령 키).

---

## 15. 효과음은 씬을 넘어서도 계속 울린다

`SoundManager` 의 플레이어는 **오토로드의 자식**이고 `process_mode = ALWAYS` 다.
씬을 갈아 끼워도 파괴되지 않으므로, 재생 중이던 소리는 **다음 씬까지 그대로 이어진다.**
`change_scene_to_file()` 은 소리를 정리해 주지 않는다.

평소엔 티가 안 난다 — 대부분의 효과음이 0.1~0.5초라 전환 애니메이션 안에서 끝나기 때문이다.
드러나는 건 **긴 소리 + 빠른 이탈**이 겹칠 때다. 게임오버 스팅어가 그랬다: 2.6초짜리인데
패널은 0.35초 만에 뜨니, 플레이어가 곧바로 메뉴로 나가면 2초 넘게 따라 들어왔다.

**두 군데를 같이 고쳐야 한다.** 하나만 하면 반쪽이다.

1. **길이** — 스팅어가 연출보다 길면 애초에 어딘가로 새게 되어 있다. §7 의 "연출과 맞추기"다.
2. **정지** — 그래도 남는 구간이 있으므로 씬을 떠날 때 끈다.

### 끄는 방법 — 그냥 `stop()` 하면 안 된다

감쇠하는 소리는 끝에서 자연히 0 에 가까우므로 끊어도 티가 안 난다. 그런데 **징글류는
감쇠하지 않는다.** 게임오버 스팅어는 0.5초부터 2.2초까지 피크 대비 3dB 안에 머무는
지속음이었다 — 중간에 끊으면 파형이 0 이 아닌 지점에서 잘려 딸깍임이 난다.

화면 페이드와 **같은 시간**을 줘서 소리도 함께 빼면 그림과 소리가 같이 사라진다.
`SceneFade.transition_to(path, dur)` 가 `SoundManager.stop_sfx(dur)` 를 부르는 이유다.

⚠️ **볼륨을 낮췄다 되돌리는 구조는 되돌리기를 빠뜨리면 조용히 망가진다.** 그 소리만
영구히 작아진 채 남고, 화면에는 아무 증상이 없다. 복구를 두 겹으로 두는 편이 안전하다 —
페이드 트윈의 마지막 콜백, 그리고 `play()` 가 재생 직전에 볼륨을 설정값으로 다시 쓰는 것.
후자가 있으면 "재생 시점의 볼륨은 언제나 옳다"가 무조건 성립해 이 종류의 누출이 사라진다.
`verify_audio_playback.gd` 가 이 왕복을 검사한다.

⚠️ **트윈 검사는 프레임이 지나야 의미가 있다.** 만든 직후에 값을 보면 아직 원래 값이라
무엇을 고장 내도 통과한다. 낮아진 상태를 직접 만들거나 실제 시간을 기다려서 봐야 한다.

⚠️ **`SceneFade` 를 안 거치는 전환이 있다.** `reload_current_scene()`(다시하기)이 그렇다 —
중앙에서 한 번 처리했다고 끝났다고 보면 그 경로만 남는다. `change_scene_to_file` 과
`reload_current_scene` 을 같이 grep 해서 빠진 곳을 확인할 것.

> 안 고친 경로가 있다면 **왜 안전한지를 숫자로 남겨 둔다.** 타이틀→메뉴는 `gold`(1.06초)가
> 울리지만 씬 교체가 1.60초 뒤라 먼저 끝난다 — 여유는 0.54초다. "여긴 짧으니까 괜찮다"로
> 적어 두면, 나중에 대기 시간을 줄인 사람이 그 판단이 깨진 걸 알 수 없다.

---

## 도구

| 도구 | 용도 |
|---|---|
| `tools/analyze_sfx.py` | 진단 — 이상하면 여기부터 |
| `tools/import_sfx.py` | 생성음 → 게임용 OGG(자르기·페이드·셸프·정규화) |
| `tools/make_arrow_rain.py` | 단발을 겹쳐 텍스처 만들기 |
| `tools/gen_sfx.py` | 절차적 합성 및 소스 가공 |
| `tools/verify_sound_keys.gd` | 키 정합성 게이트 — 오타·고아 키를 CI 에서 잡는다 |

공통 규격: **48kHz, OGG Vorbis q6, RMS -16dBFS, 피크 -1.5dBFS, 온셋 0ms.**
