# 개발 로드맵

모바일 세로 WebGL 탑다운 액션 디펜스(뱀서식). **초기 5단계 로드맵은 전부 끝났다** —
이 문서는 그 기록이고, **지금 살아 있는 작업 큐는 `HANDOFF.md` §1 점유 표다.**

> ⚠️ 2026-08-20 현행화. 그 전 버전은 "웨이브 6단계 · 좀비 3종 · 플레이스홀더 스프라이트"를
> 전제로 4번이 "진행 중"이라고 적고 있었는데, 그 넷 중 셋은 이미 끝났고 하나(웨이브)는
> **개념 자체가 삭제됐다**(P2-6). 낡은 로드맵은 다음 사람에게 없는 시스템을 전제하게 만든다.

## ✅ 완료

- [x] **0. 프로젝트 뼈대** — Main/Player/Zombie/Bullet/Gold/HUD + 가상 조이스틱, 자동 공격,
      좀비 추적, 화면 밖 스폰, 골드 자석 수집, `gl_compatibility` 고정.
- [x] **1. 오브젝트 풀링** — `Pool` 오토로드로 좀비/총알/골드/FX 재사용(GC 스파이크 방지).
      `on_spawn()` 상태 초기화 + `on_despawn()` 정리 훅 + `_alive` 가드, `Main` 에서 prewarm.
- [x] **2. 플레이어 체력 · 게임오버** — 접촉 피해(무적 쿨다운) → `Events.player_died` →
      게임오버 패널. 재시작 시 `Pool.clear()` 후 씬 리로드.
- [x] **3. 좀비 종류 · 난이도 곡선** — 좀비 **11종**(`data/zombies/*.tres`).
      ~~웨이브 테이블~~ → **시간 기반 디렉터로 교체됐다.** 곡선은
      `ZombieSpawner._hp_mult()` 의 2차식과 `data/difficulty.tres` 두 곳뿐이고,
      런을 나누는 개념은 **보스 처치 마일스톤**(600초 주기)이다. 자세한 것은 `CLAUDE.md` §5.
- [x] **4. 실제 스프라이트 · 아틀라스 · 배경**
      - [x] `Polygon2D` → `Sprite2D`. 플레이어 3종·좀비 11종·테마 보스 3종 전용 아트.
      - [x] 단일 아틀라스로 배칭(`assets/atlas/`, `tools/build_atlas.py`,
            CI 게이트 `tools/check_atlas.gd`). 프롭은 테마별로 분리한다(한 판에 한 테마).
      - [x] 배경 타일 — `Ground.gd` 가 테마별 바닥을 월드 고정 타일링.
      - [x] 사망 연출 후 풀 반납(FXBurst 3종 + 골드 드랍).
      - [x] **`AnimatedSprite2D` 는 채택하지 않았다.** `Zombie._animate_walk` 의 절차적
            바운스/스쿼시(sin)로 **대체 결정**했다 — 스프라이트 시트 11종 × 프레임 수만큼
            아틀라스와 pck 가 커지는데(웹 상한 15MB), 세로 720px 화면에서 체감 차이가
            그 비용을 정당화하지 못했다. 되돌리려는 사람은 이 트레이드오프부터 다시 볼 것.
- [x] **5. 성장·메타 루프** — 레벨업 카드, 진화, 보물상자, 영구 강화, 도전과제·퀘스트,
      랭킹, 도감(P1-13), 위협 등급 20단계(P1-12).

## 지금 남은 것

**여기에 적지 않는다.** 항목이 늘고 줄 때마다 두 곳을 고쳐야 하면 한쪽은 반드시 낡는다
(이 문서가 그렇게 낡았다). 살아 있는 큐는 한 곳뿐이다:

→ **`HANDOFF.md` §1 점유 표**

콘텐츠 확장 계획은 `CONTENT_PLAN.md`, 연출 완성도는 `POLISH_PLAN.md`,
밸런스 실측은 `BALANCE.md` 를 본다.
