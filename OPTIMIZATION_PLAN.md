# Zombie Buster — 최적화 리뷰 & 실행 계획

> 2026-08 전수 리뷰. 게임플레이 핫패스 / UI·FX / 에셋·익스포트 3개 축으로
> 전체 스크립트(~15k줄)·씬·에셋·CI를 조사한 결과와 단계별 실행 계획.

## 진행 상태

- **완료:** A1(BGM) · A2(가짜 폰트) · **A3(텍스처 손실 압축)** · **A4(폰트 서브셋)** ·
  B1(Ground/PropField 재드로우) · B2 · B3 · B4 · B5(조준·스플래시·번개) · B6 · B9 · B10 · B11 · B13 ·
  Phase D(CI) · 익스포트 프리셋 정정 · 관통탄 버그 · Phase C 일부
- **보류(사유 있음):** B12 물리 틱 30Hz, A5 PWA — 각 항목 참조
- **미착수:** B7(y_sort 분리) · B8(전역 훅 제거) · B5 잔여 무기 5종 · Phase C 잔여

### 초기 로딩 결과

| 구성 | 이전 | 현재 |
|---|---|---|
| 오디오 | 17.9 MB | 2.8 MB |
| 텍스처(임포트 후) | 5.59 MB | 2.17 MB |
| 폰트 | 2.36 MB | 0.22 MB |
| 코드/씬/데이터 | 0.7 MB | 0.7 MB |
| **pck 합계(추정)** | **약 26.5 MB** | **약 5.8 MB** |

목표였던 "30MB → 8MB"를 넘어섰다. 마지막 덩어리였던 오디오는 5-D 에서 루프 구간 절단으로
9.0MB → 2.8MB 가 됐다(BGM 3곡 8.34 → 2.15MB, 효과음 0.68MB).

## 검증 결과 (Godot 4.3 헤드리스 실측)

| 검증 | 결과 |
|---|---|
| 전체 프로젝트 임포트 + 스크립트 파싱 | 오류 0건 |
| 공간 해시 정확성 테스트 9건(격자 경로·대반경 폴백·중첩 순회·데미지숫자 상한) | 9/9 통과 |
| 자동플레이 실제 전투 70초(10,110프레임 / 좀비 최대 53 / 이동 9,566px) | 스크립트 오류 0건 |
| **Ground 재드로우 빈도** | **10,110프레임 중 98회** (이동 ~98px당 1회) |
| 보스 피격 잔광(연속 30회 피격 → 감쇠 → 사망) | 오류 0건, 기본 색 복귀 확인 |
| 텍스처 손실 압축 화질(138장을 원본 PNG와 픽셀 비교) | 평균 오차 1.05/255 (0.4%) |
| 폰트 서브셋 커버리지(Locale 사전 448자 × 2종) | 전부 커버, 텍스트 폭 정상 |
| CI 경로 재현(.godot 삭제 후 재임포트) | 나인패치 10장 무손실 + 나머지 128장 손실 유지 |
| 타이틀 화면 20초 / 인게임 40초 재실행 | 오류 0건 |

Ground는 예전 임계값(0.7px)이면 이동 중 사실상 매 프레임 전체 재드로우였으므로,
같은 구간에서 수천 회 → 98회, **약 1/100** 로 줄었다. 이것이 이번 작업의 최대 성과다.

### 실측 벤치마크 — 그리고 그 한계

좀비 300마리를 고정하고 최적화 전 커밋(`2d414f8`)과 같은 조건으로 비교했다
(FPS를 60으로 고정, 물리 600틱, 각 3회).

| | 최적화 전 | 현재 |
|---|---|---|
| `_process` 스크립트 시간 | 0.499 ~ 0.675 ms | 0.372 ~ 0.469 ms |
| 스크립트 합계(중앙값) | 5.06 ms | 4.19 ms |

**이 수치를 과신하면 안 된다.** 편차가 커서(최적화 전 4.09~6.21ms) 합계 차이는 단정하기
어렵다. 더 중요한 건 **헤드리스는 더미 렌더러라 이번 작업의 최대 성과인 렌더 경로 비용을
거의 재지 못한다**는 점이다 — 바닥 전체 재드로우, 데미지 숫자 텍스트 렌더, 투명 오버레이
fill-rate, 총알의 물리 broadphase 는 실제 GPU/렌더 서버가 붙어야 드러난다.
그나마 일관되게 낮아진 `_process` 시간이 바닥 재드로우 감소와 방향이 맞고, 직접 센
**Ground 재드로우 98회/10,110프레임**이 가장 신뢰할 만한 근거다.

정확한 성능 개선폭은 **실기기(모바일 브라우저)에서 재는 수밖에 없다.** 그래서 게임 안에
성능 디버그 오버레이를 넣었다 — **일시정지 → CHEATS → PERF HUD**.

```
FPS  58   frame 17.2 / worst 31.4 ms      ← worst = 최근 2초 최대 프레임 시간(히칭 감지)
CPU  proc 0.42  phys 3.71 ms              ← 스크립트 시간
DRAW calls 812  items 1043                ← 바닥 재드로우·y_sort·FX 상한 효과가 여기 보인다
MEM  video 31.2MB  tex 26.7MB             ← 텍스처 VRAM 압축 판단 근거
NODE tree 1069  pooled 410                ← pooled 는 풀이 트리 밖에 보관 중인 노드(정상)
GAME zombies 302  gems 88
FX   dmg 12/36  burst 8/48  spr 5/40  lit 0/6   ← 상한에 계속 붙어 있으면 상한이 낮다는 뜻
```

읽는 법: `worst` 가 튀면 히칭, `DRAW calls` 가 이동 중에만 크게 오르면 바닥 재드로우 의심,
`tex` 가 크면 VRAM 압축 검토, `pooled` 가 계속 늘면 그때가 진짜 노드 누수다.

> 검증에 쓴 임시 테스트/벤치 스크립트는 커밋 대상이 아니라 삭제했다.
> 다만 실측은 모두 헤드리스(더미 렌더러)라 **화면에 그려지는 결과 자체는 확인하지 못했다** —
> 바닥 스냅으로 화면 가장자리가 비지 않는지, 데미지 숫자 상한이 시각적으로 과하지 않은지는
> 실기기/에디터에서 한 번 눈으로 볼 것.

---

## 0. 총평

**기본기는 이미 잘 되어 있다.** 아래는 검증된 "잘 된 부분"이므로 건드리지 않는다.

- 오브젝트 풀링: `Pool.gd`(트리 분리 + `call_deferred` 중복 반납 방지 + prewarm), FX/데미지숫자 정적 풀
- 스폰 제어: 적 상한 40→320 보간, 스폰 큐 프레임당 12개 분산(`spawn_budget_per_frame`), 젬 상한 140 + 강제 흡수
- 좀비 자체: 물리 바디 미사용(직접 적분 이동), 씬 3노드로 슬림, 30Hz 애니 + 화면 밖 LOD, 그리드 기반 분리
- 공간 해시: `Events.live_zombies()` 스냅샷 + `zombies_near()` 셀 질의
- HUD: 완전 이벤트 구동(`_process` 없음), 진행 신호 초당 1회 스로틀
- SoundManager: 사운드별 최소 재생 간격 스로틀, SFX 총 680KB로 가벼움
- 렌더러 `gl_compatibility`, 부트 스플래시 off, stretch 설정 — 웹/모바일 타깃에 적절
- `Main.gd`: `_process` 없음, prewarm을 첫 프레임 이후로 지연 — 구조 양호

**그러나 두 가지 큰 구멍이 있다:**

1. **초기 로딩 28MB 중 17.9MB가 BGM.** 웹 pck는 전량 받아야 첫 화면이 뜨는데, 모바일 웹
   BGM치고 비트레이트(190~210kb/s 스테레오)가 과했다. 코드 최적화 전부를 합친 것보다 이거 하나가 크다.
2. **런타임 병목은 적 수가 아니라 "그리기"에 있다.** 바닥 전체 재드로우(이동 중 매 프레임),
   데미지 숫자 동시 상한 부재(~500개 동시 텍스트 렌더 가능), 총알의 불필요한 물리 등록 등.
   좀비 쪽(풀링·LOD·스폰 예산·공간 해시)은 이미 잘 만들어져 있었고, 실제로 손댈 곳은 렌더 경로였다.

---

## 1. Phase A — 용량/로딩 (코드 수정 거의 없음, 효과 최대)

목표: 초기 로딩 **~30MB → ~8MB**

| # | 작업 | 절감 | 상태 |
|---|---|---|---|
| A1 | BGM 3곡 재인코딩 (190~210kb/s 스테레오 → **96kb/s 모노**, 길이·곡 내용 그대로) | **-9.2MB** | ✅ 완료 |
| A2 | `assets/fonts/NotoSansKR-Regular.ttf` / `NotoSansKR.bin` **삭제** — 둘 다 폰트가 아니라 다운로드 실패로 커밋된 **동일한 HTML 문서**(598KB). 코드 참조 0건, `export_filter="all_resources"` 탓에 웹 빌드에 포함되던 중 | -598KB | ✅ 완료 |
| A3 | 텍스처 손실 압축: `project.godot`의 `[importer_defaults]`로 전역 기본값을 Lossy(WebP, 품질 0.95)로 전환. `.import`는 CI가 재생성하므로 파일을 커밋할 필요가 없다 — 단 나인패치 텍스처만 예외로 무손실 고정 | **-3.42MB** | ✅ 완료 |
| A4 | CJK 폰트를 **실제 표시 가능한 글자만** 남겨 서브셋 (2.36MB → 0.22MB). KR 전용이 아니다 — 아래 정정 참조 | **-2.14MB** | ✅ 완료 |
| A5 | PWA 활성화 (`progressive_web_app/enabled=true`) — GitHub Pages는 캐시 헤더 제어 불가라 재방문 로딩 개선 수단이 이것뿐 | 재방문 로딩 | ⏸ 아이콘 미설정 |

**A1 정정 — 최초 조사 보고가 틀렸다.** 조사에서는 "`bgm_game_2`가 19.1분/64kbps 모노"라고 했으나
실제 파일은 **5분 48초 / 210kb/s 스테레오**였다(나머지 두 곡도 3분대). 즉 문제는 "곡이 비정상적으로
길다"가 아니라 **모바일 웹 BGM치고 비트레이트가 과했다**는 것이다. 그래서 곡을 자르는 대신
음악 내용을 100% 보존한 채 96kb/s 모노로만 재인코딩했다(BGM은 `AudioStreamPlayer` 비위치 재생이라
스테레오 이미징이 쓰이지 않는다). 길이는 밀리초 단위까지 동일하고, 루프는 코드에서
`stream.loop = true`로 켜므로 영향 없다. 여기서 더 줄이는 유일한 수단이 루프 구간 단축이었고,
그건 음악 내용을 버리는 결정이라 판단을 남겨 뒀다 — **5-D 에서 실행했다**(8.34 → 2.15MB).

**A3 방식과 화질 실측:** `[importer_defaults]`에 텍스처 기본값을 넣으면 CI가 `.import`를
재생성할 때 그대로 적용되므로, 138개 `.import` 파일을 커밋할 필요가 없다. 품질값은 실측으로
골랐다 — 0.85는 1.45MB(평균 오차 MAE 1.55/255), 0.95는 2.06MB(MAE 1.05/255). 용량 차이가
0.6MB뿐이라 화질 여유가 큰 **0.95**를 택했다(평균 오차 0.4%).

단, `assets/ui/frames`·`assets/ui/hud`의 10개 텍스처는 **무손실로 고정**했다. 이들은
`StyleBoxTexture` 나인패치로 **늘려서** 쓰이므로 테두리의 손실 아티팩트가 띠로 번져 보이기 쉽다.
이 파일들만 `.import`를 저장소에 두고 `.gitignore`에 예외를 뒀다. 실측에서도 오차 상위 목록에
있던 `hud_gauge_frame`·`hud_slot_small`이 이 조치 후 목록에서 사라졌다.

무손실 PNG 재압축(픽셀 동일)은 별도로 시도했으나 **121KB(1.7%)밖에 안 줄어** git 히스토리에
7MB 블롭을 남길 가치가 없어 폐기했다.

**A4 정정 — 조사 보고의 권고가 틀렸다.** 보고서는 "JP 글리프까지 포함하고 있으니 한국어 UI만
쓴다면 KR 전용으로 줄여라"라고 했지만, 이 게임은 **일본어를 실제로 지원한다**
(`Locale.SUPPORTED = ["en","ko","ja"]`, JP 번역 전량 존재). KR 전용으로 줄이면 일본어가 깨진다.

대신 안전한 접근을 썼다 — 이 게임은 **사용자 텍스트 입력 UI가 없어 표시 문자가 전부 정적**이므로,
프로젝트 소스에 등장하는 문자만 남기면 된다. 그 결과 세 언어를 모두 유지하면서 2.36MB → 0.22MB.
수집 대상은 `.gd`의 **문자열 리터럴**과 `.tres`/`.tscn` 데이터다 — 주석의 한글까지 넣으면
폰트가 커질 뿐 아니라, 주석을 고칠 때마다 커버리지 검사가 깨져 쓸모없는 실패를 낸다.

새 문자를 추가하면 조용히 두부(□)가 되는 위험이 있으므로 **CI 검사를 함께 넣었다**
(`tools/check_font_coverage.gd` — Godot만으로 동작해 CI에 의존성 추가가 없다).
재생성 도구는 `tools/subset_fonts.py`이며, 폰트에 없는 글자가 새로 들어오면 빌드가 실패한다.

**A5 보류 사유:** `progressive_web_app/icon_*` 3종이 전부 비어 있고 `config/icon`도 미설정이라,
아이콘을 먼저 준비해야 한다. 익스포트 검증 없이 켜면 CI 배포가 깨질 위험이 있다.

**✅ 익스포트 프리셋 정정(완료):** `export_presets.cfg`가 타깃과 정확히 반대였다 —
`for_desktop=true, for_mobile=false`. 모바일 웹이 주 타깃이므로 뒤집었다.

**남은 과제 — VRAM은 아직 안 줄었다.** A3에서 쓴 Lossy(WebP)는 *파일 용량*만 줄이고,
GPU에 올라갈 때는 여전히 RGBA8로 전개된다(텍스처 138장 = VRAM 약 26.7MB). 저사양 모바일
브라우저의 WebGL 컨텍스트 메모리 한계에서는 이게 별개의 위험 요소다. 줄이려면 임포트 모드를
`compress/mode=2`(VRAM 압축, ETC2/ASTC)로 바꿔야 하는데, 블록 압축은 알파 경계·선명한
UI에서 손실 WebP보다 아티팩트가 크므로 **실기기에서 눈으로 보며** 판단해야 한다.
프리셋(`for_mobile=true`)은 그때 바로 쓸 수 있도록 이미 맞춰 뒀다.

## 2. Phase B — 런타임 핫패스 (인게임 프레임)

목표: 대난전(좀비 300+) 프레임 안정화. 효과 순.

| # | 작업 | 파일 | 상태 |
|---|---|---|---|
| B1 | **Ground/PropField 재드로우를 격자 기준으로**: 임계값이 0.7px(Ground)·4px(PropField)라 이동 중 사실상 매 프레임 화면 전체를 재발행했다. 두 노드 모두 드로잉이 월드 좌표로 보정돼 있어 위치를 128px 격자에 스냅해도 화면은 동일하다 — 스냅 칸이 바뀔 때만 재드로우하고, 스냅 오차만큼 그리는 범위에 여유를 뒀다. **실측 1/100 감소** | `Ground.gd`, `PropField.gd` | ✅ 완료 |
| B2 | **DamageNumber 동시 활성 상한**: `MAX_PER_FRAME 14`만 있어 이론상 ~500개 동시 텍스트 렌더. `MAX_ACTIVE=36` 추가, `MAX_PER_FRAME` 14→8, 보스용 `bypass_cap`에 별도 상한 2/프레임, 문자열 폭 spawn 시 1회 캐시, 팝 크기 2px 양자화 | `DamageNumber.gd` | ✅ 완료 |
| B3 | **Bullet을 Node2D로**: 명중은 스윕+공간 해시로 하는데 씬 루트가 `Area2D`+CollisionShape라 총알 수십~수백 발이 물리 broadphase에 상시 등록됨. Zombie.tscn엔 CollisionShape가 없어 죽은 비용이었다. Player.tscn의 미사용 `Hurtbox`도 제거 | `Bullet.tscn`, `Bullet.gd`, `Player.tscn` | ✅ 완료 |
| B4 | **공간 해시 할당 제거**: `zombies_near()`가 호출마다 새 Array 생성(프레임당 ~70회), 그리드 rebuild가 셀 배열을 매번 새로 만듦(프레임당 200~450회). 셀 배열 재사용 + 조회 버퍼 재사용 + 빈 셀 주기적 GC | `Events.gd` | ✅ 완료 |
| B5 | **`zombies_in_radius(pos, r)` 헬퍼 + 전수 스캔 이관**: 헬퍼 추가 후 **조준 경로**(WeaponModule·TurretUnit — 매 프레임 O(N))·**스플래시**·**번개 후보 탐색**·**마늘 오라 피해 틱**을 이관. ProjectileWeapon·Tesla·HolyWater·MeleeArc·Ultimate·Drone은 아직 전수 스캔(모두 발사·착탄 시점이라 저빈도) | `Events.gd`, `WeaponModule.gd`, `TurretUnit.gd`, `Bullet.gd`, `Lightning.gd`, `GarlicAura.gd` | 🟡 부분 완료 |
| B6 | **Boss 피격 연출을 Zombie 방식으로 통일**: 피격마다 Tween 생성(초당 수십 개) → `_flash` 감쇠 변수로 | `Boss.gd` | ✅ 완료 |
| B7 | **y_sort 대상 축소**: 좀비·총알·FX·숫자 전부 Main 루트(y_sort=on) 직속이라 자식 500~800개를 매 프레임 정렬. `Units`(y_sort on)/`Effects`(off) 컨테이너 분리 | `Main.tscn`, 스폰 호출부 | ⬜ 미착수 |
| B8 | **UITheme 전역 `node_added` 훅**: 모든 노드 추가(풀 재사용 포함)에 콜백이 걸린다. **제거 대신 유지**하기로 했다 — 씬에 직접 배치된 버튼은 `apply_button_style()`을 거치지 않아 옮기면 눌림 피드백이 조용히 사라진다. 대신 비용은 `is Button` 타입 검사 1회로 끝나고(풀 스폰은 Node2D라 즉시 탈락), **중복 connect 버그는 메타 가드로 해소**했다 | `UITheme.gd` | 🟡 버그만 수정 |
| B9 | **FXLightning 머티리얼 공유 + 동시 상한**: 인스턴스마다 `CanvasItemMaterial.new()`라 배칭이 깨졌다 → `static` 공유로 전환. 다중 낙뢰가 한 프레임에 전부 생성되던 문제는 `MAX_ACTIVE=6` 상한 + `spawn()` 팩토리로 방어하고, `Main._clean_slate()`에 리셋을 등록했다(상한이 영구히 막히는 것 방지). 노드 재사용 풀링은 4초 간격 효과라 실익이 작아 생략 | `FXLightning.gd`, `Lightning.gd`, `Main.gd` | ✅ 완료 |
| B10 | **HUD 오버레이 alpha=0 시 `visible=false`**: FlashOverlay/LowHpOverlay가 투명한 채 상시 풀스크린 블렌딩(프레임당 ~184만 px fill-rate 낭비). 씬 기본값도 `visible=false`로. Vignette는 실제 텍스처가 있는 의도된 연출이라 유지 | `HUD.tscn`, `HUD.gd` | ✅ 완료 |
| B11 | **HP바 트윈 kill 누락**: `_update_hp_bar`가 이전 트윈을 kill하지 않아 연속 피격 시 트윈이 누적되고 바가 튐(성능+시각 버그) | `HUD.gd` | ✅ 완료 |
| B12 | 엔진 설정: `max_physics_steps_per_frame=4`(death spiral 방지) 적용. **`physics_ticks_per_second=30`은 적용하지 않았다** | `project.godot` | 🟡 일부만 적용 |
| B13 | `AUTOSAVE_INTERVAL` 4→20초: 웹에선 저장이 IndexedDB 동기화라 히칭 유발 | `Player.gd` | ✅ 완료 |
| | ↑ 당시 근거였던 "웨이브/상점 체크포인트 저장"은 둘 다 사라졌다(P0-2·P0-3). 지금 런 저장을 받치는 것은 이 20초 주기와 백그라운드 전환뿐이고, 퀘스트·도전과제는 보스 처치(`milestone_reached`)에서 따로 내려간다 | | |

**B12에서 물리 틱 30Hz를 적용하지 않은 이유:** 좀비·총알·플레이어가 전부 `_physics_process`에서
이동하는데 이 프로젝트는 `config/features="4.1"`이라 **2D 물리 보간이 없다**. 틱을 30으로 낮추면
물리 연산은 절반이 되지만 화면상 움직임도 30fps로 끊겨 보인다 — 성능과 체감을 맞바꾸는 결정이라
실기기 측정 후에 판단할 항목이다. death spiral 방지용 `max_physics_steps_per_frame`만 적용했다.

**B5 이관 범위를 조준·스플래시로 한정한 이유:** 피해 루프는 순회 중 `take_damage()`를 호출하는데,
그 콜백이 다시 광역 질의를 하면 공유 버퍼가 덮여 순회가 깨진다. 현재 코드에는 그런 경로가 없지만
(`Zombie`/`Boss`는 광역 질의를 하지 않는다) 방어적으로, 콜백이 없는 **읽기 전용 조준 질의**부터
이관했다. 스플래시는 바깥 명중 순회와 다른 버퍼(`_radius_buf`)를 쓰도록 분리해 안전을 확보했다.

## 3. Phase C — 마무리 (낮은 심각도)

**완료분:**
- ✅ HolyWater: `filter()`+람다로 프레임당 배열 3개 + Callable 3개 할당 → 역방향 `remove_at()` 제자리 제거
- ✅ PropField: `_cell_prop()`이 호출마다 7키 Dictionary 생성 → 주변 3×3 장애물 캐시, 셀 경계에서만 갱신
- ✅ SpriteFX `_recycle()`: 트리 분리 누락(비활성 노드의 `_process`가 계속 돎) + `_active` 음수 가드
- ✅ 죽은 코드 삭제: `Player.gd`의 미사용 O(N) 타겟팅(`_get_nearest_zombie`/`_is_live_target`)과
  그 캐시 변수·이제 아무도 안 쓰는 `attack_range` export까지 함께 제거
- ✅ 게임오버 시 좀비 전원이 매 프레임 `get_first_node_in_group("player")` 호출 →
  개체별 위상으로 분산해 30프레임에 1회(광고 부활이 있어 완전히 끊지는 않는다)

**남은 것:**
- ✅ GarlicAura: 매 물리 프레임 절차 드로우(살 폴리곤 9개를 매번 새로 할당) → 재드로우를 20Hz로
  낮추고 삼각형 버퍼를 재사용. 피해 틱도 전수 스캔에서 반경 질의로 이관

**남은 것:**
- 스폰 dict `duplicate()` → 티어별 스탯 캐시 (`ZombieSpawner.gd`). 다만 스폰은 이미 프레임당
  12개로 분산돼 있어 실익이 작다
- 부메랑 투사체 풀링 (`BoomerangProj.gd`) — 2초에 1~2개라 영향 미미
- B5 잔여 무기(ProjectileWeapon·Tesla·HolyWater·MeleeArc·Ultimate·Drone)의 전수 스캔.
  전부 저빈도(발사·착탄 시점)라 300마리 기준으로도 비용이 작다
- StyleBox 정적 캐시: `UIStyle`의 disabled/focus 싱글턴화, `UIListRow` 상태별 3종 캐시, 메뉴 리스트 행 재사용
  (참조 구현이던 `ShopPanel._refresh_buttons()` 는 상점 폐기로 사라졌다 — HANDOFF P0-3. `UIListRow` 를 참고할 것)
- HUD 로드아웃 슬롯 전체 재생성 → 변경분만 갱신 (`HUD.gd:748-763`)
- 좀비 사망 플레이어 조회: 게임오버 시 좀비 320마리가 각자 매 프레임 그룹 조회 → `player_died` 구독 (`Zombie.gd:127-129`)
- 폰트 크기 19종 → 인접 값 통합(글리프 아틀라스/래스터화 히칭 감소)

## 4. Phase D — CI/인프라 ✅ 완료

- ✅ 임포트 스텝의 `|| true` 제거 + 타임아웃 120→600s. 예전에는 타임아웃으로 잘려도 그대로
  export로 넘어가 **임포트가 덜 된 산출물이 조용히 배포**될 수 있었다. 컨테이너 셸이 dash라
  `pipefail`을 못 쓰므로 파이프 대신 로그 파일로 받아 종료 코드(124)를 직접 검사한다.
- ✅ pck 크기 검증 스텝 추가(상한 24MB) — 용량 회귀 방지. A3 이후 더 조일 것.
- ⬜ (배포 계획 확정 시) Android 익스포트 프리셋 추가 — 현재 Web 프리셋뿐

## 5. 별건 — 리뷰 중 발견한 동작 버그 ✅ 수정

- **관통탄이 프레임당 1마리만 타격**: `Bullet.gd` 스윕 판정이 명중 즉시 `return`이라 `pierce`가
  남아도 같은 프레임의 추가 관통이 무시됐다. 밀집 대열에서 석궁 계열 실 DPS가 설계보다 낮았다.
  소멸했을 때만(`not _alive`) 순회를 멈추도록 수정. **밸런스에 영향이 있으므로 관통 무기 체감을
  한 번 확인할 것** — 의도된 설계대로면 관통 무기가 이전보다 강해진다.

---

## 5-B. 아틀라스 정리 — 죽은 아트 제거 + 프롭 테마 분리 ✅ 완료

아틀라스를 만들 때는 "게임에 있는 그림을 전부 한 장에" 넣었는데, 그중 **실제로 그려지지 않는
그림이 상당수**였다. 시트는 2의 거듭제곱이라 한 변이 한 단계 올라가면 VRAM 이 4배다 — 안 쓰는
그림 몇 장이 시트 전체의 크기 등급을 올리고 있었다.

**빼낸 것**

| 대상 | 왜 죽었나 |
|---|---|
| `run_{veteran,hunter,engineer}.png` (576×168 등) | 세 캐릭터 모두 `run_frames = 0` → `Player.gd` 의 시트 분기에 한 번도 진입하지 않는다. 경로를 문자열로 조립해 grep 에도 안 잡혔다 |
| `player_{veteran,hunter,engineer}.png` | 옛 아트. `sprite_path` 가 가리켜 쓰는 것처럼 보였지만 `_fit_shadow()` 가 `get_size().x` **숫자 하나**만 얻으려고 load 할 뿐 그리지 않았다 |
| `player.png` (48×54) | 더 옛 아트. `Player.tscn` 의 기본 텍스처인데 `_apply_character_sprite()` 가 즉시 교체해 화면에 뜬 적이 없다 |
| `arena_field.png.import` | 원본 PNG 가 없는 고아 임포트 파일 |

숫자 하나 때문에 남아 있던 `player_<id>.png` 는 `CharacterData.shadow_ref_width`
(80 / 104 / 64 — 옛 그림의 폭 그대로)로 바꿨다. 그림자 크기는 이전과 **완전히 동일**하다.
`sprite_path` 는 실제로 쓰는 `idle_<id>.tres` 를 가리키도록 정정했다(메인메뉴의 초상화 폴백
경로가 죽은 리소스를 가리키고 있었다).

**프롭은 테마별로 나눴다.** 프롭 15종이 게임플레이 시트에 통째로 들어가 있었는데, 한 판에서
뜨는 테마는 하나뿐이라 나머지 두 테마 몫이 늘 VRAM 에 얹혀 있었다.
`assets/sprites/props/{suburb,city,lab}/` → `assets/atlas/props/<테마>.png` 3장으로 분리하고
`PropField` 가 선택 테마 폴더만 `load()` 하게 했다. 상자 2종(`chest_*`)은 테마 프롭이 아니라
픽업이므로 `assets/sprites/` 로 올려 게임플레이 시트에 남겼다.

**결과**

| | 전 | 후 |
|---|---|---|
| 게임플레이 시트 | 2048×2048 (70장, 점유 30%) | **1024×1024** (48장, 점유 59%) |
| 게임플레이 VRAM(RGBA8) | 16.0MB | **4.0MB** |
| 프롭 | 게임플레이 시트에 동봉(항상 상주) | 테마별 3장, 한 판에 최대 1장만 로드 |
| UI 시트 | 2048×2048 | 2048×2048 (변화 없음) |

프롭 시트도 패커에 **직사각형 후보**(1024×512 등)를 넣어 한 단계씩 낮췄다 — 정사각형만
시도하면 512 에 아슬아슬하게 안 들어간 도심 프롭이 1024×1024(점유 17%)를 잡는다.
`build_atlas.py` 는 원본이 사라진 `.tres` 를 자동으로 정리한다(남으면 region 이 그 자리에 새로
들어온 다른 그림을 가리켜 엉뚱한 스프라이트가 그려진다).

젬은 요청대로 0.8배로 리샘플했다 — `xp_gem.png` 80×72 → 64×58, `COLLECT_SCALE` 은 0.4 그대로라
화면 표시가 32×28.8px → 25.6×23.2px 로 함께 줄어든다(의도된 동작).

규칙은 `ASSET_PIPELINE.md` 1절에 반영했다.

---

## 5-C. UI 아틀라스 분리 — 인게임 VRAM 16MB → 4MB ✅ 완료

5-B 에서 프롭에 적용한 것과 같은 판단을 UI 시트에도 했다. `ui.png` 는 2048×2048(16MB)로
**항상 상주**했는데, 그 면적의 60% 가 메인메뉴에서만 쓰는 6장이었다.

| | 원본 | 면적 |
|---|---|---|
| `portrait_{veteran,hunter,engineer}` | 451×369 / 357×445 / 374×351 | 456k px |
| `theme_{suburb,city,lab}` | 512×332 / 512×286 / 512×386 | 515k px |
| **메뉴 전용 6장** | | **971k px (전체의 60%)** |
| 인게임 아이콘 49장 | 대부분 128×128 | 654k px |

원본이 큰 6장이 시트 한 변을 2048 로 밀어올리고 있었고, 게임 플레이 내내 그 16MB 가
VRAM 에 얹혀 있었다. `menu` 아틀라스로 갈라냈다.

| | 전 | 후 |
|---|---|---|
| `ui.png` | 2048×2048 = **16.0MB, 항상** | 1024×1024 = **4.0MB, 항상** |
| `menu.png` | — | 2048×1024 = 8.0MB, **메인메뉴에서만** |

**분리가 실제로 먹히는지 먼저 확인했다.** 폴더만 나눠서는 소용이 없고, 그 시트를 물고 있는
참조가 전부 끊겨야 해제된다. 세 조건을 모두 만족했다.
- 참조는 `MainMenu.gd` 두 곳뿐(`:544` 초상화, `:1189` 테마 썸네일)이고 전부 동적 `load()` —
  `preload`(영구 고정) 없음
- `.tscn`/`.tres` 의 `ext_resource` 고정 참조 없음
- 메뉴 → 게임 전환이 `change_scene_to_file` 이라 메뉴 씬이 해제됨

그리고 실측으로 못을 박았다 — `Main.tscn`·`Player.tscn`·`Zombie.tscn`·`HUD.tscn` 과 인게임
아이콘 9종을 전부 `load()` 한 뒤 `ResourceLoader.has_cached("res://assets/atlas/menu.png")`
가 `false` 임을 확인했다(헤드리스). 인게임 리소스 어디에서도 메뉴 시트에 도달하지 못한다.

5-B 와 합치면 **인게임 상주 VRAM 이 32MB → 8MB** 가 됐다(게임플레이 16→4, UI 16→4).

### 같이 검토했으나 하지 않은 것

- **과대 해상도 5장**(`drone` 256×199 → 표시 34px, `weapon_boomerang`·`chainsaw_summon`·
  `field_mine`·`flame_pod`): 다 줄이면 11만 px 이 빠지지만 시트는 1024×1024 그대로라
  (점유 59%→48%) **VRAM 등급이 안 바뀐다**. 페이로드만 ~110KB — 우선순위 낮아 보류.
- **타일 3종 `preload`**(`Ground.gd`): 한 판에 쓰는 건 1종인데 `const preload` 라 셋 다
  상주한다. 지연 로드로 바꾸면 512KB. 같은 패턴이지만 규모가 작다.

---

## 5-D. BGM 루프 절단 — pck 의 약 70% 를 차지하던 오디오 ✅ 완료

웹 pck 는 전량 받아야 첫 프레임이 뜨는데 BGM 3곡이 8.34MB 로 남은 pck 의 약 70% 였다.
비트레이트는 A1 에서 이미 96kb/s 모노까지 짜여 있어 남은 수단은 **길이**뿐이었다.

| 곡 | 전 | 후 | 용량 |
|---|---|---|---|
| `bgm_title` | 3:11 | **55.8s** | 2247KB → 655KB (-71%) |
| `bgm_game_1` | 3:08 | **65.0s** | 2206KB → 763KB (-65%) |
| `bgm_game_2` | 5:48 | **67.1s** | 4087KB → 787KB (-81%) |
| **합계** | | | **8.34MB → 2.15MB (-74%, 6.19MB 절감)** |

도구는 `tools/make_bgm_loop.py` 다. 곡의 반복 주기를 자기상관으로 찾아 그 정수배 길이로
자르고(마디가 맞아야 되돌아갈 때 음악적으로 튀지 않는다), 되돌아가는 지점을 0.75초
크로스페이드로 마감한 뒤 세 곡 공통 라우드니스로 정규화한다.

### 실측 결과

| 곡 | 클릭(통상 변화량 대비) | 튐(루프 자체 변화량 대비) | 음량 |
|---|---|---|---|
| `bgm_title` | 57% 지점 | **7% 지점** | -12.48 dBFS |
| `bgm_game_1` | 3% 지점 | **20% 지점** | -12.49 dBFS |
| `bgm_game_2` | 6% 지점 | **15% 지점** | -12.46 dBFS |

"튐" 이 낮다는 것은 루프 전환의 스펙트럼 변화가 그 곡이 원래 갖는 변화보다도 완만하다는
뜻이다 — 전환이 음악의 일부처럼 지나간다.

### 덤으로 고친 것 — 곡 사이 음량이 3.8dB 어긋나 있었다

원본은 -14.20 / -11.17 / -10.44 dBFS 로 제각각이었다. 테마마다 BGM 이 바뀌는 게임이라
테마를 옮길 때 음량이 튀었다는 뜻이다. 어차피 자르면서 재인코딩하므로 공통 목표
(-12 dBFS RMS)로 맞췄다 — 세 곡이 **0.03dB 이내**로 모였다. 게인만으로 안 되는 구간은
드문 피크만 tanh 니로 눌러 담았고(리미터가 만진 샘플 1.0~1.3%), 그래서 조용한 대목을
골랐다는 이유로 곡이 작아지지 않는다.

### 코덱은 MP3 를 유지했다

Ogg Vorbis 를 검토했으나 **원본이 이미 MP3 라 탠덤 손실이 커져 오히려 나빴다** —
같은 60초 구간에서 멜 거리가 MP3 재인코딩 0.72dB vs Vorbis q2 2.01dB(용량은 Vorbis 가
작지만 MP3 64k(1.37dB)보다도 나쁘다). 재인코딩 2세대 손실 0.72dB 는 비트레이트를 64k 로
낮출 때의 절반 수준이다. `SoundManager` 의 `stream is AudioStreamMP3` 분기도 그대로 둔다.

### 지표를 만들며 세 번 틀렸다 — 기록해 둔다

1. **파형 SNR 로 판단하려 했다.** 손실 코덱은 위상·프리에코 차이가 지배해 SNR 이 22dB 여도
   지각 차이는 작다. 로그 멜 스펙트럼 거리로 바꿨다.
2. **선택 기준과 검증 기준이 달랐다.** 자기상관 점수로 고르고 다른 값으로 검증하니
   "점수는 좋은데 들으면 튀는" 지점을 골랐다(`bgm_game_1` 이 튐 99% 지점). 검증하는 값을
   그대로 최적화하도록 바꿨다.
3. **기준 분포를 원곡 전체로 잡았다.** 플레이어가 반복해 듣는 것은 루프 구간이고 귀는 그
   변화 폭에 적응한다. 기준을 루프 구간으로 바꾸니 선택과 검증이 비로소 일치했다.

절차와 판정 기준은 `ASSET_PIPELINE.md` 4절에 있다.

---

## 5-E. 프레임 저하 원인 규명 — 좀비 그림자의 `modulate` ✅ 완료

"프레임이 잘 안 나온다" 는 보고를 받고 프로파일링했다. **범인은 한 줄이었다.**

### 먼저 잴 수 있게 만들었다

지금까지 성능 측정의 한계는 "헤드리스는 더미 렌더러라 렌더 경로를 못 잰다" 였다
(이 문서 상단 벤치마크 절 참조). 가상 디스플레이(Xvfb) + 소프트웨어 GL(llvmpipe)로
**진짜 OpenGL 컨텍스트**를 띄워 `RenderingServer` 계측을 쓰도록 `tools/profile_frame.gd`
를 만들었다. draw/items 는 GPU 종류와 무관한 명령 수라 실기기와 같은 값이다.

시나리오는 `time_skip` 으로 난이도 시계를 당겨 좀비 320마리 후반 난전을 재현한다 —
초반 10분은 좀비가 10~30마리라 문제가 아예 안 드러난다.

### 원인

`Zombie.tscn` 의 `Shadow` 에 `modulate = Color(1,1,1,0.5)` 가 걸려 있었다. Godot 캔버스
배처는 **연속된 아이템의 modulate 가 다르면 배치를 끊는다.** 좀비마다
`그림자(α0.5) → 몸통(흰색)` 이 번갈아 나와 아이템마다 배칭이 깨졌다.

좀비 320마리 격리 측정으로 범인을 좁혔다:

| 실험 | 드로우 콜 | 아이템 |
|---|---|---|
| 기본 | 152 | 188 |
| **그림자 modulate 만 흰색으로** | **39** | 188 |
| 그림자 텍스처를 몸통과 통일 | 152 | 188 |
| 그림자 숨김 | 38 | 124 |

그림자는 **그대로 다 그려지는데**(아이템 수 동일) 드로우 콜만 무너진다. 텍스처 통일은
효과가 0 이니 **아틀라스 작업(6-B)은 제대로 작동 중**이었고, 남은 범인이 modulate 였다.

### 고친 방법

알파 0.5 를 구운 좀비 전용 텍스처 `shadow_soft.png` 를 만들고 `Zombie.tscn` 의 `modulate`
를 없앴다. 공유 `shadow.png` 에 굽지 않은 이유는 사용처 6곳의 알파가 제각각이기 때문이다
(플레이어 0.55 · 보스 0.5 · 터렛 1.0 · 프롭 0.38 · 지뢰). 특히 터렛은 현재 불투명이라
공유 텍스처에 0.5 를 구우면 **위로 보정할 방법이 없다.** 좀비만 개체가 수백이라 배칭에
영향을 주므로 전용 텍스처를 따로 두는 쪽이 안전하다.

### 결과 (실렌더 실측, 3회 평균)

| | 전 | 후 |
|---|---|---|
| 좀비 320마리 격리 | draw 152 / items 188 | **draw 37** / items 176 |
| 전체 게임 | draw 435 / items 674 | **draw 239** / items 658 |

**드로우 콜 −45%.** 아이템 수는 그대로이니 그리는 내용은 하나도 줄지 않았다.
외형도 픽셀로 확인했다 — 옛 방식과 최대 차이 1/255, 평균 0.07/255(손실 압축 반올림 수준).

### 회귀 방지

`tools/verify_batching.gd` 를 추가하고 CI 스텝(`Check canvas batching`)에 넣었다.
핫 씬(좀비)의 스프라이트에 `modulate`/`self_modulate` 가 걸리거나 서로 다른 아틀라스를
쓰면 빌드를 실패시킨다. 일부러 회귀를 넣어 잡히는 것까지 확인했다.
**눈으로는 안 보이고 프레임만 깎이는 종류**라 안전망 없이는 반드시 되돌아온다.

### 남은 계통 (같은 프로파일에서 측정)

수정 전 기준 계통별 몫: 좀비 ~149(40%) · **바닥 ~71(19%)** · FX ~51(14%) · 프롭 ~11.

바닥은 별개의 같은 계열 문제다 — `Ground._draw_decals` 가 **색이 제각각인
`draw_circle` 42개 + `draw_line` 45개**를 발행한다(재드로우 1회당 총 151개 명령).
색이 다르니 서로 배칭되지 않는다. 그리고 이 명령 목록은 **재드로우 빈도와 무관하게 매 프레임
GPU 에 재제출된다** — B1 의 재드로우 억제(1/100)는 CPU 재구축 비용만 줄였고 프레임당
드로우 콜은 그대로였다. 고치려면 데칼을 작은 아틀라스의 텍스처 쿼드로 바꿔야 한다.

### 측정 방법에서 배운 것

- **`off=`(계통 끄기)는 몫을 과대평가한다** — 그 계통을 끄면 나머지의 배칭까지 달라진다.
  바닥이 격리 53 vs ablation 166 으로 3배 차이가 났다. `only=`(격리)가 더 깨끗하다.
- 시나리오를 유지하지 않으면 **게임오버 화면을 측정하게 된다.** 값이 통째로 얼어붙고
  `phys_ms` 가 0.06 으로 떨어지는 것이 그 신호다.
- llvmpipe 의 `frame_ms` 절대값은 실기기와 무관하다. **draw/items 만 이식 가능한 수치다.**

---

## 5-F. 바닥 데칼을 텍스처 쿼드로 — 드로우 콜 239 → 185 ✅ 완료

5-E 에서 남은 가장 큰 덩어리였다. `Ground._draw_decals` 가 `draw_circle` 42개 +
`draw_line` 45개를 발행하고 있었다.

### 먼저 원인을 잘못 짚을 뻔했다

5-E 의 교훈("modulate 가 배칭을 깬다")을 그대로 적용해 **색을 텍스처에 굽는** 설계를
세웠다. 그러면 테마의 `mark` 색이 `themes.tres` 에서 오는 데이터 구동인데 그 연결이
끊어진다. 만들기 전에 실험으로 확인한 것이 다행이었다 — 색은 그대로 두고 프리미티브만
쿼드로 바꾸자 그것만으로 93 → 43 이 됐다.

**두 규칙은 적용 범위가 다르다:**

| | 무엇이 배칭을 깨나 |
|---|---|
| 서로 다른 CanvasItem 사이 | `modulate` 가 다르면 깨진다 (5-E, 좀비 그림자) |
| 한 CanvasItem 안(`_draw()`) | **색은 안 깨진다**(정점 데이터). **프리미티브 종류**가 깬다 |

좀비는 스프라이트마다 별도 CanvasItem 이라 전자, 바닥은 단일 CanvasItem 이라 후자였다.

### 고친 방법

흰색+알파 모양만 담은 `decal_blob.png` / `decal_streak.png`(+ 세로용) 을 만들어
게임플레이 아틀라스에 넣고, 색은 `draw_texture_rect` 의 틴트로 그대로 넘겼다.
`mark` 색은 데이터 구동으로 유지된다.

> 모양은 원본과 같게 맞췄다. 처음에 부드러운 그라데이션으로 만들었더니 도심·연구소의
> 얼룩이 **뚜렷한 원반에서 흐릿한 자국으로 바뀌었다** — 스크린샷 비교에서 바로 드러났다.
> 성능을 위해 아트를 조용히 바꾸면 안 된다. 내부를 불투명하게 채우고 가장자리 2px 만
> 감쇠시키니 픽셀 차이가 평균 0.02/255, 최대 21/255(테두리 AA)로 떨어졌다.

세로/가로 획은 `draw_set_transform` 없이 축 정렬 쿼드로 그린다 — 변환 명령이 하나 덜 붙는다.

### 결과 (실렌더 실측)

| | 5-E 전 | 5-E 후 | 5-F 후 |
|---|---|---|---|
| Ground 격리 | 93 | 93 | **43** |
| 전체 게임 | 435 | 239 | **185** |

두 수정을 합쳐 **드로우 콜 435 → 185 (-57%)**. 아이템 수는 674 → 576 으로,
줄어든 98 은 데칼 하나가 원(폴리곤 여러 정점) 대신 쿼드 하나가 된 몫이다.

### 회귀 방지

`verify_batching.gd` 에 규칙 B 검사를 추가했다 — `_draw()` 를 가진 핫 스크립트에서
프리미티브를 **루프로** 발행하면 실패시킨다. 루프 밖 한두 개 고정 발행은 통과시킨다
(드로우 콜이 개수에 비례해 늘지 않는다).

핫패스가 아닌 곳은 `# batching-exempt: <이유>` 로 넘긴다. `Ground` 의 절차적 체커보드
폴백이 그 경우다 — 세 테마가 전부 `grass/stone/frozen` 이라 타일 경로에서 `return` 되고,
도달하는 유일한 경로는 `ThemeManager.selected()` 가 null 이고 `THEMES` 추첨이 `desert`
(타일 텍스처 없음)를 뽑는 때다. **파일 통째 예외는 두지 않았다** — 이유를 코드 옆에
남겨야 새로 들어오는 위반이 계속 잡힌다. 회귀 2종(데칼을 원으로 되돌리기 / PropField 에
루프 프리미티브 넣기)을 일부러 넣어 잡히는 것까지 확인했다.

---

## 5-G. 정정 — 계통별 귀속이 틀렸다, 그리고 남은 것은 HUD 다

5-E·5-F 의 **수정과 개선폭은 유효하지만, 계통별 절대 귀속값이 틀렸다.**

### 무엇이 틀렸나

`profile_frame.gd` 의 `only=`(격리) 모드가 `c is CanvasItem` 으로만 노드를 걸렀는데
**`HUD` 는 `CanvasLayer` 라 `CanvasItem` 이 아니다.** 격리한다면서 HUD 가 계속 그려졌고,
"좀비 격리 37 드로우 콜" 의 대부분이 실은 HUD(40)였다.

수정 자체의 개선폭(152 → 37)은 그대로다 — HUD 가 양쪽에 똑같이 섞여 있었으므로 상대
비교는 성립한다. 틀린 것은 "좀비가 37을 쓴다" 는 절대값이다.

`only=nothing` 으로 바닥값(1 draw / 1 item)을 먼저 재도록 도구를 고쳤다.

### 다시 잰 계통별 격리 (좀비 320마리, 두 수정 이후)

| 격리 | draw | items | 배칭비 |
|---|---|---|---|
| `nothing` (바닥) | 1 | 1 | — |
| **좀비 320마리** | **3** | 118 | 39:1 |
| **바닥(Ground)** | **4** | 197 | 49:1 |
| 프롭 | 13 | 13 | 1:1 |
| 날씨 | 3 | 3 | — |
| **HUD** | **40** | 115 | 2.9:1 |

`items` 는 제출한 캔버스 명령 수, `draw` 는 배칭 후 실제 GL 드로우 콜이다. 좀비 320마리의
items 가 118뿐인 것은 **화면 밖 컬링** 때문이다(실제 표시 ~59마리 × 2 스프라이트).

### 전체 게임 ablation (기준 138)

| 끈 것 | Δ draw |
|---|---|
| **HUD** | **-61 (44%)** |
| **FX 레이어** | **-38 (28%)** |
| 프롭 | -20 |
| 바닥 | **+5** |
| 좀비 | **+12** |

**좀비·바닥은 꺼도 드로우 콜이 늘어난다.** 이미 3~4개라 뺄 게 없고, 빼면 남은 것들의 배칭
순서만 흐트러진다. 바닥 데칼을 타일 이미지에 굽는 것은 최대 4개 절약에 비반복 다양성을
잃는 거래라 **하지 않는다**(5-F 에서 이미 쿼드화로 접었다).

### 남은 것 — HUD

HUD 의 40 드로우 콜은 정확히 "아틀라스 밖 텍스처" 문제다.

- `ui_coin` · `ui_heart_full` · `ui_heart_empty` — 전부 48×48 이고 **매 프레임 그려지는데
  아틀라스 밖**이다. `ASSET_PIPELINE.md` 가 `assets/ui` 루트를 "한 번에 한 장만 뜨는 큰
  그림이라 배칭 이득이 없다" 로 분류했는데 **이 셋은 그 분류가 틀렸다.** UI 아틀라스로
  옮기면 텍스처 교체가 사라진다.
- `frames/`·`hud/` 나인패치 12장 — `StyleBoxTexture` 무손실 고정이라 의도적으로 제외한
  것들이다. 각각이 드로우 콜 하나다. 아틀라스로 묶으려면 나인패치와 AtlasTexture 의
  region 처리를 확인해야 해서 별건이다.

별개로, **HUD 격리는 `frame_ms` 가 66.7 로 유독 높다**(나머지는 10~17). `vignette`(512²를
화면 크기로 확대)와 `fog_vision`(720×1280) 전체화면 알파 오버레이의 fill-rate 다.
llvmpipe 라 증폭돼 보이지만 모바일 GPU 에서도 전체화면 알파 블렌딩은 비싼 축이다 —
"프레임이 안 나온다" 의 남은 축이 여기일 수 있다. 실기기 확인이 필요하다.

---

## 5-H. 진짜 남은 병목 — 무기 비주얼 (측정 완료, 미수정)

5-G 까지의 측정은 **무작위 레벨업**으로 빌드를 굴렸다. 그래서 어떤 무기를 들었느냐에 따라
수치가 흔들렸고, 계통별 표에서 "나머지" 로 뭉뚱그려져 있었다. 무작위를 제거하고
(`levels=0`) 무기를 하나씩 강제로 들려(`give=`) 다시 재니 그림이 완전히 달라졌다.

| 보유 무기 | draw | Δ vs 기본(81) |
|---|---|---|
| 없음(기본 총만) | 81 | — |
| **오브 실드** | 202 | **+121** |
| **테슬라** | 193 | **+112** |
| **마늘 오라** | 172 | **+91** |
| 라이트닝 | 111 | +30 |
| **4종 전부** | **307** | **+226** |

**무기 하나가 5-E·5-F 로 줄인 것 전부(-250)에 맞먹는다.** 그리고 이것들은 다 규칙 B —
`_draw()` 안에서 `draw_circle`·`draw_line`·`draw_polyline`·`draw_colored_polygon`·`draw_arc`
를 루프로 발행한다. 바닥에서 고친 것과 **똑같은 패턴이고 규모만 훨씬 크다.**

### 병렬 스윕으로 찾은 목록

에이전트 28개로 코드베이스를 훑고 각 후보를 반증 검증했다(확인 13건 · 반증 8건).

| 심각도 | 대상 | 위치 |
|---|---|---|
| high | `Orb._draw` | `scripts/Orb.gd:58` |
| high | `Tesla._draw` | `scripts/Tesla.gd:133` |
| high | `FXLightning._draw` | `scripts/FXLightning.gd:99` |
| high | `Ultimate._draw_arrowstorm` | `scripts/Ultimate.gd:179` |
| high | `TeslaCoil._draw_bolt` | `scripts/TeslaCoil.gd:71` |
| high | `FlySwarm._draw` | `scripts/FlySwarm.gd:63` |
| high | `BossArena._draw` | `scripts/BossArena.gd:179` |
| medium | `GarlicAura._draw` | `scripts/GarlicAura.gd:68` |
| medium | `Ultimate._draw_quake` / `_draw_orbital` | `scripts/Ultimate.gd:135,215` |

### 스윕이 틀린 것도 있었다 — 측정이 갈랐다

`Gold.set_value()` 가 젬 등급을 `body.modulate` 3색(황금/청백/흰색)으로 칠하는 것이
규칙 A 결함(좀비 그림자와 같은 급, 젬은 최대 140개)으로 보고됐다. **측정은 반증했다.**

| | draw | items |
|---|---|---|
| 기준 | 150 | 651 |
| 젬 틴트 전부 흰색 | 160 | 660 |
| 젬 숨김 | 132 | 601 |

틴트를 없애도 드로우 콜이 안 줄어든다. 대부분의 젬이 기본 등급(흰색)이라 연속된 젬 사이에서
modulate 가 거의 안 바뀌기 때문이다. **정적 분석이 패턴을 맞게 찾아도 실제 비용은 0일 수 있다
— 고치기 전에 재라.**

### 고친 것 (1차) ✅

| 무기 | 전 | 후 |
|---|---|---|
| 오브 실드 | +121 | **+48** |
| 마늘 오라 | +91 | **+48** |
| 테슬라 | +112 | **+27** |
| 라이트닝 | +30 | **~0** |
| **4종 전부** | **+226** | **+98** |

바닥(5-F)에서 검증한 방법을 그대로 썼다 — **프리미티브를 텍스처 쿼드로.** 색은 정점
데이터라 틴트로 그대로 넘기므로 데이터 구동이 유지된다.

- **`FXBurst`** — 이게 가장 컸다. 타격 이펙트라 **모든 무기가 공유**하는데, 동시 48개가
  각자 `draw_circle` 1~2개를 그렸다. 오브 하나만 들어도 상한에 붙는다. 쿼드로 바꾸자
  오브가 +121 → +73 이 됐고(다른 무기도 같이 내려갔다) 기본 빌드 기준선도 낮아졌다.
- **`Orb`** — 모양이 **완전히 정적**이라(회전은 노드 변환) 9커맨드를 통째로 한 장으로
  구웠다(`fx_orb_blade.png`). 쿼드 1개.
- **`Tesla` · `GarlicAura`** — 폴리라인·원·쐐기·호를 `QuadDraw` 헬퍼로 바꿨다.

**`scripts/QuadDraw.gd`** 를 만들어 공용화했다 — `disc` / `segment` / `polyline` /
`wedge` / `ring`. 전부 게임플레이 아틀라스의 같은 시트를 쓰므로 서로, 그리고 좀비·탄·바닥
쿼드와도 한 배치로 묶인다. `polyline` 은 폭이 넓으면 꺾임점에 원판을 얹어 이음매가 벌어지는
것을 막는다(`draw_polyline` 은 조인트를 스스로 채우지만 쿼드는 그러지 못한다).
`ring` 은 링 텍스처를 늘리는 대신 세그먼트 쿼드로 그린다 — 텍스처를 늘리면 두께까지
반지름에 비례해 늘어난다.

외형은 좀비 없이 결정적으로 렌더해 비교했다. 마늘 평균 1.68/255(링·살 경계의 서브픽셀
차이만), 오브는 모양·색이 동일하고 차이는 전부 공전 위상이다.

### 고친 것 (2차) ✅

남은 다섯 파일도 전부 `QuadDraw` 로 바꿨다 — `FXLightning` · `Ultimate` · `TeslaCoil` ·
`FlySwarm` · `BossArena`. 새 텍스처는 `fx_solid`(`draw_rect` 대체) 하나만 추가하면 됐다.

`Ultimate._draw_arrowstorm` 이 규모가 가장 컸다 — `for i in 42` 안에서 선 5~6개를 발행해
프레임당 약 237 프리미티브였다.

**최종 실측** (좀비 320, `levels=0`, 4회 중앙값):

| | 처음 | 지금 |
|---|---|---|
| 기본 빌드(기본 총만) | 81 | **80** |
| 4종 무기 빌드 | **307** | **185** |
| 무기가 더하는 몫 | +226 | **+105** |

편차가 ±10 정도 있다(좀비 위치·발동 타이밍이 실행마다 다르다). 기본 빌드가 거의 그대로인
것은 원래 기본 총만으로는 프리미티브를 별로 안 그렸기 때문이고, **무기를 갖출수록 이득이
커진다** — 실제 플레이는 후자에 가깝다.

`verify_batching.gd` 의 `HOT_DRAW` 에 열한 파일을 전부 등록했다. 이제 `_draw()` 에서
프리미티브를 루프로 발행하면 CI 가 막는다.

### 눈으로 확인이 남은 것

궁극기·기믹(테슬라 코일·파리떼)·보스 아레나는 발동 조건이 까다로워 스크린샷 비교를 못 했다.
좌표·색·순서를 그대로 옮긴 기계적 변환이고 `verify_environment`·`verify_boss_arena` 는
통과하지만, **실기기에서 한 번 눈으로 볼 것.** 특히 폴리라인을 쿼드로 바꾼 곳(번개·아레나
전기선)은 꺾임점 처리가 `draw_polyline` 과 다르다 — `QuadDraw.polyline` 이 폭 3px 초과일 때
꺾임점에 원판을 얹어 메우지만, 아주 날카로운 각에서는 차이가 날 수 있다.

### 원래 고치려던 방향### 원래 고치려던 방향

바닥(5-F)에서 검증한 방법을 그대로 쓴다 — **프리미티브를 텍스처 쿼드로.** 색은 정점
데이터라 그대로 틴트로 넘기면 되므로 데이터 구동이 유지된다. 필요한 모양은 이미 만든
`decal_blob` / `decal_streak` 로 상당수 커버되고, 없는 것(호·부채꼴)만 추가하면 된다.

`verify_batching.gd` 의 `HOT_DRAW` 목록에 이 파일들을 넣으면 회귀도 막힌다 —
지금은 `Ground.gd` · `PropField.gd` 만 검사한다.

---

## 5-I. HUD — 아이콘은 아틀라스로 옮겼으나, 비용은 다른 데 있었다

5-G 에서 HUD 를 "남은 가장 큰 덩어리(-61)" 로 지목하고 원인을 **아틀라스 밖 아이콘**으로
추정했다. 옮겨 놓고 재니 **HUD 격리 드로우 콜이 40 그대로**였다. 추정이 틀렸다.

화면에 실제로 보이는 것만 훑어 보니:

| | 개수 |
|---|---|
| **Label(텍스트)** | **10** |
| **Panel / PanelContainer**(나인패치 StyleBox) | **8** |
| TextureRect | 7 (그중 4장은 이미 UI 아틀라스) |
| ColorRect · Button | 3 |

즉 HUD 의 40 은 **텍스트 렌더와 나인패치 StyleBox** 가 만드는 구조적 비용이다. 텍스트는
폰트 아틀라스라 UI 시트와 섞이지 않고, 나인패치 12장은 무손실 고정이라 설계상 아틀라스에서
뺀 것들이다(ASSET_PIPELINE.md 3절). 게다가 `Panel → Label → Panel` 로 번갈아 나와 같은
폰트끼리도 안 묶인다.

### 그래도 아이콘은 옮겼다

`ui_coin` · `ui_heart_full/empty` · `ui_joystick_base/knob` 5장을 `assets/ui/icons/` 로
옮겨 UI 아틀라스에 넣었다. 드로우 콜 이득은 측정 오차 안이지만 옮기는 것이 맞다.

- 파이프라인 문서의 분류 오류를 고친 것이다 — `assets/ui` 루트를 "한 번에 한 장만 뜨는 큰
  그림" 으로 규정해 두고 48×48 인게임 상시 아이콘을 거기 두고 있었다.
  **기준은 위치가 아니라 크기와 표시 빈도다.**
- 조이스틱은 인게임 내내 매 프레임 그려진다 — 아틀라스 밖에 둘 이유가 없다.

UI 시트 크기는 그대로다(1024×1024, 점유 61%). 외형도 동일하다(HUD 스크린샷 비교 — 코인·
하트·레벨 배지·무기 슬롯·조이스틱 전부 유지).

### 검토했으나 하지 않은 것

- **나인패치 아틀라스** — `frames/` `hud/` 12장을 무손실 아틀라스로 묶으면 8 안팎 줄어든다.
  하지만 `StyleBoxTexture` 로 **늘려서** 쓰기 때문에 region 경계에서 번짐이 생기기 쉽고,
  무손실로 고정한 것도 그 때문이다. 줄어드는 양에 비해 화질 위험이 크다 — 실기기에서
  나인패치 테두리를 확인할 수 있을 때 다시 볼 것.
- **Control 트리 재배치** — `Panel`/`Label` 을 같은 종류끼리 연속으로 그리면 배치가 접히지만
  레이아웃 회귀 위험이 크다. 지금 우선순위가 아니다.

---

## 5-J. 남은 이펙트·기믹 전수 변환 + 인스턴싱 검토

### 전수 변환 (75건)

원형 확산·화염·폭발 기믹을 포함해 프리미티브가 남아 있던 곳을 전부 `QuadDraw` 로 바꿨다.

`SteamVent`(11) · `ItemPickup`(15) · `WeaponPickup`(11) · `Boss`(10) · `ToxicPool`(4) ·
`FallingDebris`(4) · `BossShell`(4) · `CryoVent`(3) · `GroundHazard`(3) · `Drone`(3) ·
`EnemyBullet`(3) · `BurningCar`(2) · `LandMine`(1) · `WeatherSystem`(1)

`Flamethrower` · `Chainsaw` · `BoomerangProj` 의 폴리곤은 **`tex == null` 폴백**이라
`# batching-exempt` 로 표시했다 — 아틀라스에 그림이 있어 도달하지 않는다.

`verify_batching.gd` 의 `HOT_DRAW` 에 28개 파일을 전부 등록했다.

### 지금 남은 것 — 텍스트와 인스턴스별 색

무기 4종 + 좀비 320 기준, 3회 중앙값 **168**:

| 끈 것 | draw | Δ | 비중 | |
|---|---|---|---|---|
| **HUD** | 95 | **−73** | 43% | |
| **데미지 숫자(텍스트)** | 119 | **−49** | 29% | ← 5-L 에서 해결 |
| **좀비 피격 잔광 `modulate`** | 145 | **−23** | 14% | ← 5-K 에서 해결 |
| 테마 기믹 | 154 | −14 | 8% | |

**스프라이트·이펙트 쪽은 사실상 다 짜냈다.** 남은 것은 성격이 다르다.

### 인스턴싱(언리얼 ISM)은 여기서 답이 아니다

Godot 의 2D 대응물은 `MultiMeshInstance2D` 다(한 드로우 콜에 N 인스턴스, 인스턴스별
변환·색 지원). 하지만 **이 프로젝트에서 얻을 게 거의 없다.**

- **캔버스 배처가 이미 인스턴싱과 같은 일을 하고 있다.** 좀비 320마리가 화면 아이템
  118개 → 드로우 콜 **3개**다(5-I 실측). MultiMesh 로 바꿔도 3 → 1 이 전부다.
- **MultiMesh 는 Y 정렬을 못 한다.** 모든 인스턴스가 그리기 순서상 한 지점에 놓인다.
  이 게임은 `Main` 이 `y_sort` 라 좀비·플레이어·픽업이 Y 순서로 섞여야 한다 —
  MultiMesh 로 묶는 순간 그 깊이감이 사라진다.
- 남은 큰 항목(텍스트 49 + HUD 73)은 **MultiMesh 로 그릴 수 없다.**

**딱 한 곳에서 인스턴싱의 장점이 유효하다** — 인스턴스별 색이다. 좀비 피격 잔광이
`body.modulate` 를 쓰는데, `modulate` 는 아이템 상태라 다르면 배치가 끊긴다(실측 -23).
MultiMesh 는 인스턴스 색을 공짜로 주지만 Y 정렬을 잃는다.

**그런데 같은 이득을 Y 정렬을 지키면서 얻을 수 있다** — 바닥·무기에서 쓴 것과 같은 방법이다.
좀비 몸통을 `Sprite2D` 대신 루트의 `_draw()` 에서 `draw_texture_rect(tex, rect, false, 색)`
으로 그리면 **색이 아이템 상태가 아니라 정점 데이터가 되어 배치가 안 끊긴다.**
Y 정렬은 좀비 루트가 그대로 담당한다. 노드도 3개 → 1개로 줄어 B7 과도 겹친다.

다만 `_animate_walk` 가 `body.scale/position/rotation/frame` 을 직접 만지므로 그 변환을
`_draw` 안에서 다시 구현해야 한다 — **게임에서 가장 뜨거운 오브젝트라 신중히 해야 한다.**
기대 이득은 약 23(14%)이다.

---

## 5-K. 좀비를 `_draw()` 로 — 인스턴싱이 노리던 이득을 Y 정렬을 지키면서

5-J 에서 "인스턴싱의 장점이 유효한 곳은 인스턴스별 색 하나뿐" 이라고 정리했다. MultiMesh 는
그걸 공짜로 주지만 Y 정렬을 잃는다. **같은 이득을 Y 정렬을 지키면서 얻는 방법**을 적용했다.

좀비 몸통을 `Sprite2D` 자식 대신 루트의 `_draw()` 에서 그린다:

```gdscript
draw_texture_rect(_d_tex, Rect2(-ts * 0.5, ts), false, _d_col)   # 색이 정점 데이터
```

`modulate` 는 **아이템 상태**라 다르면 배치가 끊기지만, `draw_texture_rect` 의 색 인자는
**정점에 실려** 배치를 안 끊는다. Y 정렬은 좀비 루트가 그대로 담당한다.

### 결과 (좀비 320 + 무기 4종, 동일 시드 4회 중앙값)

| | 전 | 후 |
|---|---|---|
| 드로우 콜 | 166 | **152** |
| 노드 수 | ~1,500 | **~630** |
| `proc_ms` | 36.3 | 34.7 ~ 37.6 (편차 큼) |

**CPU 가 나빠질 것을 걱정했는데 실측은 반대였다.** `_draw` 는 매 프레임 커맨드 목록을 다시
만들어야 하지만(Sprite2D 는 노드 변환만 갱신하면 된다), 노드가 3개 → 1개로 줄어 변환 전파가
사라진 것이 그 비용을 상쇄하고도 남았다. 노드 수 감소는 B7(y_sort 정렬 부하)과도 겹친다.

외형은 동일하다 — 스프라이트·피격 잔광·그림자·좌우 플립 모두 유지(스크린샷 비교).

### 주의할 점 두 가지

1. **`_d_*` 를 바꾸면 `queue_redraw()` 를 반드시 부른다.** `Sprite2D` 와 달리 자동 갱신이
   없다. 8곳에 넣었다(걷기 2·잔광 2·도화선·그림자·스폰 2).
2. **`Zombie.tscn` 에 `Sprite2D` 를 다시 붙이지 말 것.** 붙이는 순간 `modulate` 로 배치를
   끊는 옛 구조로 돌아간다. `verify_batching.gd` 가 "스프라이트 0개" 를 정상으로 보고
   넘기도록 해 두었고, 프리미티브 검사는 `HOT_DRAW` 의 `Zombie.gd` 가 맡는다.

### 겪은 사고 — `check_gdscript.py` 는 파싱 오류를 다 못 잡는다

주석 뒷부분이 코드 줄로 떨어져 나간 편집 실수가 있었는데(`queue_redraw() — 이후 ...`),
`check_gdscript.py` 는 **문제 0건으로 통과시켰다.** 잡은 것은 Godot 임포트
(`--headless --editor --quit`)의 `Parse Error` 였다. 커밋 전 검증에서 임포트를 건너뛰면 안 된다.

---

## 5-L. 데미지 숫자를 비트맵 자릿수로 — 텍스트 49 → 0

5-J 에서 남은 것을 "HUD 73(43%) + 데미지 숫자 49(29%)" 로 정리했다. 둘 다 텍스트라
지금까지 쓴 방법(아틀라스 + 텍스처 쿼드)이 그대로 안 먹는 줄 알았는데, **숫자는 글자
종류가 10개뿐이라 구울 수 있다.**

### 왜 49나 나왔나

폰트 글리프 아틀라스는 게임플레이 시트와 **별개 텍스처**다. `draw_string` 이 그 텍스처를
물고 오는 순간 배치가 끊기고, `draw_string_outline` 이 한 번 더 끊는다.
노드 하나당 2회 × 동시 활성 상한 36개 = 실측 49.

### 고친 방법

`tools/gen_damage_digits.gd` 가 0~9 를 PNG 로 굽고 `build_atlas.py` 가 게임플레이
시트에 넣는다. `DamageNumber._draw()` 는 자릿수 쿼드를 늘어놓는다.

```gdscript
draw_texture_rect(_DIGIT_TEX[_digits[i]], Rect2(Vector2(x, 0.0) + qoff, qsz), false, col)
```

설계에서 중요한 것 네 가지:

1. **검은 아웃라인 + 흰 글리프를 한 장에 굽는다.** 색을 틴트로 곱하면 아웃라인은
   `0 × c = 0` 이라 검정 그대로고 글리프만 `1 × c = c` 로 색이 된다 —
   텍스처 한 장이 원본의 두 번 드로우를 대신한다.
2. **글리프는 Godot 이 직접 렌더한다.** PIL 로 다시 그리면 모양이 달라진다.
   그래서 생성기가 `SubViewport` 에 `draw_string_outline` + `draw_string` 을 그려 캡처한다.
3. **10장을 공통 사각형으로 자른다.** 장마다 따로 자르면 세로 위치가 어긋나 숫자열이
   출렁인다. 덤으로 폰트 전체 높이(55×98)의 빈칸이 사라져 아틀라스 면적이 1/4 이 됐다.
4. **전진폭은 표시 크기(24pt)에서 직접 잰다.** 굽는 크기(72pt)에서 재고 3으로 나눴더니
   힌팅 반올림 때문에 13.33 이 나왔다 — 원본이 쓰는 값은 13.0 이다. 이 0.33 이
   자릿수마다 누적돼 네 자리에서 1.3px 어긋난다.

크기 2px 양자화도 없앴다. 그건 "팝 중 매 프레임 다른 폰트 크기를 쓰면 크기별 글리프
아틀라스가 새로 생긴다" 는 이유로 넣은 것이라, 텍스처 한 장을 배율만 바꿔 그리는 지금은
비용이 없다. 팝이 연속 배율로 부드러워졌다.

### 완전 재현은 아니다 — 의도된 근사

원본 아웃라인은 **폰트 크기와 무관하게 5px 고정**인데(코드에 상수 5), 텍스처는 글리프와
같이 확대된다. 그래서 기본 크기(24pt)에서 정확히 5px 가 되도록 굽고(72pt 에 아웃라인 15),
크리티컬 팝(최대 49pt)에서는 아웃라인도 두꺼워지는 것을 받아들였다.
원본과 나란히 렌더해 비교했고, 어긋나는 것은 이 굵기뿐이다 — 가독성은 오히려 낫다.
아웃라인 알파도 0.9 → 1.0 이 됐다(원본은 `a * 0.9`). 검정 위 검정이라 눈에 띄지 않는다.

### 겪은 함정 두 가지

1. **`--headless` 로는 못 굽는다.** 더미 렌더러라 `SubViewport.get_texture().get_image()`
   가 null 이다. 처음에 조용히 실패하는 대신 멈춰 버려서 3분을 기다렸다. 지금은 생성기가
   그 자리에서 사유를 찍고 종료한다. 실제 GL 컨텍스트(xvfb + llvmpipe)가 필요하다.
2. **`PackedByteArray` 는 값 타입이다.** 풀에서 꺼낸 노드에 `d._digits.append(...)` 로
   채우면 **사본이 바뀌고 원본은 빈 배열로 남는다**(동적 프로퍼티 접근이 Variant 사본을
   돌려준다). 지역 변수에 만들어 통째로 대입해야 한다. 조용히 숫자가 안 보이는 종류라
   실제 게임 화면 스크린샷으로 확인했다.

### 회귀 방지

`verify_batching.gd` 에 문자열 드로우 검사를 넣었다. 프리미티브 검사와 달리 **루프 밖에
있어도** 잡는다 — `HOT_DRAW` 는 "인스턴스가 수십 개 뜨는 스크립트" 목록이라 배수가
루프가 아니라 인스턴스 수이기 때문이다. 데미지 숫자가 정확히 그 형태였다(루프 없음, 49콜).

임의 로케일 문자열을 그리는 `ItemPickup`·`WeaponPickup` 은 구울 수 없어
`# batching-exempt:` 로 사유를 남겼다(동시 표시가 한 자릿수라 손실이 그만큼뿐이다).
검사는 일부러 틀린 코드를 넣어 실패하는 것을 확인했다.

### 결과 (좀비 320 + 무기 4종, 시드 3개 중앙값)

| | 전 | 후 |
|---|---|---|
| 기준선 드로우 콜 | 168 | **126** |
| `off=dmgnum` 과의 차 | −49 | **0** (시드별 +2 / −8 / −4 = 노이즈) |
| 게임플레이 시트 | 1024×1024 · 55장 | 1024×1024 · 62장 (+21KB, **VRAM 동일**) |

`off=dmgnum` 을 꺼도 드로우 콜이 안 줄어든다 = **데미지 숫자가 더는 배치를 끊지 않는다.**
5-E·5-F 에서 좀비·바닥이 그랬던 것과 같은 상태다(이미 뺄 게 없어 ablation 이 오히려 늘기도 한다).

시트 한 변이 그대로라 VRAM 은 안 늘었다. 공통 사각형 자르기로 자릿수 한 장이
55×98 → 47×65 가 된 것이 여기서 값을 했다.

---

## 6. 남은 작업 우선순위

1. **HUD (남은 가장 큰 덩어리)** — 5-I 에서 아이콘은 아틀라스로 옮겼지만 비용의 본체는
   텍스트 라벨 10개 + 나인패치 `StyleBoxTexture` 패널 8개라는 것을 확인했다. 숫자 라벨은
   5-L 의 비트맵 자릿수를 그대로 쓸 수 있지만 HUD 는 `Control` 트리라 `_draw` 로 내리는
   구조 변경이 필요하고, 나인패치는 무손실 규약(늘어나는 테두리)과 부딪힌다 — 8콜 이득에
   테두리 번짐 위험이라 보류 중이다(5-I 하단)
2. **실기기 확인** — 5-E~5-L 로 드로우 콜을 4종 빌드 기준 307 → 126 까지 줄였지만
   전부 소프트웨어 렌더러(llvmpipe) 기준 명령 수다. 게임 안 PERF HUD 로 실제 프레임을
   보는 것이 다음 단계다. 궁극기·기믹·아레나 비주얼도 눈으로 볼 것(5-H 하단)
3. **전체화면 알파 오버레이 비용 측정** — vignette·fog_vision. HUD 격리만 frame_ms 66.7 로
   튄다. 실기기에서 재고 필요하면 해상도를 낮추거나 셰이더로 대체한다
4. **B7 (y_sort 분리)** — 자식 500~800개 정렬 축소. 다만 컨테이너를 나누면 그리기 순서가
   달라질 수 있어 **눈으로 확인하며** 진행해야 한다
5. **VRAM 압축 검토** — 5-B·5-C 로 인게임 상주 VRAM 을 32MB → 8MB 까지 낮췄지만 여전히
   RGBA8 전개다(Phase A 하단 참조). `compress/mode=2`(ETC2/ASTC)면 1/4 이 되지만
   알파 경계·선명한 아이콘의 블록 아티팩트를 실기기에서 봐야 한다
6. **A5 (PWA)** — 아이콘 준비 후. 재방문 로딩이 크게 줄어든다
7. **B5 잔여 무기 5종** · **Phase C 잔여 3건** — 전부 저빈도라 효과는 작다
8. **B12 물리 틱** — 실기기 측정 후 판단
9. **과대 해상도 5장 · 타일 지연 로드** — 5-C 하단 참조. 둘 다 효과가 작다

## 7. 리뷰 중 발견한 별건 — 일본어 폰트 커버리지 버그 (미수정)

폰트 서브셋 작업 중 발견했다. **최적화와 무관한 기존 버그다.**

일본어 UI 문자열에 쓰이는 한자 12자가 **원래 폰트에도 없어** 일본어로 플레이하면 두부(□)로
보인다: `体 先 入 分 別 延 数 末 端 見 購 実`. 영향받는 문자열(`scripts/Locale.gd`):

| 키 | 일본어 문자열 | 노출 위치 |
|---|---|---|
| `title_tagline` | アウトブレイクを生き**延**びろ | 타이틀 화면 |
| `hud_kills_fmt` | %d **体** | HUD 처치 수 |
| `run_cleared` | 30**分** 生存 | 클리어 메시지 |
| `hs_by_mode` | モード**別**ハイスコア（この**端末**） | 메뉴 하이스코어 |
| `rank_view` | ランキングを**見**る | 랭킹 버튼 |
| `upg_lightning_count_name` | 稲妻の**数** | 업그레이드 이름 |
| `upg_requires_orbs` / `_lightning` | …を**先**に**購入** | 업그레이드 안내 |

이미 같은 문제를 한 번 우회한 흔적이 있다 — `scripts/HUD.gd:1129` 주석
("`❚❚` 글리프는 서브셋 폰트에 없어 깨지므로 텍스트 대신 막대 2개를 직접 그린다").

고치는 방법은 두 가지다:
1. **원본 Noto Sans CJK를 받아 다시 서브셋** — 근본 해결. `tools/subset_fonts.py`가 그대로 쓰인다.
   (이 작업 환경에서는 네트워크가 막혀 폰트 원본을 받을 수 없어 진행하지 못했다)
2. **해당 일본어 문자열을 커버되는 글자로 바꾸기** — 예: `%d 体` → `%d キル`(가타카나는 전부 커버됨).
   번역 내용을 바꾸는 결정이라 임의로 하지 않았다.

어느 쪽이든 `tools/font_known_absent.txt`에서 해당 글자를 지우면 CI 검사가 재발을 막아 준다.

### 실기기에서 확인할 것 (헤드리스로 검증 못한 항목)
- **텍스처 손실 압축 결과** — 평균 오차는 0.4%로 낮지만 아이콘·스프라이트를 눈으로 볼 것
  (나인패치 UI 프레임은 무손실로 제외해 뒀다)
- 바닥/프롭 스냅(128px)으로 화면 가장자리에 빈 칸이 보이지 않는지
- 데미지 숫자 상한(동시 36·프레임당 8)이 난전에서 너무 빈약해 보이지 않는지
- 관통 무기(석궁 등) 체감 — 버그 수정으로 설계대로 강해졌으므로 밸런스 재확인
- 보스 피격 잔광이 이전과 같아 보이는지
