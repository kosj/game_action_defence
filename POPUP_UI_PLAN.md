# 팝업 UI · 리스트 행 완성도 개선 계획

> 대상: `MainMenu.gd` 의 팝업 8종(옵션·강화·캐릭터·도전과제·퀘스트·보상함·아레나·랭킹),
> `LevelUpPanel`, 전역 테마(`UITheme.gd`).
> (`ShopPanel` 도 대상이었으나 인게임 상점은 2026-08 폐기 — HANDOFF P0-3)
> 문제 요약: 팝업마다 **크기·여백·제목·닫기 버튼이 제각각**이고, 리스트 행은 **텍스트만
> 쌓여 있어** 완성 전 화면처럼 보인다. 진행 게이지는 아예 엔진 기본값이다.

---

## 1. 진단 (실측)

### 1-1. 진행 게이지가 테마 없이 엔진 기본값
`_refresh_quests()` 는 `ProgressBar.new()` 를 그대로 쓴다. **`UITheme` 에 `ProgressBar`
스타일이 없어** Godot 기본 테마가 그려진다 — 밋밋한 회색 막대.

- HUD 에는 이미 건메탈+골드 게이지 에셋이 있다: `assets/ui/hud/hud_gauge_frame.png`,
  `hud_gauge_fill.png`. **만들어 둔 자산을 팝업이 안 쓰고 있다.**
- 진행도 0% 일 때(예: `Wave Rider I (0/8)`) 채움이 폭 0이라 **행이 비어 보인다** —
  트랙(배경)이 시각적으로 드러나지 않아 "바가 없는 항목"처럼 읽힌다.

### 1-2. 팝업 크기가 제각각
| 팝업 | 형태 |
|---|---|
| 강화 · 캐릭터 · 아레나 | 전체 화면 (여백 12/30) |
| 도전과제 | 고정 490×600 |
| 보상함 | 고정 490×560 |
| **퀘스트** | **고정 490×520** |
| 옵션 | 고정 420×480 |
| 랭킹 | 고정 420×460 |

720×1280 화면에서 퀘스트 팝업은 **폭 68% · 높이 41%** 만 쓴다. 프레임 테두리(마진 40)가
두꺼워 실제 콘텐츠 영역은 더 좁아지고, 그래서 항목이 눌려 보인다.
앞서 아레나·캐릭터는 전체 화면으로 고쳤는데 **나머지 5개가 옛 방식에 남아 있다.**

### 1-3. 팝업 조립 코드가 8번 복붙돼 있다
| 반복 요소 | 횟수 |
|---|---|
| dim `ColorRect` | 8 |
| `PanelContainer` | 8 |
| `MarginContainer` | 9 |
| `margin_*` 상수 지정 | 18 |
| `HSeparator` | 10 |
| `close.text = "Close"` (하드코딩) | 6 |
| Close 버튼 스타일 지정 | 6 |

**새 팝업을 만들 때마다 톤이 어긋날 수밖에 없는 구조**다. 지금 크기가 제각각인 것도 결과다.

### 1-4. 리스트 행에 구조가 없다
퀘스트 행은 `VBox[제목+보상 / 설명+진행수 / ProgressBar]` 텍스트 뭉치다.
- **아이콘이 없다** — `assets/ui/hud/hud_slot_small.png`(슬롯 프레임)과 `UIIcon` 이 있는데도 미사용
- **행 구분이 없다** — 카드/구분선 없이 텍스트가 이어져 어디까지가 한 항목인지 흐리다
- **완료 상태 표현이 없다** — 진행 중/완료/수령 대기가 모두 같은 모양
- 방금 개선한 **아레나 카드**(이름·설명·와이드 썸네일)와 문법이 다르다

### 1-5. 언어 혼용
| 위치 | 내용 |
|---|---|
| 팝업 제목 | `QUESTS` · `REWARDS` · `ACHIEVEMENTS` 영어 하드코딩 |
| 힌트 문구 | 한국어 하드코딩 (`완료 보상은 REWARDS 보관함에…`) — 영어/일본어에서도 한국어가 나온다 |
| 퀘스트 이름·설명 | `Zombie Hunter X` · `Kill 10878 zombies` (`QuestManager` 생성 문자열) |
| 닫기 버튼 | `"Close"` 하드코딩 6곳 (`Locale.t("menu_close")` 가 이미 있는데도) |

---

## 2. 방향

1. **팝업은 한 껍데기에서 나온다** — 크기·여백·제목·닫기를 공통 셸이 책임진다.
2. **리스트 행은 한 컴포넌트에서 나온다** — 아이콘 슬롯 + 제목/보상 + 설명 + 게이지 + 상태.
3. **이미 만든 자산을 쓴다** — 게이지는 HUD 에셋, 슬롯은 `hud_slot_small`, 아이콘은 `UIIcon`.
   신규 아트 없이 완성도를 올린다.

---

## 3. 단계별 작업

### Phase 1 — 게이지·행 구조 (가장 눈에 띄는 부분)

| # | 작업 | 파일 | 내용 |
|---|---|---|---|
| 1-1 | `ProgressBar` 전역 테마 | `UITheme.gd` | `background` = `hud_gauge_frame.png` 나인패치(마진 7), `fill` = `hud_gauge_fill.png`(마진 4) + 틴트. 게임 내 모든 진행바가 한 번에 HUD 와 같은 문법이 된다 |
| 1-2 | 0% 트랙 가시성 | `UITheme.gd` | 프레임(트랙)이 항상 보이므로 0% 행이 비어 보이던 문제 해소. 채움 최소 폭 보정도 함께 |
| 1-3 | 공통 리스트 행 컴포넌트 | `scripts/UIListRow.gd` (신규) | `UIListRow.make({icon, icon_color, title, reward, desc, cur, goal, state})` → 카드 1장 반환. 좌측 44px 슬롯(`hud_slot_small`) + 우측 [제목 ⋯ 보상] / [설명] / [게이지] |
| 1-4 | 퀘스트·도전과제·보상함 적용 | `MainMenu.gd` | 세 패널의 행 생성을 `UIListRow` 로 교체. 보상은 코인 아이콘 + 숫자로 |
| 1-5 | 상태 표현 | `UIListRow.gd` | 진행 중(기본) / 완료-수령대기(금색 테두리 + 체크) / 수령 완료(딤). 아레나 카드의 선택/잠금 문법과 맞춘다 |

### Phase 2 — 공통 팝업 셸

| # | 작업 | 파일 | 내용 |
|---|---|---|---|
| 2-1 | 셸 컴포넌트 | `scripts/UIPopup.gd` (신규) | `UIPopup.make(parent, title_key, accent, opts)` → `{dim, panel, body, close}`. dim·프레임·여백·제목·구분선·스크롤·닫기 버튼을 한곳에서 생성 |
| 2-2 | 크기 통일 | `UIPopup.gd` | 전 팝업을 **전체 화면(여백 12/30)** 으로. 작은 내용은 `body` 를 세로 중앙 정렬해 허전하지 않게 |
| 2-3 | 5개 팝업 이관 | `MainMenu.gd` | 퀘스트·도전과제·보상함·옵션·랭킹을 셸로 이관. 기존 전체화면 3종(캐릭터·아레나·강화)도 셸로 통일 |
| 2-4 | 중복 제거 | `MainMenu.gd` | dim 8·PanelContainer 8·MarginContainer 9·HSeparator 10·Close 6 → 셸 1곳으로 수렴 |

> 이관은 **한 패널씩** 진행한다(한 번에 8개를 바꾸면 회귀 지점을 못 찾는다).

### Phase 3 — 언어 정리

| # | 작업 | 내용 |
|---|---|---|
| 3-1 | 팝업 제목 로케일화 | `popup_quests` · `popup_rewards` · `popup_achievements` · `popup_arena` · `popup_character` · `popup_power` (en/ko/ja) |
| 3-2 | 힌트 문구 로케일화 | 한국어 하드코딩 2곳 → `quest_hint` · `rewards_hint` |
| 3-3 | 닫기 버튼 | 하드코딩 `"Close"` 6곳 → 셸이 `Locale.t("menu_close")` 로 일괄 처리 |
| 3-4 | 퀘스트 문구 | `QuestManager` 의 이름·설명을 키+수치 조합으로 바꿔 번역 가능하게 (`quest_kill_fmt` 등) |
| 3-5 | 글리프 검증 | 신규 일본어 문자열을 서브셋 폰트로 검증(実/績/強 처럼 빠진 한자 회피) |

### Phase 4 — 폴리시 (선택)

1. 완료된 퀘스트 행에 **금색 글로우 펄스** — 수령 유도
2. 팝업 열림 시 스케일 팝(0.94→1.0) + dim 페이드
3. 게이지 값 변화 시 트윈(현재는 즉시 대입)
4. 리스트가 비었을 때 안내 문구(현재는 빈 화면)

---

## 4. 변경 파일 요약

- `scripts/UITheme.gd` — `ProgressBar` 전역 스타일
- `scripts/UIListRow.gd` (신규) — 공통 리스트 행 카드
- `scripts/UIPopup.gd` (신규) — 공통 팝업 셸
- `scripts/MainMenu.gd` — 8개 팝업 이관, 행 생성 교체
- `scripts/Locale.gd` — 제목·힌트·퀘스트 문구 키
- `scripts/QuestManager.gd` — 문구를 키+수치로

## 5. 검증

1. 단계마다 720×1280 목업 렌더링으로 검수(엔진 없이 확인 가능)
2. 체크리스트: 진행도 0%·50%·100% 세 상태에서 게이지가 모두 보이는가 /
   팝업 8종의 여백·제목·닫기가 동일한가 / 항목이 1개·많을 때 모두 자연스러운가 /
   en·ko·ja 에서 잘림이 없는가 / 완료·수령 상태가 구분되는가
3. 회귀: 레벨업 패널의 진행바·버튼이 깨지지 않는지 확인(전역 테마 변경 영향)

## 6. 권고 순서

**Phase 1 → 리뷰 → Phase 2 → Phase 3** 순.
Phase 1 만으로 "완성도가 떨어져 보이는" 원인의 대부분(게이지·행 구조)이 해소된다.
Phase 2 는 그 완성도가 **앞으로도 유지되게** 만드는 구조 작업이고,
Phase 3 은 언어 혼용을 정리한다. Phase 4 는 결과를 보고 판단.
