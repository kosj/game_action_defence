# 메인 메뉴 버튼 개선 계획 — 아트 통일성

> 대상: `scenes/MainMenu.tscn` · `scripts/MainMenu.gd` (버튼 스택) · `scripts/UIStyle.gd` (플레이트 아트)
> 문제 요약: 메뉴 버튼이 **광택 플라스틱 웹 버튼**으로 보여 로고(코믹 호러)·인게임(셀셰이딩
> 아포칼립스)·HUD(건메탈+골드) 어느 쪽과도 붙지 않는다.

---

## 1. 진단

### 1-1. 지금 게임 안에 시각 언어가 셋이다
| 영역 | 현재 톤 | 근거 |
|---|---|---|
| 로고 | 코믹 호러 — 크림 균열 글자 + 핏빛 외곽선·흘러내림 | `logo_title.png` (크림 `0.93/0.90/0.82`, 핏빛 `0.70/0.12/0.13`) |
| HUD·패널·프레임 | 다크 건메탈 + 골드 헤어라인 (산업/군용) | `frames/*.png`, `assets/ui/hud/*` |
| **메뉴 버튼** | **광택 플라스틱 (사탕색)** | 아래 1-2 |

앞선 작업으로 HUD·패널은 프레임 아트와 통일됐는데, **메뉴 버튼만 예전 톤에 남아 있다.**

### 1-2. "플라스틱"으로 보이는 기계적 원인
`UIStyle.button_box()` 는 `frames/button_plate.png` 를 `modulate_color` 로 틴트한다.
그런데 그 원본이 **151×93 크기의 거의 흰색 광택 플레이트**다(세로 밝기 255 → 203).

> 밝은 광택면 × 채도 높은 색 = 사탕/플라스틱. 좀비물 금속이 되려면 **어두운 금속면 + 낮은 채도**여야 한다.

또한 151×93 원본을 300×70 으로 늘리면서 광택 띠까지 함께 늘어나 "번들거림"이 강조된다.

### 1-3. 색이 정보를 전달하지 않는다
8개 버튼에 8색을 쓰는데, 그중 셋은 사실상 같은 금색이다(틴트 후 실측값):

| 버튼 | 틴트 결과 | 비고 |
|---|---|---|
| 도전과제 | `(140, 118, 51)` | ← |
| 보상함 | `(147, 125, 51)` | ← **육안 구분 불가** |
| 랭킹 | `(147, 121, 51)` | ← |
| 새 게임 | `(59, 125, 66)` 초록 | |
| 이어하기 | `(59, 88, 140)` 파랑 | |
| 퀘스트 | `(81, 140, 81)` 초록 | 새 게임과 유사 |
| 강화 | `(106, 73, 147)` 보라 | |
| 옵션 | `(81, 85, 103)` 회색 | |

색을 다 썼는데 **의미 구분은 오히려 안 된다**. 가장 중요한 "새 게임"이 색으로 튀지도 않는다.

### 1-4. 로고와 버튼이 끊겨 있다
로고의 **핏빛/크림**을 쓰는 버튼이 하나도 없다. 브랜드 색이 "행동(Play)"으로 이어지지 않는다.

### 1-5. 언어 혼용 (기능 결함)
`Achievements` · `Quests` · `PowerUp` · `Rewards` 가 **하드코딩 영어**다 — Locale 키 자체가 없다.
반면 `새 게임` · `이어하기` · `랭킹` · `옵션` 은 번역된다. HUD 에서 고쳤던 것과 같은 문제.

| 위치 | 코드 |
|---|---|
| `MainMenu.gd:179` | `_ach_btn.text = "Achievements"` |
| `MainMenu.gd:187` | `_quest_btn.text = "Quests"` |
| `MainMenu.gd:211` | `_power_btn.text = "PowerUp"` |
| `MainMenu.gd:928` | `_rewards_btn.text = "Rewards (%d)"` |

### 1-6. 균일한 8단 스택 — 위계도 리듬도 없다
전부 폭 300, 높이 70/70/56×6. 세로를 거의 다 먹으면서 **어디를 눌러야 하는지** 안 보인다.
아이콘도 없어 글자를 읽어야만 구분된다(게임엔 아이콘 자산이 풍부한데도).

---

## 2. 방향 — 시각 언어를 셋에서 **둘**로

| 역할 | 담당 | 적용처 |
|---|---|---|
| **구조물(크롬)** | 다크 건메탈 + 골드 헤어라인 | 모든 버튼 플레이트, 패널, HUD |
| **브랜드/행동** | 핏빛 + 크림 (로고에서 채취) | 주요 CTA 1개, 강조 텍스트 |

핵심 원칙 세 가지:
1. **플레이트는 전부 같은 금속** — 색으로 구분하지 않는다.
2. **색은 의미가 있을 때만** — 붉은색 = 시작, 골드 = 보상 계열, 회색 = 설정.
3. **위계는 크기·명도로** — 색이 아니라 크기와 밝기로 1·2·3순위를 만든다.

---

## 3. 단계별 작업

### Phase 1 — 위계·언어 정리 (코드만, 에셋 불필요)

| # | 작업 | 파일 | 내용 |
|---|---|---|---|
| 1-1 | 3단 위계 도입 | `MainMenu.gd` | **1차** 새 게임 `320×78`·핏빛 플레이트·크림 텍스트 26px / **2차** 이어하기 `300×66`·건메탈+골드 / **3차** 나머지 6개 `300×52`·어두운 건메탈·딤 텍스트 19px |
| 1-2 | 색 정리 | `MainMenu.gd`, `UITheme.gd` | 3차 버튼은 **모두 같은 건메탈**. 구분은 좌측 아이콘 + 강조점 색(골드=보상 계열, 보라=강화, 회색=옵션)으로만. `MENU_*` 상수로 통합 |
| 1-3 | 로케일 키 추가 | `Locale.gd`, `MainMenu.gd` | `menu_achievements` `menu_quests` `menu_rewards` `menu_rewards_n` `menu_powerup` (en/ko/ja). 서브셋 폰트 글리프 검증 포함 |
| 1-4 | 아이콘 배치 | `MainMenu.gd`, `UIIcon.gd` | 좌측 아이콘 슬롯: 도전과제=trophy, 퀘스트=flag, 보상=coin, 랭킹=star, 강화=bolt, 옵션=gear(신규 벡터). 기존 `UIIcon` 절차 드로잉 재사용 → 신규 에셋 0 |
| 1-5 | 보상 배지 개선 | `MainMenu.gd` | `Rewards (3)` 텍스트 대신 **붉은 원형 배지**를 버튼 우상단에 — 미수령이 눈에 띄게 |

### Phase 2 — 플레이트 아트 교체 (절차 생성, `tools/`)

HUD 에셋과 같은 방식(`tools/gen_hud_assets.py` 패턴)으로 생성:

| 파일 | 용도 | 사양 |
|---|---|---|
| `btn_plate_steel.png` | 기본 버튼 플레이트 | 256×96 나인패치(마진 14). 다크 건메탈, 상단 미세 하이라이트, 하단 음영, 얇은 골드 헤어라인, 모서리 리벳 2개, **가로 스크래치 그런지** |
| `btn_plate_blood.png` | 1차 CTA 전용 | 같은 형태에 핏빛(`0.70/0.12/0.13`) 금속 + 크림 하이라이트 |
| `btn_plate_dark.png` | 3차/비활성 | 채도를 더 낮춘 어두운 판 |

코드: `UIStyle.button_box()` 가 **틴트 대신 플레이트 종류를 고르도록** 변경
(`UIStyle.plate(kind)`), 기존 `apply_button_style(btn, bg, accent)` 시그니처는 유지해
다른 화면(상점·레벨업 등)이 깨지지 않게 한다.

### Phase 3 — 레이아웃 압축 (선택)

- 3차 버튼 6개를 **2열 × 3행 그리드**로 → 세로 약 200px 절약
- 확보한 공간으로 로고를 키우거나 태그라인(`title_tagline`, 이미 Locale 에 있음) 복원
- 세로가 짧은 기기에서 버전 라벨이 잘리던 위험도 함께 해소

### Phase 4 — 장르 감성 폴리시 (선택)

1. 버튼 눌림 시 **짧은 붉은 플래시** + 기존 스케일 팝 유지
2. 1차 CTA 에 아주 느린 **맥동 글로우**(인트로 비컨과 같은 문법)
3. 배경에 인트로에서 쓴 **잔불(ember)** 소량 재사용 → 타이틀·인트로·메뉴가 한 세계로
4. 로고 아래 얇은 **핏빛 구분선**으로 브랜드와 버튼 스택을 시각적으로 묶기

---

## 4. 변경 파일 요약

- `scripts/MainMenu.gd` — 위계·아이콘·배지·로케일 적용
- `scripts/UIStyle.gd` — `plate(kind)` 헬퍼, 플레이트 종류 분기
- `scripts/UITheme.gd` — `MENU_*` 색 상수
- `scripts/Locale.gd` — 메뉴 로케일 키 5종 (en/ko/ja)
- `scripts/UIIcon.gd` — `gear` 아이콘 추가
- `tools/gen_menu_plates.py`, `assets/ui/frames/btn_plate_*.png` — Phase 2

## 5. 검증

1. Phase 마다 720×1280 목업 렌더링으로 위계·대비 확인(엔진 없이도 검수 가능)
2. 체크리스트: 새 게임이 한눈에 1순위로 보이는가 / 3차 버튼 6개가 한 덩어리로 읽히는가 /
   ko·ja·en 세 언어에서 텍스트가 잘리지 않는가 / 이어하기 비활성 상태가 구분되는가 /
   보상 배지가 미수령 시에만 보이는가
3. 다른 화면(상점·레벨업·게임오버)의 버튼이 그대로인지 회귀 확인 — `apply_button_style` 시그니처 유지

## 6. 권고 순서

**Phase 1 → 목업 리뷰 → Phase 2** 가 핵심이다.
Phase 1 만으로 1-3(색 혼란)·1-5(언어)·1-6(위계)이 해소되고,
Phase 2 가 1-2(플라스틱 질감)를 해결한다. Phase 3·4 는 그 결과를 보고 판단.
