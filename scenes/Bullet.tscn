[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/Bullet.gd" id="1"]

; 탄 그림은 Bullet.gd 의 _draw() 가 전부 그린다(캐릭터·무기별 모양). 예전에는 여기에
; bullet.png(화살 모양) Sprite2D 가 있어 그 위에 항상 겹쳐 그려졌다 — 어떤 무기를 쏘든
; 화살이 날아가 보이던 원인. 스크립트가 참조하지 않는 노드였으므로 제거했다.
[node name="Bullet" type="Node2D"]
script = ExtResource("1")
