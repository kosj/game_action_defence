# 게임 환경 — 시간 경과 + 날씨 (구현 완료)

아레나에 "시간이 흐른다"는 감각과, 주기적으로 바뀌는 날씨를 넣는다.
전부 **런타임 절차 생성**(신규 아트 0장)이며 `gl_compatibility`/WebGL·모바일 예산 안에서 돈다.

구현: `scripts/DayNightCycle.gd`, `scripts/WeatherSystem.gd`, 검증 `tools/verify_environment.gd`.

---

## 0. 전제 (조사 결과)

| 항목 | 내용 |
|---|---|
| 시간 | `Events.elapsed_time` 이 난이도·보스·엘리트를 전부 구동. 세이브에 포함(이어하기 복원됨). `Cheats.time_skip` 도 이 값을 움직인다 |
| 난이도 시계 | `clear_seconds=1800`(30분 클리어), `boss_seconds=600`, `elite_seconds=300`, `tier_seconds=60` |
| 월드 렌더 | `Main`(Node2D, 캔버스 레이어 0) → Background(z=-3) / Ground(z=-2) / PropField(z=-1) / 유닛(0) / FX(1~3) / DamageNumber(60) |
| UI | HUD·LevelUpPanel·SceneFade 는 전부 **CanvasLayer** → 월드 틴트가 닿지 않는다 |

`CanvasModulate` 는 캔버스 레이어 0(=월드)만 곱연산으로 물들인다. 그래서 화면 전체 오버레이 없이
월드만 정확히 물들이고 HUD 는 원색을 유지할 수 있다(실촬 확인함).

---

## A. 시간 경과 — Day/Night Cycle

### 구동원
`Events.elapsed_time` 하나만 읽는다. 자체 타이머를 두지 않는 이유:
이어하기가 **공짜로** 맞고, `Cheats.time_skip` 이 자동으로 따라오며, 일시정지·히트스톱과 어긋날 여지가 없다.

### 주기 = 보스 주기(600s)에 정렬

`u = fposmod(elapsed, cycle) / cycle`, `cycle = GameData.difficulty.boss_seconds` (하드코딩 금지 규칙).

| u | 시각 | 틴트(곱연산) |
|---|---|---|
| 0.00 | 한밤 — **가장 어두움** | (0.67, 0.71, 0.95) — 초판 (0.62, 0.66, 0.92) 는 캐릭터 가독성 피드백으로 상향 |
| 0.07 | 여명 | (0.86, 0.72, 0.78) |
| 0.14 | 일출 | (1.00, 0.90, 0.82) |
| 0.26 | 아침 | (1.00, 0.99, 0.96) |
| 0.45 | 한낮 | (1.00, 1.00, 1.00) |
| 0.60 | 오후 | (1.00, 0.97, 0.90) |
| 0.72 | 해질녘 | (1.00, 0.80, 0.60) |
| 0.86 | 땅거미 | (0.80, 0.74, 0.92) |
| 1.00 | 한밤(= 0.00) | (0.67, 0.71, 0.95) |

키프레임 사이는 연속 lerp. `u=0` 과 `u=1` 이 **같은 색**이라 순환 이음매에서 화면이 튀지 않는다.

**설계 의도:** 가장 어두운 지점이 정확히 600/1200/1800초 = 보스 등장 시각에 떨어지고,
보스를 잡는 동안 해가 뜬다. "밤이 깊을수록 보스가 온다."

**주기가 보스 주기와 같으므로 t=0 도 같은 위상이다** — 게임은 한밤에 시작해 약 40초에 걸쳐
일출로 밝아진다. 초반 5초는 스폰 유예 구간이라 가독성 부담이 없고, 감염 47일차라는 인트로
서사와도 맞는다. (주기적 순환에서 "시작은 낮 + 보스는 밤"은 수학적으로 동시에 만족할 수 없다.)

### 가독성 가드
- 날씨 틴트까지 곱한 **최종** 색의 Rec.709 휘도를 **0.66 밑으로 내리지 않는다**(초판 0.62 에서 상향).
  한밤 "기준 틴트" 휘도(≥ 0.70)는 검증이 따로 잠근다 — 하한 보정은 날씨 합성 후에만 걸리기 때문.
  바닥은 이미 `Ground.TILE_DARKEN=0.55` 로 깔려 있어 그 아래로는 좀비 실루엣이 죽는다.
  하한에 걸리면 채널을 비례 확대한다(실제로 밤×비 조합에서 발동한다 — 검증 항목에 포함).
- **달빛 헤일로**: 반경 520px, 알파 0.28(초판 460/0.18 — 밤 캐릭터 가독성 피드백으로 상향), 푸른 달빛 색(0.72, 0.82, 1.00). 런타임 생성
  `GradientTexture2D`(radial) + 가산 블렌드 쿼드 1장.
  - 순백으로 더하면 잔디 위에서 초록만 증폭돼 스포트라이트처럼 뜬다(1차 실촬에서 확인 → 수정).
  - `PointLight2D` 를 쓰지 않는 이유: 2D 라이트는 조명 대상 CanvasItem 을 라이트마다 다시 그린다.
    좀비 상한 320마리에서 드로우콜이 배로 뛴다. 가산 쿼드 1장이면 같은 그림에 비용은 1.
  - CanvasModulate 가 헤일로에도 곱해지므로 그만큼 되밀어(`1/휘도`) 밤에도 세기가 유지된다.
- 밤에는 `Main.gd` 의 앰비언트 티끌을 반딧불 톤으로 옮긴다(노드 재사용, 신규 생성 없음).

### 비용
프레임당 Color 1회 쓰기(변화 < 0.002 면 생략) + 텍스처 쿼드 1장. 셰이더·추가 패스 없음.

### 치트 토글 (일시정지 > CHEATS > DAY/NIGHT)
`Cheats.daynight`(기본 ON). 끄면 **시간 틴트만** 한낮(무보정)으로 고정하고 달빛 헤일로·반딧불
앰비언트도 함께 끈다. **날씨 틴트와 가독성 하한은 그대로 살아 있다** — 치트가 끄는 것은 "시간"뿐이라
비·눈은 계속 보여야 한다. 밤 구간이 어두워 아트·스크린샷 확인이 어려울 때 쓴다.

- 게이트는 `_apply()` 한 곳에만 둔다. `tint_at()`/`composed_at()` 은 순수 함수로 남겨야
  검증 스크립트와 다른 소비자가 시간 곡선을 그대로 읽는다(검증 항목에 포함).
- 토글은 **일시정지 중에** 눌린다 — 그때는 `_process` 가 멈춰 있으므로 `Cheats.changed` 를 받아
  즉시 다시 칠한다. 그래서 패널을 닫기 전에 화면이 바뀐 것을 확인할 수 있다.

---

## B. 날씨 — Weather System

### 스케줄 (특정 주기 + 랜덤)
- **슬롯 75초.** 슬롯 경계에서만 날씨가 바뀐다.
- 페이드 인 5초 / 아웃 5초. 경계에서 세기가 정확히 0이라 교체 순간의 팝이 보이지 않는다.
- `clear`(맑음) 가중치 **34%**, 나머지 66%를 테마 날씨가 균등 분배.
- **동시 활성 1종**(하드 캡) — 이미터가 절대 2개 이상 생기지 않는다.

### 결정론 (이어하기 + 검증 가능성)
`Events.env_seed`(신규, `Events.reset()` 발급 · SaveManager 저장) + 슬롯 인덱스로만 결정된다.
시퀀스는 슬롯 0부터 순서대로 확정해 캐시한다 — 연속 반복을 제대로 막으려면 "이전 슬롯의 **최종**
결과"를 알아야 하기 때문이다(이전 슬롯의 기본 롤과 비교하면 그 슬롯이 재롤된 경우 비교 대상이
틀려 반복이 새어 나간다 — 초기 구현의 실제 버그, 검증에서 잡음). 30분 런의 슬롯은 24개뿐이라
처음부터 훑어도 비용이 없다. 캐시는 시드 **또는 테마 날씨 목록**이 바뀌면 버린다.

재롤은 직전 날씨를 뺀 나머지 중에서만 고른다 — 맑음을 섞으면 맑음 비율이 34%에서 밀린다
(이것도 초기 구현의 실제 버그: 실측 41.9%).

**날씨가 1종뿐인 테마(suburb·lab)는 재롤 자체를 건너뛴다.** 피할 다른 날씨가 없어 맑음으로
넘길 수밖에 없는데, 그러면 같은 이유로 맑음 비율이 무너진다 — 비가 두 슬롯 이어지는 편이 낫다.

### 종류 (전부 **연출 전용** — 전투 수치 무변경)

| 키 | 틴트(곱) | 화면 판 | 입자 |
|---|---|---|---|
| `rain` | (0.80, 0.84, 0.92) | 0 (+ 간헐 번개 섬광 0.5 + `lightning` sfx) | 수직 빗줄기 90 + 착지 파문 24 |
| `snow` | (0.92, 0.95, 1.00) | 0.06 | 눈 90 (7~18px) |
| `dust` | (1.00, 0.88, 0.70) | 0.12 · 황토빛(1.00, 0.80, 0.52) | 모래 알갱이 90 |

화면 판은 고정 색으로 한 번만 그리고 세기는 `self_modulate` 로 조절한다 — 매 프레임
`queue_redraw` 하지 않기 위해서다. `self_modulate` 는 자식(파티클)에 번지지 않는다.
번개 섬광은 날씨 색조와 무관하게 흰빛으로 당긴다.

### 테마별 테이블
`ThemeData.weather_keys`(신규, 기존 `gimmick_keys`/`prop_keys` 와 동일 패턴).

| 테마 | 날씨 |
|---|---|
| suburb | rain |
| city | rain, dust |
| lab | snow |

### 비: 낙하감 + 착지 파문
- 빗줄기는 **거의 수직**(4.6°)으로 떨어진다. 초기 구현의 16.7° 기울기는 플레이어가 가로로
  달릴 때 화면상 비가 **옆으로 흐르는 것처럼** 읽혔다. 대신 줄을 길고(50~86px) 빠르게
  (1150~1500 px/s) 해서 낙하감을 준다.
- **착지 파문**: 비일 때만 도는 두 번째 이미터(24입자, 수명 0.7s). 링 텍스처를 런타임 생성해
  작게 시작→퍼지며 사라진다. `z_index = -1`(절대)로 지면에 깔려 플레이어·좀비 발밑에 들어가고,
  `local_coords=false` 라 파문이 바닥에 못 박힌다 — 카메라를 따라 끌려다니면 즉시 가짜로 읽힌다.

### 치트 토글 (일시정지 > CHEATS > WEATHER)
`Cheats.weather`(기본 ON). 끄면 슬롯 세기를 0 으로 눌러 **입자·뿌연 판·날씨 틴트·번개·전환 배너**가
한꺼번에 사라진다(= 상시 맑음).

- **슬롯 스케줄과 `_key` 는 계속 굴러간다** — 결정론과 이어하기를 치트가 깨지 않는다. 다시 켜면
  그 시점에 원래 와야 할 날씨가 그대로 이어진다(검증 항목에 포함).
- 게이트는 `_process()` 의 `strength` 한 곳뿐이다. 세기 0 이 이미 "맑음"의 정의라, 이미터·파문·
  뿌연 판·틴트·번개가 전부 기존 경로로 자동으로 꺼진다.
- 번개 잔광은 세기 0 에서 **즉시** 지운다 — 끈 순간 섬광 잔상이 남으면 꺼진 것으로 읽히지 않는다.
- 낮/밤 토글과 마찬가지로 `Cheats.changed` 를 받아 일시정지 중에도 즉시 반영한다.

### 예산
날씨 이미터 1개 + 비 전용 파문 1개(비가 아니면 `emitting=false` 라 비용 0) ·
입자 ≤ 90 (+파문 24) · `local_coords=false`.
방출 사각형은 화면의 **1.24배**까지만 — 예전 2.25배 설정에서는 입자 대부분이
화면 밖에 있어 같은 개수로 체감 밀도가 절반이었다(1차 실촬에서 확인 → 수정).

### 피드백
날씨 전환 시 기존 `_show_toast` 를 재사용한 짧은 중앙 알림(Locale en/ko/ja).
**상시 위젯은 만들지 않는다** — `HUD_IMPROVEMENT_PLAN.md` P1 이 이미 "시간 표시 3중"을 문제로 잡아둠.

---

## C. 검증 결과

`tools/verify_environment.gd` — 헤드리스 SceneTree, 3600초 시뮬. **90항목 전부 통과, 실패 0건.**

```bash
godot --headless --path . --fixed-fps 60 --script res://tools/verify_environment.gd
```

시간: 틴트 연속성(0.05s당 채널 변화 < 0.01) · 순환 이음매 동일 색 · 최저 휘도 = 보스 등장 시각 ·
가독성 하한 준수 · **하한 보정이 실제로 발동함**(테스트가 무의미하지 않은지) · 최종 채널 ≤ 1.0
치트 토글: OFF 시 한밤에도 무보정 · OFF 시 헤일로 꺼짐 · **OFF 여도 날씨 틴트 유지** ·
ON 복귀 시 밤 틴트 복원 · 치트가 순수 함수(`tint_at`/`composed_at`)를 건드리지 않음
날씨: 테마 데이터 유효성 · 세기 봉투 · 시드 결정론(재호출/새 인스턴스/다른 시드) ·
테마별 맑음 비율 ~34% · 모든 날씨 등장 · 연속 반복 0(날씨 1종 테마는 반대로 "반복이 일어남"을 확인) ·
이미터 항상 1개 · 입자 예산 · **이어하기 재현** · **fog 정의/테마 키 완전 삭제**
날씨 치트 토글: OFF 시 이미터 정지·투명 / 파문 정지 / 뿌연 판 투명 / 날씨 틴트 무보정 / 전환 배너 없음 ·
**OFF 여도 슬롯 스케줄은 진행** · ON 복귀 시 같은 시점에 같은 날씨 + 배너 재개

그 밖에:
- `python tools/check_gdscript.py` — 문제 0건
- `--headless --quit-after 9000 res://scenes/Main.tscn`(150초) — 스크립트 에러 0건
- **실촬 확인**: 720×1280 윈도우 모드로 실제 Main 씬을 띄워 뷰포트 텍스처를 PNG 로 덤프
  (화면 캡처가 아니라 앱 자신의 프레임버퍼). 일출/한낮/해질녘/한밤 × 비·눈·모래바람·번개.
  이 실촬이 계산으로는 안 잡히는 버그 3건을 잡았다: 초록 스포트라이트로 뜨던 헤일로,
  40~80px 짜리 거대 눈·모래 입자, 화면 밖에 몰려 있던 입자 배치.

### 검증되지 않은 것
- 실제 모바일/WebGL 단말에서의 프레임 영향(오버드로·필레이트)은 측정하지 않았다. 데스크톱
  실촬과 입자·드로우콜 예산 상한으로만 통제했다.
- 좀비가 다수 몰린 실전 상황에서의 밤 가독성은 눈으로 확인하지 못했다(실촬은 초반 상태).

---

## C-2. 삭제 이력

- **안개(`fog`) 삭제.** 정의·테마 키·로케일 문자열·전용 입자 분기를 전부 걷어냈다.
  남은 날씨는 `rain` / `snow` / `dust` 3종. 뿌연 판(haze) 자체는 눈(0.06)·모래바람(0.12)이
  계속 쓰므로 유지한다. 삭제 후 테마 구성은 suburb=비, city=비·모래바람, lab=눈이며,
  **1종 테마가 생긴 탓에 위의 연속 반복 예외 규칙이 필요해졌다.**
  검증에 "fog 정의 삭제됨 / 정의에 없는 날씨 키를 쓰는 테마 없음"을 넣어, 데이터와 코드가
  따로 놀아 테마에만 남은 키가 조용히 맑음으로 떨어지는 일을 막는다.

## E. 프롭 배치 — 모티프(군집) 방식

균일 단독 배치(셀당 30% 독립 추첨)는 프롭끼리 아무 상관관계가 없어 "뿌려진" 것으로 읽혔다.
셀 추첨을 둘로 나눈다: **모티프 8%**(앞마당·검문소·서버 열 같은 3~4개 군집이 셀 하나를 쓴다)
+ **단독 14%**(기존 방식). 군집 셀이 여러 개를 그리므로 체감 밀도는 이전과 비슷하고,
빈 지역과 밀집 지역의 대비가 생겨 "배치된" 것으로 읽힌다.

규칙(전부 `verify_environment.gd` 가 실측 확인):
- 군집은 자기 셀 안에 들어온다(오프셋 ±95 + 군집 지터 ±20) — 충돌 판정이 3×3 셀 조회로 끝난다.
- **solid 간 통행 간격**: 중심 거리 ≥ r1+r2+2×PLAYER_R+8. solid 는 크기 지터 없이 고정 배율이라
  이 간격이 흔들리지 않는다 — 군집이 함정이 되지 않는다.
- 일렬 모티프는 군집 단위로 플립을 공유한다 — 정렬이 곧 "사람이 놓은 것"의 신호다.
  글자가 있는 팻말(forsale)은 반전 금지(거울 글씨 방지).
- 테마당 모티프 평균 solid = 1.0 으로 맞춰(장애물 0짜리 완충 모티프 포함) 테마 간 장애물
  밀도가 균일하다(실측: 장애물 비율 suburb 28.6% / city 31.4% / lab 31.6%, 전부 ≤ 1/3).
- 시작 지점 주변 3×3 셀은 장애물 금지 — 스폰 직후 장애물에 밀려나며 시작하지 않는다.

같이 고친 버그: `_draw` 의 좌우반전 프롭이 `draw_set_transform` 을 되돌리지 않아, 반전 프롭
**이후에** 그려지는 그림자·비반전 프롭이 전부 거울 좌표로 날아갔다. 스크롤로 그리기 범위가
바뀔 때마다(128px 스냅) "반전 뒤에 오는" 프롭 집합이 바뀌어 프롭이 깜박이는 것으로 보였다 —
반전 직후 변환을 즉시 복원해 해결.

## D. 하지 않은 것 (명시)

- **날씨의 게임플레이 수치 개입 없음** — 이동 감속·명중률 저하 등 미적용(사용자 결정). 후속 가능.
- ~~프롭 재활성화 없음~~ — #300 에서 15종 배치로 해제됐고, 이후 **모티프(군집) 배치**로 개편됐다(아래 E 참고).
- **신규 아트 없음** — 그라디언트·파티클 전부 런타임 생성.
- **HUD 상시 시계/날씨 위젯 없음** — 위 P1 사유.
- **눈의 앞/뒤 레이어 분리 없음** — 이미터 1개 하드 캡을 지키는 쪽을 택했다.
