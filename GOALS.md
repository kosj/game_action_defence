# 목표 명확성 (Goal Clarity) — 현행 기준

> **2026-08 재작성(HANDOFF P3-2).** 이전 판은 **웨이브제 + 난이도 3종(Easy/Normal/Hard)** 을
> 전제로 쓰였다 — "10웨이브 도달 = STAGE CLEAR", "난이도별 목표 웨이브 8/10/12",
> "난이도별 클리어 배지". 그 시스템은 전부 없어졌다.
> 아래는 **지금 있는 것**과 **아직 없는 것**을 현행 코드 기준으로 다시 적은 것이다.

## 지금의 목표 구조

이 게임은 **단일 모드 · 30분 생존 클리어 + 무한 오버타임**이다(`data/difficulty.tres`
`clear_seconds = 1800`). 웨이브는 없고, 런 안의 리듬은 **시간 기반 마일스톤**이 만든다.

| 층위 | 질문 | 지금의 해답 | 어디에 |
|---|---|---|---|
| **마이크로** (지금) | "다음에 뭐가 오지?" | 타임라인 바의 다음 눈금 + 카운트다운 배너 | `TimelineBar.gd` · `HUD._on_forecast` |
| **매크로** (이 판) | "언제 끝나?" | 남은 시간 카운트다운 → 30:00 생존 시 `CLEAR` 배너 | `HUD._on_run_progress` · `Events.run_cleared` |
| **메타** (반복 동기) | "그다음엔?" | 퀘스트 3트랙(처치·보스·생존) · 도전과제 · 메타 강화 · 랭킹 | `QuestManager` · `AchievementManager` · `MetaManager` |

**마일스톤 주기** — 보스 600초, 엘리트 300초(`difficulty.tres`). 30분 런에서 보스 2~3회 ·
엘리트 5회. 이것이 런을 나누는 유일한 구조다(`Events.milestone_reached`).

## ✅ 해결된 것 (이전 판의 P0)

| 이전 항목 | 현재 상태 |
|---|---|
| 다음 목표 배너 | **타임라인 바**(P1-4) — 상단에 엘리트/보스/클리어 눈금과 현재 위치. 문구 한 줄이 아니라 리듬 전체를 보여주는 쪽을 택했다 |
| 보스 임박 경고 | **카운트다운 배너**(P1-4) — 보스 60초 전 · 엘리트 20초 전 |
| 결승선 표시 | 남은 시간 카운트다운이 상시 노출(`TimeLabel`). 30:00 도달 시 `CLEAR` 배너 + 오버타임 전환 |

이전 판의 P1(승리 화면·엔들리스 분기·클리어 배지)은 **설계가 바뀌어 무효**다 —
클리어가 런의 끝이 아니라 오버타임으로 이어지는 구조라 "승리 화면 → 계속/메뉴 분기"가 성립하지 않는다.

## 아직 없는 것

1. **결과 화면의 목표 대비 피드백** — 게임오버 통계는 처치 수·생존 시간을 보여주지만,
   "클리어까지 얼마나 남았었는가"를 말해 주지 않는다. 30분 중 18분에서 죽은 판과 4분에서 죽은 판이
   같은 화면으로 끝난다. *다음 판을 시작할 이유*를 만드는 자리다.
2. **최초 진입 온보딩** — 첫 판에서 타임라인 바가 무엇인지 알려 주지 않는다.
   눈금의 의미(주황=엘리트, 붉은색=보스)는 한 번 겪어야 알게 된다.
3. **보스 등장 연출** — 이름/풀스크린 인트로는 없다. 배너 한 줄 + 격리 구역 전개가 전부다.

## 성공 기준

- ~~플레이어가 시작 10초 안에 "이 판의 목표"를 말할 수 있다~~ → **충족**(남은 시간 + 타임라인 바)
- ~~"게임오버" 외에 "클리어"라는 종료 상태가 존재한다~~ → **충족**(30분 생존 = `run_cleared`)
- 게임오버 시 **결승선 대비 위치**를 알아 다음 판을 시작할 이유가 생긴다 → **미충족**(위 1번)

## 코드 훅

- 마일스톤 예정 시각: `Events.forecast_changed(next_boss, next_elite)` — **`ZombieSpawner` 가 소유한
  실제 예약값**을 흘린다. 주기 상수로 따로 계산하지 말 것(보스는 전투 중 미뤄지고 치트로도 밀린다).
- 클리어/오버타임: `Events.run_progress(elapsed, clear)` · `Events.run_cleared`.
- 런 구간: `Events.milestone_reached(index)` — 보스 처치 시점. 퀘스트·도전과제 저장도 여기에 물려 있다.
- 문구: `Locale.STRINGS` 의 `hud_boss_in_fmt` · `hud_elite_in_fmt` · `run_cleared` · `hud_overtime`.
