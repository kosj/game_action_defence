# 밸런스 — 측정 방법 · 판정 기준 · 실측 (2026-08-18)

이 문서는 **측정에서 나왔다.** 수식 추정이 아니라 엔진을 그대로 돌린 결과다.

> 이전 판(v1.1)은 폐기했다. 난이도 3종(Easy/Normal/Hard)·웨이브별 킬 목표·상점 비용 곡선을
> 기준으로 쓰였는데 **셋 다 지금 게임에 없다.** 게다가 근거였던 시뮬레이터가 레포에 없어
> 재현이 불가능했고, DPS 를 수식으로 추정해 장판·설치물·연쇄 무기를 잡지 못했다.
> 같은 실수를 막으려고 이번엔 **측정 도구를 레포에 넣었다**(`tools/sim_balance.*`).

---

## 1. 측정 방법

```sh
export GODOT=/path/to/godot
python3 tools/sim_balance.py --runs 12 --character veteran        # 분포 + 판정
python3 tools/sim_balance.py --runs 8 --all-characters            # 캐릭터 상대 비교
python3 tools/sim_balance.py --runs 8 --csv out.csv               # 원자료
```

오토플레이(`Cheats.autoplay`)가 한 판을 끝까지 진행하고, 판마다 프로세스를 새로 띄워
생존 시간·레벨·처치·피격·보스전 소요·최종 빌드를 JSON 한 줄로 남긴다.
`user://` 도 판마다 격리해 메타 골드·과제·퀘스트가 뒤 판으로 새지 않는다.

### 이 수치를 어떻게 읽어야 하나 ⚠️

| 한계 | 뜻 |
|---|---|
| 빌드 페르소나가 둘이다 | `--persona random` = 무작위 카드 = **하한선** · `--persona greedy` = 진화 지향 집중 빌드 = **상한 근사**. 둘 다 재야 한다 — 하한만 보면 "너무 어렵다", 상한만 보면 "너무 쉽다"로 기운다 |
| 이동 AI 는 좀비 반발 + 젬 수집이 전부다 | 사람의 판단(위치 선점, 위험 감수)이 없다 |
| 교전 이탈(도주 루프) 판이 섞일 수 있다 | 2026-08-18 에 원인을 고쳐 12판 중 0~1판으로 줄었다(그 전엔 20~40%). 남는 판은 분당 처치 40 미만으로 걸러 집계에서 제외한다(`KILLS_PER_MIN_FLOOR`) |
| 시드를 고정해도 완전 재현은 안 된다 | 물리·타이밍이 프레임 스케줄에 좌우된다. 판수를 늘려 분포로 본다 |

**결론: 절대값을 "이 게임의 난이도"로 읽지 말 것.** 이 도구가 신뢰할 수 있는 용도는 둘이다 —
**조정 전/후 비교**와 **캐릭터·테마 간 상대 비교**.

---

## 2. 판정 기준

무엇이 "맞는 밸런스"인지 먼저 정하지 않으면 숫자를 봐도 판단할 수 없다.
아래는 설계 목표이며, `tools/sim_balance.py` 의 `TARGETS` 와 같은 값이다 — **바꿀 땐 둘 다 바꾼다.**

| 지표 | 목표 | 왜 이 값인가 |
|---|---|---|
| 랜덤 빌드 생존 중앙값 | **12~18분** | 하한선이다. 30분을 클리어해 버리면 목표선이 무의미하고, 8분 미만이면 빌드 운이 곧 사형이라 배울 기회가 없다. **⚠️ 이 값은 오토플레이 기준이다 — 사람에게 적용하지 말 것**(§3-6) |
| 보스전 소요 | **20~40초** | 60초↑는 체력 스펀지, 10초↓면 이정표가 아니다 |
| 첫 피격 시점 | **90초 이후** | 초반은 조작을 익히는 구간이어야 한다 |
| 30분 클리어율(좋은 빌드) | **30~50%** | 클리어가 목표선인 이상, 매번 되면 목표가 아니고 한 번도 안 되면 좌절이다 |

> 마지막 항목은 `--persona greedy` 로 측정한다(§3-4). **현재 실측 0%** — 30분은커녕 10분을 못 넘는다.

---

## 3. 실측 (2026-08-18 · Godot 4.3 헤드리스 · 교외)

> 아래는 **오토플레이 도주 루프를 고친 뒤**(P2-8) 다시 잰 값이다. 그 전 측정치는 AI 가 판의
> 20~40% 를 버리던 상태라 비교 대상이 아니다 — 폐기했다.

| 조건 | n | 생존 중앙값 | 범위 | 보스 처치 | 클리어 |
|---|---|---|---|---|---|
| 베테랑 (기준) | 12 | **9.1분** | 5.3~10.6 | **0** | 0 |
| 베테랑 | 8 | 8.3분 | 6.8~10.2 | **0** | 0 |
| 사냥꾼 | 8 | **5.8분** | 3.5~7.0 | **0** | 0 |
| 엔지니어 | 8 | 6.5분 | 5.7~8.1 | **0** | 0 |
| 베테랑 · `hp_accel_per_min2` 0.042→**0.021** (실험) | 7 | 8.2분 | 5.1~10.7 | **0** | 0 |
| 베테랑 · **탐욕형 빌드**(상한 근사) | 10 | **10.1분** | 6.2~10.5 | **0** | 0 |

첫 피격은 141~170초로 **목표를 만족**한다. 초반 구간은 문제없다.

### 3-1. 발견 ① — 벽은 곡선이 아니라 **첫 보스**다

**보스를 잡은 판이 하나도 없다.** 그리고 난이도 곡선을 **절반으로 완화해도 중앙값이 8.3 → 8.2분으로
사실상 그대로였다.** 최대 생존은 두 조건 모두 **10.2~10.7분에서 멎는다** — 보스는 10:00 에 나온다.

곡선을 만지는 것으로는 이 벽이 안 없어진다는 뜻이다. 오히려 곡선은 이 구간의 한계 요인이 아니다.

- 10분 시점 적 체력 배수 **6.46배**, 보스 체력은 `boss_curve_scale`(0.8) 로 **약 5.6배** 스케일 →
  첫 보스 실효 체력 약 **770**(교외 사냥개 기준) + 호위 정예 5마리.
- 동시에 **`BossArena` 가 플레이어를 반경 480px 원에 가둔다.** 경계는 감전 피해다.
- 즉 **적 체력·물량·회피 봉쇄가 한 순간에 겹친다.**

**⚠️ 해석 주의** — 오토플레이의 생존 수단은 여전히 회피 위주라 아레나 봉쇄에 특히 약하다.
이 데이터로 **"보스가 불공정하다"고 단정할 수 없다.** 말할 수 있는 건 이것이다 —
**모든 런이 같은 지점에서 끝나고, 그 지점은 회피가 봉쇄되는 순간이다.**

### 3-4. 발견 ④ — 상한 근사(집중 빌드)도 같은 벽에서 멎는다

같은 시드 10판을 두 페르소나로 돌린 결과다.

| 페르소나 | 생존 중앙값 | 범위 | 레벨 | 처치 | 최고 무기 Lv | 보스 처치 | 진화 |
|---|---|---|---|---|---|---|---|
| random (하한) | 7.5분 | 2.9~10.4 | 18 | 820 | 6 | 0 | 0 |
| **greedy (상한 근사)** | **10.1분** | **6.2~10.5** | 26 | 1412 | 8 | **0** | **0** |

빌드를 제대로 짜면 생존이 **7.5 → 10.1분**으로 크게 늘어난다 — 이 게임의 파워가 빌드에서
나온다는 설계는 정상 작동한다. **그런데 범위 상단이 10.5분이다.** 10판 중 9판이 10:00 의
보스에서 멎는다. 하한이든 상한이든 **끝나는 지점이 같다.**

**진화는 20판 모두에서 한 번도 발동하지 않았다.** 진화 조건은 ①무기 만렙(6~8) ②짝꿍 패시브
③상자 개봉인데, 집중 빌드가 무기 Lv8 에 도달한 판에서도 조건이 닫히기 전에 런이 끝났다.
~~진화 12종은 도달 불가 콘텐츠다~~ → **AI 한정 결론이었다.** 사람 실측에서 19분 런에
`napalm`(화염병+화약 진화)이 나왔다(§3-6). 다만 **19분을 가야 나온다** — 대부분의 유저는
평생 못 본다. 도달 불가가 아니라 **극소수만 보는 콘텐츠**가 정확한 표현이다.

> 측정 도구 참고: 오토플레이는 원래 **상자를 줍지 않았다**(젬만 수집). 진화·무료 레벨업·골드가
> 통째로 빠져 있어 P1-8 에서 상자 추적을 추가했다. 위 수치는 그 이후 값이다.

### 3-5. 발견 ⑤ — 보스 벽의 실제 원인은 **접촉 피해**였다 (P1-7)

손잡이를 하나씩 바꿔 8판씩 돌렸다. **그럴듯한 후보 8개가 전부 아무것도 바꾸지 못했다.**

| 실험 | 변경 | 보스 처치 |
|---|---|---|
| A 기준 | — | 0/8 |
| B 아레나 반경 480→900 | 회피 공간 확보 | 0/8 |
| C 보스 체력 160→80 | 절반 | 0/8 |
| D 호위 정예 4→0 | 제거 | 0/8 |
| E 경계 감전 1→0 | 제거 | 0/8 |
| F 아레나 680 + 감전주기 1.2 | 완화 | 0/8 |
| G 피격 무적 0.25→0.8 | 3배 | 0/8 |
| J 피격 무적 0.25→0.5 | 2배 | 0/8 |
| **I 보스 접촉 보너스 1→0** | **접촉 피해 −1** | **2/8** |

**진단 계측**(`boss_hp_left_pct` / `player_hp_at_boss` / `survived_after_boss_s`)이 이유를 말해준다:

- 보스 조우 시 플레이어 체력 **10/10**, 좀비 수 22~42로 여유롭다. **10분까지는 전혀 문제가 없다.**
- 그런데 **14초 만에 죽으면서 보스 체력을 58% 깎았다.** 화력은 충분하다 — 24초면 잡는다.
- 즉 **화력이 아니라 생존이 문제다.** 그리고 생존을 끊는 건 아레나도 호위도 체력도 아닌
  **보스 접촉 피해**였다. 접촉 3(테마 보스 2 + `boss_contact_bonus` 1) × 무적 0.25초 =
  **닿은 채 1초면 체력 10이 증발한다.**

`boss_contact_bonus` 를 **1 → 0** 으로 내렸다(피해 3 → 2). 결과:

| 페르소나 | 최대 생존 (전 → 후) | 보스 처치 | 보스전 소요 |
|---|---|---|---|
| random | 10.4분 → **17.5분** | 1/10 | 57초 |
| greedy | 10.5분 → **15.2분** | 1/10 | 42초 |

벽이 절대적이지는 않게 됐다. 다만 통과율 1/10 이고 보스전이 42~57초로 **목표(20~40초)보다 길다** —
여전히 어려운 관문이다.

> **⚠️ 이 조정은 AI 측정에 근거한다.** 오토플레이는 보스 접촉을 피하는 데 특히 서툴고,
> 그건 사람이 학습으로 해결하는 기술이다. **사람 기준으로는 과교정일 수 있다.**
> 실기기 플레이로 검증하고, 필요하면 되돌리거나 회차별로 차등화한다(첫 보스만 0, 이후 1).
> 20분·30분 보스는 아직 아무도 도달하지 못해 **측정된 바 없다.**

### 3-6. 발견 ⑥ — 사람 실측: **AI 수치를 사람에게 적용하면 안 된다** (2026-08-19)

배포 빌드에서 받은 첫 사람 데이터다. 기록 5개지만 이어하기가 섞여 **실제로는 3세션**이다
(샘플의 앞 구간이 같은 값으로 반복되는 것이 이어하기 서명이다).

| | 오토플레이(상한 근사) | **사람** |
|---|---|---|
| 보스 처치 | **1/10** | **3/3** |
| 보스전 소요 | 42초 | 55 → 26 → 18초 (**중앙 26초**) |
| 최고 생존 | 15.2분 | **19.0분** |
| 최종 레벨 | 26 | 36 · 39 · **62** |
| 최다 처치 | ~1,600 | **4,141** |

**오토플레이는 사람의 하한선이 아니라 다른 종(種)이다.** 보스 접촉 회피는 사람이 학습으로
해결하는 기술이고 AI 는 못 한다 — 그 하나로 보스 처치율이 1/10 과 3/3 로 갈렸다.
레벨·처치도 2~3배 차이다.

**따라서 §2 판정 기준의 절대값은 오토플레이 전용이다.** 사람 난이도를 판단할 때는
이 표의 실측을 기준으로 삼고, `sim_balance` 는 **조정 전/후 비교**에만 쓴다(§1 의 원칙 그대로).

#### 보스 조정(P1-7)의 사후 검증 — 되돌리지 않기로 했다

`boss_contact_bonus` 1→0 이 사람 기준 과교정일 수 있다고 적어 두었는데, 실측 결과 **아니었다**:

- 보스전 **중앙 26초**로 목표(20~40초) 한가운데다. 최고 숙련 판 하나가 18초였을 뿐이다.
- 플레이어 체감도 "적당했다".
- 즉 **위험도 자체는 지금이 맞다.** 다만 숙련될수록 짧아지는 추세(55→26→18)라,
  표본이 쌓이면 다시 볼 값이다.

#### 그 외

- **중도 종료 2건은 이탈이 아니다.** 둘 다 이어하기로 재개했다(5.6분·8분에서 끊고 복귀).
  모바일 웹의 정상적인 세션 분할이다 — "이탈 지점"으로 읽으면 안 된다.
- **초반이 항상 안전하지는 않다.** 첫 피격이 10초·39초인 판이 있었다(목표 90초 이후).
- ~~30분 클리어는 여전히 멀다~~ → **2026-08-19 달성됐다**(플레이어 보고). 다만 그 판의 기록은
  남지 않았다 — 클리어 후 메뉴로 나가면 텔레메트리가 아무것도 쓰지 않던 버그였다(P0-7 에서 수정).
  **목표선이 도달 가능하다는 것이 확인됐다** — `CONTENT_PLAN` 의 위협 등급·도감이 이제 의미를 갖는다.

### 3-2. 발견 ② — 난이도 곡선은 4분부터 2차항이 지배한다

`ZombieSpawner._hp_mult()` = `(1 + 0.16·m + 0.042·m²) × 0.95`

| 분 | 배수 | 선형항 | 2차항 |
|---|---|---|---|
| 5 | 2.7 | 0.80 | 1.05 |
| 10 | 6.5 | 1.60 | 4.20 |
| 20 | 20.0 | 3.20 | 16.8 |
| 30 | **41.4** | 4.80 | 37.8 |

두 항이 같아지는 시점은 **3.8분**이다. 그 뒤로는 `hp_per_min`(선형)을 만져도 거의 의미가 없다 —
**후반 난이도는 사실상 `hp_accel_per_min2` 하나가 정한다.**

### 3-3. 발견 ③ — 캐릭터 3종의 차이는 노이즈 안에 있다

5.9 ~ 7.8분으로 벌어져 보이지만 범위가 크게 겹친다(엔지니어 4.6~10.7 vs 베테랑 7.3~10.1).
판수 8로는 우열을 말할 수 없다. **캐릭터 밸런스는 아직 미측정이다** — 판단하려면 판수를
30 이상으로 올리거나, 빌드를 고정한 페르소나가 필요하다(§5).

### 3-7. 발견 ⑦ — **`seed=` 는 판을 재현하지 못한다** (2026-08-19, P1-12)

같은 빌드(`origin/main`)를 **같은 시드로 두 번** 돌린 통제 실험이다.

| | 생존 | 처치 | 레벨 | 첫 피격 | 최종 무기 |
|---|---|---|---|---|---|
| main @ seed 1000 (A) | 637초 | 1,461 | 29 | 143초 | garlic·gun·machinegun·orb |
| main @ seed 1000 (B) | 622초 | 1,629 | 27 | 88초 | garlic·orb·sawblade·**shotgun 8** |

**빌드가 통째로 다르다.** 첫 레벨업 카드부터 갈린다는 뜻이다. `sim_balance.gd` 는 `seed(sd)` 로
전역 RNG 를 심지만, 프로세스 간 재현은 거기서 보장되지 않는다.

**무엇이 무효가 되는가**
- **"같은 시드로 짝지어 비교한다"는 접근은 이 하네스에서 성립하지 않는다.** P1-12 에서
  "등급 1이 기존과 같은가"를 짝 비교로 증명하려다 4/4 시드가 전부 달라 나왔는데,
  통제 실험을 해 보니 **변경과 무관한 하네스의 성질**이었다. 시드는 판을 고정하지 않고
  **분산시킬 뿐**이다 — 비교는 언제나 분포로 해야 한다.
- ⚠️ **P1-14(일일 시드 챌린지)의 전제가 이것이다.** 그 항목은 "`ZombieSpawner` 의 `randf` 10곳을
  시드 기반 RNG 로 바꾸면 같은 시드가 같은 판을 만든다"고 적혀 있다. 위 결과는 **스폰만
  고쳐서는 부족하다**는 것을 말한다 — 레벨업 카드 추첨을 포함해 판을 만드는 모든 추첨이
  같은 결정론 위에 올라와야 하고, 착수 전에 **이 통제 실험부터 다시 돌려 원인을 짚어야 한다.**

### 3-8. 발견 ⑧ — 위협 등급의 곡선 (2026-08-19, P1-12)

같은 조건(베테랑·교외·탐욕형)으로 세 갈래를 각각 12판씩 돌렸다. **통제군은 그때의
`origin/main`** 이다 — `BALANCE.md` 의 기존 수치는 다른 세션·다른 기기에서 잰 것이라
이번 비교의 통제로 쓸 수 없다(§3-7 의 이유와 같다).

| 조건 | n | 생존 중앙값 | 범위 | 레벨 | 처치 | 첫 피격 |
|---|---|---|---|---|---|---|
| `origin/main` (통제) | 12 | 8.9분 | 6.2~10.7 | 22 | 1,233 | 120초 |
| **등급 1** | 12 | **9.5분** | 2.8~10.6 | 24 | 1,404 | 106초 |
| **등급 20** | 9 | **6.6분** | 3.5~10.5 | 20 | 831 | 60초 |

**등급 1 = 통제군.** 중앙값 차이 0.6분은 범위가 거의 완전히 겹치는 수준이고, §3-7 에서 확인한
하네스 자체의 흔들림(같은 빌드·같은 시드로 622초 vs 637초)보다 작다. 애초에 등급 1 의 배수는
전부 정확히 1.0 이라 **곱해도 값이 변하지 않는다** — 측정은 그것을 확인해 줄 뿐 증명하지 않는다.
증명은 `scenes/ThreatTest.tscn` 의 T1(항등원 검사)이 한다.

**등급 20 은 확실히 어렵다.** 생존 −30%, 처치 −41%, 첫 피격 −43%. 세 지표가 같은 방향으로
움직였다. 교전 이탈도 등급 1의 0판에서 **3판**으로 늘었다 — 압박이 커질수록 오토플레이가
도주 루프에 더 자주 빠진다(사람에게는 해당하지 않는 성질이다, §3-6).

⚠️ **인접 등급끼리는 이 하네스로 구분할 수 없다.** 등급 1 vs 5 를 10판씩 재보니 7.6분 vs
8.2분으로 **순서가 뒤집혀** 나왔다. 등급 5의 실제 변화량(적 체력 +10% 등)이 판별 한계보다
작다는 뜻이다(§3-3 의 캐릭터 문제와 같다). 단조성은 **양 끝에서만 확인된 것**이고,
등급별 곡선을 실제로 조율하려면 판수를 30 이상으로 올려야 한다.

---

### 3-9. 발견 ⑨ — 후반 이속: **문제는 추월 자체가 아니라 그 시점이다** (2026-08-20, P1-5)

P1-5 는 "후반에 스프린터가 플레이어보다 빨라져 회피가 불가능해진다 → 이속 패시브가
사실상 강제된다"는 가설로 열린 항목이었다. 데이터에서 값을 뽑아 계산해 보니 **가설의 전제는
맞지만 결론은 틀렸다.**

**속도 공식** (`ZombieSpawner._speed_mult()`):
```
적 속도 = 좀비 기본 속도 × min(speed_cap, 1 + 분 × speed_per_min) × 0.98 × 위협 배수
플레이어 속도 = 220 + 30 × 이속 합계        (Player.gd:528)
이속 합계 = 운동화 패시브(0~8) + 메타 신속(0~8) + 캐릭터 보정(0 또는 1)
```
현재 값: 최속 좀비 = 스프린터 150(비명수 145가 바로 뒤), `speed_per_min` 0.03, `speed_cap` 2.0,
모드 배수 0.98, 위협 배수 1.00(1~6등급)~1.15763(17~20등급).

**추월 시점** (위협 1등급):

| 이속 합계 | 플레이어 | 추월 시점 | 비고 |
|---|---|---|---|
| 0 | 220 | **16.6분** | 헌터(캐릭터 보정 0) + 메타 0 + 운동화 0 |
| 1 | 250 | 23.4분 | 베테랑·엔지니어의 **시작 상태** |
| 2 | 280 | 30.2분 | 클리어 시점 직후 |
| 3 이상 | 310+ | **영구 안전** | 속도 캡(×2.0)에 닿아도 못 따라잡는다 |

읽는 법:
- **`speed_cap` 은 30분 런 안에서 걸리지 않는다.** ×2.0 도달은 33.3분이다 — 즉 캡은 클리어
  포맷에서 죽은 값이고, 후반 압박은 끝까지 선형으로 증가한다. 30분 시점 스프린터는 279.
- **투자 0으로 30분을 도망만 쳐서 클리어할 수는 없다**(279 > 220). 이건 **의도된 압박**이다 —
  뿌리칠 수 있으면 화력 빌드가 의미를 잃는다.
- **해법은 싸다.** 30분 시점 기준 합계 2면 이미 뿌리친다. 베테랑·엔지니어는 캐릭터 보정으로
  이미 1을 갖고 시작하므로, 메타 신속 1레벨(80골드) 또는 운동화 1장이면 끝난다.
- **위협 등급이 이 손잡이를 다시 연다.** 최고 등급에서는 최대 적 속도가 340 이라 영구 안전선이
  합계 3 → 5 로 올라간다. 즉 "한 번 사면 끝"이 아니라 등급을 올릴수록 다시 값을 치른다.

**사람 실측으로는 아직 확증되지 않았다.** 이속 합계가 기록에 남지 않던 시절의 데이터뿐이라,
운동화 레벨만 알 수 있고 메타 신속 몫이 보이지 않는다. 확인된 것은 *투자 0인 판의 최장 생존이
13.1분(추월 시점 이전)* 이라 **추월 이후 구간을 사람이 실제로 겪은 기록이 없다**는 것뿐이다.
그래서 이번 작업은 수치를 바꾸지 않고 **다음 측정이 결론을 낼 수 있게** 만드는 데 그쳤다:
`speed_lv`(해석된 이속 합계)를 텔레메트리·sim_balance 양쪽 스키마에 넣었고,
`analyze_telemetry.py` 가 이속 합계별 생존을 표로 뽑는다.

**결정: 수치 변경 없음.** 대신 `tools/verify_late_speed.gd` 가 위 설계 의도를 잠근다 —
좀비 속도·`difficulty.tres`·위협 배수·패시브 최대치 중 무엇을 건드려도 다음 5가지가 깨지면
CI 에서 걸린다.

1. 최대 투자(합계 17)로 최고 위협의 최속 좀비를 뿌리칠 수 있다 — 아니면 이속은 죽은 스탯이다
2. 투자 0의 추월 시점이 15분 이후다 — 그 전이면 런 절반이 강제 추격이다
3. 30분 시점에 투자 0은 추월당한다 — 도망 클리어 봉쇄
4. 30분 시점에 합계 3이면 뿌리친다 — 해법이 존재한다
5. `speed_cap` 도달이 30분 이후다 — 캡에 닿으면 후반 압박 증가가 평평해진다

⚠️ 임계값(15분·합계 3)은 **현재 값 바로 바깥에 놓은 것**이지 최적값의 증명이 아니다.
의도를 바꾸려면 상수를 고치기 전에 이 절부터 다시 쓸 것.

---

### 3-10. 발견 ⑩ — 후반 프레임: **범인은 좀비가 아니라 내 탄이다** (2026-08-20, P1-18)

사람 기록에서 후반 fps 가 18까지 떨어졌다. 좀비 수를 원인으로 의심했지만 **데이터가 그것을
부정한다** — 좀비 34마리에서 18fps 인데 143마리에서는 31fps 였다. 그래서 추측을 멈추고
후반 상태를 재현하는 하네스를 만들었다: `tools/bench_lategame.gd`.

**프레임 예산** (헤드리스 · 데스크톱 CPU · 표본 190회 이상):

| 상태 | 좀비 | 탄 | 젬 | `_process` | `_physics_process` |
|---|---|---|---|---|---|
| 초반(0분·기본 인벤토리) | 42 | 67 | — | ~0.7ms | **2.1ms** |
| 후반(26분·엔지니어 만렙 빌드) | 126~282 | **229~409** | 140 | ~1.3ms | **7.9~9.4ms** |

읽는 법:
- **`_process` 는 1ms 대다. 렌더도 아니다** — 드로우콜 162개로 배칭은 멀쩡하다.
  이 게임은 거의 모든 로직이 `_physics_process` 에 있어서, 비용도 전부 거기 있다.
- **웹(wasm)은 데스크톱 네이티브의 3~4배**다. 9.4ms × 3.5 ≈ 33ms → 30fps.
  사람 기록의 18~31fps 와 일치한다(폰이라 더 나쁘다).
- 개체당 비용은 **이미 조여져 있다.** 좀비는 화면 밖 LOD(1/3) · 걷기 30Hz · 좀비끼리 분리
  제거 · 공간 그리드 질의를 이미 쓴다. `Zombie.gd` 주석의 "320마리 5.3ms" 가 이번 측정과
  그대로 맞는다 — 즉 남은 여지는 개체 **단가**가 아니라 개체 **수**다.

**⚠️ 그런데 후반에 개체 수를 채우는 것은 좀비가 아니다.** 사람 기록의 후반 좀비 수는
22~45마리다(동시 상한 320의 1/10). 빌드가 강해서 즉사시키기 때문이다. 대신 **탄이 200~450개**로
가장 큰 개체군이 된다 — 산탄 8 · 개틀링 · 터렛 8 · 궁극기가 동시에 쏟아내는 자기 투사체다.

**그래서 "좀비가 금방 죽어 지루하다"와 "후반에 프레임이 떨어진다"는 같은 뿌리다.**
빌드 파워가 적 성장을 앞질러 → 좀비가 녹고(지루함) → 그 녹이는 수단(수백 발의 탄 + 명중
이펙트 + 젬)이 곧 프레임을 먹는다. 개체를 줄이면서 재미를 살리는 방향은 하나뿐이다:

> **후반은 "수"가 아니라 "질"로 압박한다.** 탄은 수를 줄이고 발당 위력을 올린다.
> 좀비는 수를 줄이고 체력을 올린다. 총 DPS·총 위협은 유지하되 동시 개체 수를 낮춘다.

구체안은 `HANDOFF.md` P1-19 에 있다.

#### ⚠️ 정정 (2026-08-20, P1-21) — 위 표의 수치는 **두 겹으로 잘못 잰 값**이다

두 가지가 동시에 틀려 있었다. 자세한 내역과 재현은 `OPTIMIZATION_PLAN.md` §5-L.

1. **하네스가 죽은 탄을 재고 있었다.** `bench_lategame.gd` 최상위의 `preload` 가 오토로드
   등록 전에 `Bullet.gd`·`Gold.gd` 를 컴파일해 실패시켰고, 실패한 스크립트가 리소스 캐시에
   박혀 **게임이 쏘는 탄 전부가 아무 일도 하지 않는 `Node2D`** 가 됐다. 그 상태에서는 탄이
   좀비를 못 죽이므로 좀비만 쌓인다 — 위 표의 "후반 좀비 126~282" 가 그래서 나온 값이고,
   고친 뒤 같은 명령에서는 **좀비 25~38 · 초당 처치 22.5 · 레벨 42** 가 나온다.
2. **`physics_ms` 는 프레임 비용이 아니다.** `Performance.TIME_PHYSICS_PROCESS` 는 엔진이
   **최근 1초의 최댓값**을 들고 있다가 1초마다 갱신·리셋하는 스파이크 지표다(실측 확인).
   평상시 비용으로 읽으면 2~5배 부풀려 보게 된다.

**다시 잰 값**(센티넬로 매 틱 직접 측정 · 이 컨테이너 CPU 기준):

| 상태 | 좀비 | 탄 | 젬 | 물리 틱(중앙) |
|---|---|---|---|---|
| 후반(26분·엔지니어 만렙·수정 전) | 17~38 | 136~147 | 106~140 | 9.3~10.6ms |
| 같은 상태(P1-21 수정 후) | 17~38 | 128~141 | 107~140 | **6.0~6.6ms** |

**⚠️ "웹은 데스크톱의 3~4배" 도 틀렸다 (2026-08-20 실측).** 그 배수는 아무도 재 본 적 없는
추정치였다. `tools/bench_web.sh` 로 실제 브라우저에서 같은 시나리오를 돌려 보면 **0.99배**다
(렌더 경합이 있는 판에서도 1.48배). 즉 후반 30fps 의 원인은 WASM 이 아니다 — 남은 변수는
**폰 CPU 의 절대 속도**와 **모바일 GPU fill-rate** 이고 그것만 실기기가 필요하다.
상세는 `OPTIMIZATION_PLAN.md` §5-L "웹 실측".

**결론 중 살아남은 것과 뒤집힌 것을 나눠 둔다.**

- ✅ **살아남음** — 후반에 개체 수를 채우는 것은 좀비가 아니라 탄·젬이다. 고친 계측에서도
  좀비 17~38 vs 탄 128~147 · 젬 107~140 이다.
- ❌ **뒤집힘** — "그래서 탄이 프레임을 먹는다"는 인과는 성립하지 않는다. 직진탄 1발은
  **6.0µs**, 젬 1개는 **3.4µs** 다. 탄 140발을 70발로 줄여도 프레임은 0.4ms 남짓 준다.
  실제 비용은 **개체 수가 아니라 유도탄의 조준 질의**에 있었고(반경 420px = 셀 225칸을 매
  프레임), 그것을 고친 것만으로 해당 계통이 5.12ms → 1.08ms 가 됐다.

→ **P1-19(탄 수 줄이기)는 프레임 대책이 아니라 재미(지루함) 대책으로 판단하는 편이 정확하다.**
   프레임 쪽 몫은 P1-21 이 가져갔다. 밸런스 판단 자체(후반이 "수"가 아니라 "질"이어야 한다)는
   근거 ①②(사람의 말 · 분당 처치 7배)만으로 충분히 선다.


⚠️ **하네스의 한계 — A/B 대조에는 쓸 수 없다.** 오토플레이가 판을 굴리므로 같은 설정에서도
개체 수가 크게 흔들린다(같은 명령으로 좀비 282 vs 100). 값 하나를 바꿔 비교하려면
`probe=` 모드(스포너·오토플레이 정지 + 지정 개체만 N개)를 쓰거나, 판수를 늘려야 한다.
표본이 30 미만이면 하네스가 스스로 "믿을 수 없음"을 찍는다 — 그 줄이 보이면 수치를 버릴 것.

**남긴 계측** — 분당 샘플에 `fps`·`frame_ms`·`zombies` 를 넣었다. 이제 사람 기록만으로
"몇 분부터 무너지는지" 곡선이 나온다(`analyze_telemetry.py` 가 표로 뽑는다).
지금까지는 종료 시점 한 장뿐이라 그 질문에 답할 수 없었다.

---

### 3-11. 발견 ⑪ — 탄 예산: **무기 두 종이 절반을 쓰고 있었다** (2026-08-20, P1-19)

§3-10 에서 후반 프레임을 먹는 것이 좀비가 아니라 **플레이어 자신의 투사체**라는 것까지 왔다.
그래서 무기 하나씩만 놓고 동시 탄 수를 셌다(`bench_lategame.gd` 의 `w=` 모드):

| 무기 | 동시 탄 |
|---|---|
| **개틀링 Lv5**(진화) | **113** |
| **기관총 Lv8** | **103** |
| 터렛 Lv8 | 36 |
| 산탄총 Lv8 | 32 |
| 화염병 Lv8 | 24 |
| 레일건 Lv5 | 20 |

**두 무기가 나머지의 3~5배다.** 원인은 발사 간격이다 — 기관총 0.16초, 개틀링 0.10초로
다른 무기(0.5~2.6초)와 자릿수가 다르다.

**조치: 발사 간격 ×2 · 발당 위력 ×2.** 레벨별로 검산해 **모든 레벨에서 DPS 가 정확히 동일**하고
초당 탄 수만 정확히 절반이 되도록 맞췄다:

| 무기 | 전 | 후 | 만렙 DPS | 만렙 탄/초 |
|---|---|---|---|---|
| 기관총 | 간격 0.16 · 위력 1+1/lv | 간격 0.32 · 위력 2+2/lv | 154.2 → **154.2** | 19.3 → **9.6** |
| 개틀링 | 간격 0.10 · 위력 1+2/lv | 간격 0.20 · 위력 2+4/lv | 345.8 → **345.8** | 38.4 → **19.2** |

실측: 개틀링 Lv5 동시 탄 113 → **56**, 기관총 Lv8 103 → **68**.
만렙 빌드 전체로는 동시 탄 중앙 407 → **262 (−36%)**.

⚠️ **그런데 프레임은 그만큼 좋아지지 않는다.** 같은 조건 3회씩 재보니 `_physics_process` 는
6.3ms → 7.7ms 로 오히려 올랐다. **탄이 줄어든 만큼 좀비가 덜 죽어 그 자리를 채우기 때문이다**
(하네스의 오토플레이는 사람보다 화력이 약해 좀비가 상한 쪽으로 쌓인다).
사람의 실제 후반은 좀비가 22~45마리로 상한(320)에서 한참 아래라 이 되메움이 훨씬 작지만,
**"탄만 줄이면 프레임이 좋아진다"는 주장은 이 데이터로 할 수 없다.**
개체 예산은 적 쪽(`max_z_cap`)과 같이 내려야 한다 — 그것이 P1-20 이다.

**남긴 가드** — `tools/verify_bullet_budget.gd`. 카탈로그에서 무기별 **최악 발사율**
(만렙 + 발사속도 패시브 만렙 → `fire_interval × 0.5` 하한이 걸리는 상태)을 계산해
**32발/초**를 넘으면 CI 에서 막는다. 바꾸기 전 개틀링은 60발/초였다(가드로 재현 확인).
임계값은 현재 최악값(개틀링 30) 바로 위에 놓은 것이지 최적값이 아니다 — "오늘보다 나빠지지
않는다"를 잠그는 용도다.

### ⚠️ 곁가지 발견 — 생성기와 `.tres` 가 어긋나 있었다

규약대로 `gen_item_catalog.gd` 를 고쳐 재생성했더니 **무기 4종이 조용히 사라졌다** —
부메랑과 **궁극기 3종 전부**. 생성기에 아예 없고 `data/item_catalog.tres` 에만 손으로 들어가
있었다(마늘의 표기·색도 마찬가지). 궁극기는 캐릭터마다 하나씩 묶여 있어
(`CharacterData.ultimate_weapon`) 사라지면 그 캐릭터가 반쪽이 된다.

생성기에 되살렸고, `verify_bullet_budget.gd` 가 **궁극기 소실만은** CI 에서 잡는다.
나머지는 재생성 전후 목록 대조가 유일한 안전망이라 `CLAUDE.md` 에 절차를 적었다.

---

### 3-12. 발견 ⑫ — 후반은 **DPS 제한이 아니라 스폰 제한**이다 (2026-08-20, P1-20)

"뒤로 갈수록 좀비가 금방 죽어서 지루하다"를 고치려고 적 체력부터 올리려 했는데,
데이터를 다시 보니 **전제가 틀렸다.**

사람 기록의 후반 동시 좀비는 **22~45마리**인데 동시 상한은 **320**이다. 즉 나오는 족족 죽고 있고,
필드가 상한 근처에 간 적이 없다. **그러면 분당 처치는 플레이어의 화력이 아니라 스포너의 출력이
정한다.** 26분 스폰 간격 0.1초 = 초당 10마리 = 분당 600 — 실측 500~760과 맞는다.

**따라서 체력만 올려서는 처치 수가 줄지 않는다.** 줄어드는 것은 없고, 좀비만 더 오래 살아
동시 개체가 늘어 프레임이 나빠진다. 처치마다 나오는 젬·이펙트·데미지 숫자도 그대로다.

**조치 — 유입을 줄이고 질을 올린다.** `late_hp_start_s`(15분)부터 두 항이 함께 붙는다:

| 분 | 초당 스폰 | 체력 배수 | 유입 압력(스폰×체력) |
|---|---|---|---|
| ≤15 | 변화 없음 | 변화 없음 | 변화 없음 |
| 20 | 4.00 → **3.14** (−22%) | ×21.0 → **×28.5** (+36%) | 84 → 89 |
| 26 | 10.00 → **6.23** (−38%) | ×33.6 → **×50.1** (+49%) | 336 → **312** (−7%) |
| 30 | 10.00 → **5.48** (−45%) | ×43.6 → **×66.1** (+52%) | 436 → 362 |

**난이도(유입 압력)는 사실상 유지되고, 개체 유입만 −38% 다.** 좀비 하나가 1.5배 오래 살아
보이고·다가오고·위협이 되며, 처치 수와 그에 딸린 젬·이펙트 churn 이 같이 준다.

⚠️ **왜 `max_z_cap` 도 `spawn_interval_min` 도 아닌가 — 둘 다 0분부터 효과가 시작된다.**
실측으로 확인했다: 상한 320→220 은 5분 스폰을 −21% 깎아 레벨 28→23, 스폰 하한 0.1→0.16 은
레벨 26→22 로 성장을 눌렀다. **작은 초반 차이가 XP→레벨→화력으로 복리로 커진다.**
시작 시각이 있는 항이면 15분 이전이 정확히 0 이다. 그래서 세 번째 시도에서야 맞았다.

**검증** — `sim_balance` n=6 은 7.8분 → 8.5분(둘 다 10분 보스에서 멎는다). 15분 이전 곡선이
수식상 동일하므로 이 차이는 노이즈다(§3-7).

⚠️ **`bench_lategame.gd` 의 `stress=1` 로는 이 변경을 못 잰다.** stress 는 매 초 좀비를 상한까지
직접 채워 **스포너를 우회**하기 때문이다(실제로 이것 때문에 "효과 없음"으로 잘못 읽을 뻔했다).
스폰 throughput 변경은 정상 모드로 재거나 데이터에서 계산할 것 —
`tools/verify_late_hp.gd` 가 엔진과 같은 식으로 초당 스폰을 찍어 준다.

**남긴 가드** — `tools/verify_late_hp.gd`. 다섯 가지를 잠근다:
후반 가속 시작 ≥15분 · 15분 체력 ≤×14(중반 불변) · 26분 체력 ≥×45(후반이 다시 물러지지 않는다) ·
26분 초당 스폰 ≤7.0 · 26분 유입 압력 ≤360 EHP/s(체력만 올려 압력을 폭증시키지 않는다).
되돌리면 실제로 FAIL 하는 것을 확인했다.

### ⚠️ 곁가지 — `difficulty.tres` 도 생성기와 어긋나 있었다

`gen_difficulty_data.gd` 는 `DifficultyData.new()` 의 **스크립트 기본값을 그대로 저장**할 뿐인데,
커밋된 `.tres` 는 **8개 필드**가 달랐다(동시 상한 175 vs 320, 2차 가속 0.020 vs 0.042 등).
규약대로 재생성했다면 튜닝이 통째로 되돌아갔다 — `item_catalog` 와 같은 사고다(§3-11).
기본값을 배포 값에 맞춰 두었으니, **이 생성기는 "스크립트 기본값을 고치는 것이 곧 데이터를
고치는 것"** 이다. 절차는 `CLAUDE.md` §2.

---

### 3-13. 발견 ⑬ — 좀비 수와 fps 는 **무관하다** · 게이트에 구멍이 있었다 (2026-08-21, P1-25)

P0-11(이어하기 판의 분당 샘플 위조) 수정 이후 처음으로 **사람 기록의 프레임 곡선이 온전해졌다.**
그 곡선으로 §3-10 의 결론을 다시 검정했다. 표본은 사람 판의 분당 샘플 중 좀비>0 인 57개다.

**상관계수 (fps 대비)**

| 변수 | 상관 |
|---|---|
| 동시 좀비 수 | **+0.02** — 사실상 무관 |
| 플레이어 레벨 | −0.39 |
| 경과 분 | −0.40 |
| 노드 수 | −0.36 |

동시 좀비가 가장 많았던 5개 샘플은 **전부 57~60fps** 이고, 그중엔 **99마리에서 60fps** 도 있다.
반대로 좀비 수를 5~15마리로 고정해 비교하면:

| 플레이어 레벨 | frame_ms |
|---|---|
| 3~67 | **27표본 중 24개가 16.7ms**(=60fps) |
| 78 | 23.3ms |
| 97 | 38.3ms |
| 104 | 38.6ms |

**같은 좀비 수에서 빌드만 커지면 프레임이 2.3배 무거워진다.** §3-10 의 방향은 맞았고,
"좀비를 줄이면 나아진다"는 직관은 이 데이터로 완전히 기각된다.

#### 게이트가 초록인데 화면은 26fps 였다

P1-19 가 만든 `verify_bullet_budget.gd` 는 `module == "projectile"` 인 무기의 **발사율**만 봤다.
카탈로그 30종 중 그 모듈은 **7종(23%)** 뿐이다. 실제로 위 표의 **38.3ms(26fps) 헌터 빌드**를
그 방식으로 재면 **4.2발/초**가 나온다 — 그 빌드의 projectile 무기가 crossbow 하나여서다.

```
hunter (lv97 · 38.3ms)  →  옛 게이트 합계 4.2 발/초
    crossbow  Lv8   4.2 발/초          ← 게이트가 보는 유일한 무기
    chainsaw / drone / inferno / railgun / tesla / ult_arrowstorm  → 전부 미검사
```

애초에 발사율로 잴 수 없는 것들이 있다. 장판(molotov·garlic·sanctuary)·궤도체(orb·drone)·
설치물(turret·mine)은 발사율이 0 이지만 상시 개체를 차지한다.
**필요한 값은 발사율이 아니라 동시 존재 개체 수다.**

#### 그래서 실측으로 바꿨다

`Main` 을 헤드리스로 띄워 20분 시점으로 시계를 당기고, 스포너를 끈 뒤 **죽지 않는 정지 좀비
30마리**를 세운다(사람 후반 판의 동시 좀비 13~56 의 한가운데). 그 위에서 무기를 하나씩
만렙으로 갈아 끼우며 씬 안 스크립트 노드 수를 매 프레임 세고 5초 평균을 낸다. 13초면 끝난다.

**무기 단독 만렙(패시브 만렙) · 동시 존재 개체 수 — 상위 10종**

| 무기 | 개체 | | 무기 | 개체 |
|---|---|---|---|---|
| gatling | 76 | | shotgun | 54 |
| dragonsbreath | 68 | | crossbow | 49 |
| railgun | 66 | | orb | 49 |
| sawstorm | 61 | | gun | 49 |
| machinegun | 58 | | napalm | 47 |

**후반 빌드 합계**

| 빌드 | 개체 | 사람 기록 fps |
|---|---|---|
| 최악 조합(도구가 단독 상위 7종으로 조립) | 173 | — |
| 엔지니어 d5a7ffb | 118 | 18 |
| 헌터 f0e3e90 | 87 | 43 |
| 베테랑 c711f84 | 87 | 32 |
| 베테랑 9b79bdc | 79 | 30 |

⚠️ **개체 수가 fps 를 완전히 설명하지는 않는다.** 최악(118)이 최저 fps(18)인 것은 맞지만
87 이 32fps·43fps 로 갈리고 79 가 30fps 다. 개체 수는 **지배적인 항이지 유일한 항이 아니다**
— 개체당 단가가 종류마다 다르기 때문이다(`OPTIMIZATION_PLAN.md` §5-M). 이 게이트의 목적은
fps 예측이 아니라 **"오늘보다 개체가 늘어나지 않는다"를 잠그는 것**이다.

#### 검정 — 게이트가 실제로 잡는가

P1-19 이전 상태를 생성기로 되돌려(개틀링 발사 간격 0.20 → 0.10) 재현했다.

| | 옛 게이트 | 새 게이트 |
|---|---|---|
| 개틀링 발사 간격 절반 | FAIL(60발/초) | **FAIL** — 개체 74 → 116(상한 85) · 최악 조합 173 → 200 |
| 몰로토프 투척 간격 2.60 → 0.60 | **통과** ❌ | 개체 45 → 57 (변화는 잡히나 상한 미만) |

두 번째 줄이 P1-25 의 요지다. **옛 게이트는 비-projectile 무기의 회귀를 구조적으로 볼 수 없다.**
(몰로토프는 장판 수가 내부적으로 묶여 있어 5배 빨리 던져도 +12 개에 그친다 — 실측이 그렇게
말해 주는 것 자체가 이 도구를 바꾼 값어치다. 예전에는 그것도 알 수 없었다.)

**커버리지를 항상 출력한다.** "초록이 무엇을 보장하는지 출력만 봐서는 알 수 없었던 것"이
이 항목이 생긴 원인이므로, 30종 중 몇 종을 쟀는지 매번 찍고 하나라도 빠지면 실패시킨다.

---

## 4. 조정 손잡이 — 셋뿐이다

한 번에 **하나만** 바꾸고 같은 조건으로 재측정한다.

| 손잡이 | 현재 | 무엇을 정하나 |
|---|---|---|
| `difficulty.tres` · `hp_accel_per_min2` | 0.042 | 후반 벽의 위치. **단, 10분 이전 구간에서는 효과가 없다** — 절반으로 낮춰도 중앙값 8.3→8.2분(§3-1) |
| `difficulty.tres` · `max_z_cap` / `spawn_interval_min` | 320 / 0.1 | 밀도 압박. 체력 대신 물량으로 압박하는 쪽이 뱀서식 문법에 가깝다 |
| `difficulty.tres` · `speed_per_min` / `speed_cap` | 0.03 / 2.0 | 후반 이속. 실제로 듣는 쪽은 `speed_per_min` 이다 — 캡(×2.0)은 33.3분이라 30분 런에서는 걸리지 않는다(§3-9). 손대면 `verify_late_speed.gd` 가 의도 이탈을 잡는다 |

보스 쪽에서 **실제로 듣는 손잡이는 `boss_contact_bonus` 하나였다**(§3-5). 체력·아레나·호위·
경계 감전·무적 시간은 전부 효과가 없었다 — 후보를 눈으로 고르지 말고 하나씩 재고 확인할 것.

---

## 5. 다음에 필요한 것

1. ~~빌드 페르소나(탐욕형)~~ — ✅ 완료(P1-8). `--persona greedy` 로 상한 근사를 잰다(§3-4).
   **남은 것: 무기 고정 페르소나** — 무기 28종 중 죽은 무기를 가려내려면 필요하다.
   §3-3 의 캐릭터 격차가 실제인지 AI 상성인지도 그걸로 가린다.
2. ~~오토플레이 교전 이탈 수정~~ — ✅ 완료(P2-8). 원인은 사이드뷰 자동사격이 **이동 중인 좌우
   방향으로만 나간다**는 것이었다(`Player._update_facing`). 무리에서 수평으로 도망치면 총구가
   무리 반대쪽을 향해 한 발도 맞지 않는다. 쫓기는 동안 가로를 조준축으로 쓰고 회피를 세로로
   옮겨 해결했다 — 12판 중 이탈이 5판 → 0~1판.
3. **텔레메트리(로컬)** — ✅ 완료(P1-9). `Telemetry` 오토로드가 한 판이 끝날 때 요약 1줄을
   `user://telemetry.jsonl` 에 남긴다. **스키마를 sim_balance 와 같게 맞춰** 사람 데이터와
   AI 측정을 한 도구로 나란히 본다:
   ```sh
   python3 tools/analyze_telemetry.py telemetry.jsonl --compare sim.csv
   ```
   기록 얻는 법 — **메인 메뉴 > 옵션 > 플레이 기록 복사**(클립보드). **릴리스 빌드에서도 보인다** —
   출시 전 테스터에게서 데이터를 받기 위한 것이고, 치트를 열지 않고도 회수할 수 있다.
   치트가 발동한 판은 `cheated: true` 로 표시되고 분석 도구가 기본 제외한다.
   중도 이탈(웹 탭 닫힘)도 60초 스냅샷으로 잡아 "어디서 그만두는지"를 남긴다.
   **이어하기로 시작한 판은 `resumed: true`** — 세이브에서 경과 시간만 복원되고 피격·보스
   수치는 재개 이후만 세어지므로 분석에서 기본 제외한다. **측정용 플레이는 새 게임으로 한다.**
   **두 출처가 어긋나는 지점이 곧 "AI 가 틀린 곳"이다** — 특히 보스 접촉 회피가 그렇다.
   **기기 밖으로는 아무것도 보내지 않는다.** 원격 수집은 백엔드·개인정보처리방침·동의가
   정해진 뒤 별도 작업이다(`HANDOFF.md` P1-10).

---

## 재현

```sh
export GODOT=/path/to/godot4.3
python3 tools/sim_balance.py --runs 12 --character veteran --seed-start 2500
python3 tools/sim_balance.py --runs 8 --all-characters --seed-start 2000
```
위 실측은 이 두 명령 + `hp_accel_per_min2` 를 0.021 로 바꾼 실험(시드 3000) 결과다.
판당 약 15~30초(헤드리스, 4코어 기준 병렬 3).
